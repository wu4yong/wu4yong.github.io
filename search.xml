<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Mysql基础</title>
    <url>/2026/06/05/database/Mysql/</url>
    <content><![CDATA[基础篇通用语法及分类
DDL：数据定义语言，用来定义数据库对象（数据库、表、字段）
DML：数据操作语言，用来对数据库表中的数据进行增删改
DQL：数据查询语言，用来查询数据库中表的记录
DCL：数据控制语言，用来创建数据库用户、控制数据库的控制权限

DDL（数据定义语言）数据定义语言数据库操作
查询所有数据库：SHOW DATABASES;


查询当前数据库SELECT DATABASE();


创建数据库：CREATE DATABASE [ IF NOT EXISTS ] 数据库名 [ DEFAULT CHARSET 字符集] [COLLATE 排序规则 ];


删除数据库：DROP DATABASE [ IF EXISTS ] 数据库名;


使用数据库：  USE 数据库名;

]]></content>
      <categories>
        <category>database</category>
      </categories>
      <tags>
        <tag>MySQL数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Java Basic</title>
    <url>/2026/06/04/java/Java%20Basic/</url>
    <content><![CDATA[基础篇
基础篇要点：算法、数据结构、基础设计模式

1. 二分查找要求

能够用自己语言描述二分查找算法
能够手写二分查找代码
能够解答一些变化后的考法

算法描述

前提：有已排序数组 A（假设已经做好）

定义左边界 L、右边界 R，确定搜索范围，循环执行二分查找（3、4两步）

获取中间索引 M &#x3D; Floor((L+R) &#x2F;2)

中间索引的值  A[M] 与待搜索的值 T 进行比较
① A[M] &#x3D;&#x3D; T 表示找到，返回中间索引
② A[M] &gt; T，中间值右侧的其它元素都大于 T，无需比较，中间索引左边去找，M - 1 设置为右边界，重新查找
③ A[M] &lt; T，中间值左侧的其它元素都小于 T，无需比较，中间索引右边去找， M + 1 设置为左边界，重新查找

当 L &gt; R 时，表示没有找到，应结束循环



更形象的描述请参考：binary_search.html

算法实现
public static int binarySearch(int[] a, int t) &#123;    int l = 0, r = a.length - 1, m;    while (l &lt;= r) &#123;        m = (l + r) / 2;        if (a[m] == t) &#123;            return m;        &#125; else if (a[m] &gt; t) &#123;            r = m - 1;        &#125; else &#123;            l = m + 1;        &#125;    &#125;    return -1;&#125;

测试代码
public static void main(String[] args) &#123;    int[] array = &#123;1, 5, 8, 11, 19, 22, 31, 35, 40, 45, 48, 49, 50&#125;;    int target = 47;    int idx = binarySearch(array, target);    System.out.println(idx);&#125;

解决整数溢出问题
当 l 和 r 都较大时，l + r 有可能超过整数范围，造成运算错误，解决方法有两种：
int m = l + (r - l) / 2;

还有一种是：
int m = (l + r) &gt;&gt;&gt; 1;

其它考法

有一个有序表为 1,5,8,11,19,22,31,35,40,45,48,49,50 当二分查找值为 48 的结点时，查找成功需要比较的次数 

使用二分法在序列 1,4,6,7,15,33,39,50,64,78,75,81,89,96 中查找元素 81 时，需要经过（   ）次比较

在拥有128个元素的数组中二分查找一个数，需要比较的次数最多不超过多少次


对于前两个题目，记得一个简要判断口诀：奇数二分取中间，偶数二分取中间靠左。对于后一道题目，需要知道公式：
$$n &#x3D; log_2N &#x3D; log_{10}N&#x2F;log_{10}2$$
其中 n 为查找次数，N 为元素个数
2. 冒泡排序要求

能够用自己语言描述冒泡排序算法
能够手写冒泡排序代码
了解一些冒泡排序的优化手段

算法描述

依次比较数组中相邻两个元素大小，若 a[j] &gt; a[j+1]，则交换两个元素，两两都比较一遍称为一轮冒泡，结果是让最大的元素排至最后
重复以上步骤，直到整个数组有序


更形象的描述请参考：bubble_sort.html

算法实现
public static void bubble(int[] a) &#123;    for (int j = 0; j &lt; a.length - 1; j++) &#123;        // 一轮冒泡        boolean swapped = false; // 是否发生了交换        for (int i = 0; i &lt; a.length - 1 - j; i++) &#123;            System.out.println(&quot;比较次数&quot; + i);            if (a[i] &gt; a[i + 1]) &#123;                Utils.swap(a, i, i + 1);                swapped = true;            &#125;        &#125;        System.out.println(&quot;第&quot; + j + &quot;轮冒泡&quot;                           + Arrays.toString(a));        if (!swapped) &#123;            break;        &#125;    &#125;&#125;


优化点1：每经过一轮冒泡，内层循环就可以减少一次
优化点2：如果某一轮冒泡没有发生交换，则表示所有数据有序，可以结束外层循环

进一步优化
public static void bubble_v2(int[] a) &#123;    int n = a.length - 1;    while (true) &#123;        int last = 0; // 表示最后一次交换索引位置        for (int i = 0; i &lt; n; i++) &#123;            System.out.println(&quot;比较次数&quot; + i);            if (a[i] &gt; a[i + 1]) &#123;                Utils.swap(a, i, i + 1);                last = i;            &#125;        &#125;        n = last;        System.out.println(&quot;第轮冒泡&quot;                           + Arrays.toString(a));        if (n == 0) &#123;            break;        &#125;    &#125;&#125;


每轮冒泡时，最后一次交换索引可以作为下一轮冒泡的比较次数，如果这个值为零，表示整个数组有序，直接退出外层循环即可

3. 选择排序要求

能够用自己语言描述选择排序算法
能够比较选择排序与冒泡排序
理解非稳定排序与稳定排序

算法描述

将数组分为两个子集，排序的和未排序的，每一轮从未排序的子集中选出最小的元素，放入排序子集

重复以上步骤，直到整个数组有序



更形象的描述请参考：selection_sort.html

算法实现
public static void selection(int[] a) &#123;    for (int i = 0; i &lt; a.length - 1; i++) &#123;        // i 代表每轮选择最小元素要交换到的目标索引        int s = i; // 代表最小元素的索引        for (int j = s + 1; j &lt; a.length; j++) &#123;            if (a[s] &gt; a[j]) &#123; // j 元素比 s 元素还要小, 更新 s                s = j;            &#125;        &#125;        if (s != i) &#123;            swap(a, s, i);        &#125;        System.out.println(Arrays.toString(a));    &#125;&#125;


优化点：为减少交换次数，每一轮可以先找最小的索引，在每轮最后再交换元素

与冒泡排序比较

二者平均时间复杂度都是 $O(n^2)$

选择排序一般要快于冒泡，因为其交换次数少

但如果集合有序度高，冒泡优于选择

冒泡属于稳定排序算法，而选择属于不稳定排序

稳定排序指，按对象中不同字段进行多次排序，不会打乱同值元素的顺序
不稳定排序则反之



稳定排序与不稳定排序
System.out.println(&quot;=================不稳定================&quot;);Card[] cards = getStaticCards();System.out.println(Arrays.toString(cards));selection(cards, Comparator.comparingInt((Card a) -&gt; a.sharpOrder).reversed());System.out.println(Arrays.toString(cards));selection(cards, Comparator.comparingInt((Card a) -&gt; a.numberOrder).reversed());System.out.println(Arrays.toString(cards));System.out.println(&quot;=================稳定=================&quot;);cards = getStaticCards();System.out.println(Arrays.toString(cards));bubble(cards, Comparator.comparingInt((Card a) -&gt; a.sharpOrder).reversed());System.out.println(Arrays.toString(cards));bubble(cards, Comparator.comparingInt((Card a) -&gt; a.numberOrder).reversed());System.out.println(Arrays.toString(cards));

都是先按照花色排序（♠♥♣♦），再按照数字排序（AKQJ…）

不稳定排序算法按数字排序时，会打乱原本同值的花色顺序
[[♠7], [♠2], [♠4], [♠5], [♥2], [♥5]][[♠7], [♠5], [♥5], [♠4], [♥2], [♠2]]

原来 ♠2 在前 ♥2 在后，按数字再排后，他俩的位置变了

稳定排序算法按数字排序时，会保留原本同值的花色顺序，如下所示 ♠2 与 ♥2 的相对位置不变
[[♠7], [♠2], [♠4], [♠5], [♥2], [♥5]][[♠7], [♠5], [♥5], [♠4], [♠2], [♥2]]

4. 插入排序要求

能够用自己语言描述插入排序算法
能够比较插入排序与选择排序

算法描述

将数组分为两个区域，排序区域和未排序区域，每一轮从未排序区域中取出第一个元素，插入到排序区域（需保证顺序）

重复以上步骤，直到整个数组有序



更形象的描述请参考：insertion_sort.html

算法实现
// 修改了代码与希尔排序一致public static void insert(int[] a) &#123;    // i 代表待插入元素的索引    for (int i = 1; i &lt; a.length; i++) &#123;        int t = a[i]; // 代表待插入的元素值        int j = i;        System.out.println(j);        while (j &gt;= 1) &#123;            if (t &lt; a[j - 1]) &#123; // j-1 是上一个元素索引，如果 &gt; t，后移                a[j] = a[j - 1];                j--;            &#125; else &#123; // 如果 j-1 已经 &lt;= t, 则 j 就是插入位置                break;            &#125;        &#125;        a[j] = t;        System.out.println(Arrays.toString(a) + &quot; &quot; + j);    &#125;&#125;

与选择排序比较

二者平均时间复杂度都是 $O(n^2)$

大部分情况下，插入都略优于选择

有序集合插入的时间复杂度为 $O(n)$

插入属于稳定排序算法，而选择属于不稳定排序


提示

插入排序通常被同学们所轻视，其实它的地位非常重要。小数据量排序，都会优先选择插入排序

5. 希尔排序要求

能够用自己语言描述希尔排序算法

算法描述

首先选取一个间隙序列，如 (n&#x2F;2，n&#x2F;4 … 1)，n 为数组长度

每一轮将间隙相等的元素视为一组，对组内元素进行插入排序，目的有二
① 少量元素插入排序速度很快
② 让组内值较大的元素更快地移动到后方

当间隙逐渐减少，直至为 1 时，即可完成排序



更形象的描述请参考：shell_sort.html

算法实现
private static void shell(int[] a) &#123;    int n = a.length;    for (int gap = n / 2; gap &gt; 0; gap /= 2) &#123;        // i 代表待插入元素的索引        for (int i = gap; i &lt; n; i++) &#123;            int t = a[i]; // 代表待插入的元素值            int j = i;            while (j &gt;= gap) &#123;                // 每次与上一个间隙为 gap 的元素进行插入排序                if (t &lt; a[j - gap]) &#123; // j-gap 是上一个元素索引，如果 &gt; t，后移                    a[j] = a[j - gap];                    j -= gap;                &#125; else &#123; // 如果 j-1 已经 &lt;= t, 则 j 就是插入位置                    break;                &#125;            &#125;            a[j] = t;            System.out.println(Arrays.toString(a) + &quot; gap:&quot; + gap);        &#125;    &#125;&#125;

参考资料

https://en.wikipedia.org/wiki/Shellsort

6. 快速排序要求

能够用自己语言描述快速排序算法
掌握手写单边循环、双边循环代码之一
能够说明快排特点
了解洛穆托与霍尔两种分区方案的性能比较

算法描述

每一轮排序选择一个基准点（pivot）进行分区
让小于基准点的元素的进入一个分区，大于基准点的元素的进入另一个分区
当分区完成时，基准点元素的位置就是其最终位置


在子分区内重复以上过程，直至子分区元素个数少于等于 1，这体现的是分而治之的思想 （divide-and-conquer）
从以上描述可以看出，一个关键在于分区算法，常见的有洛穆托分区方案、双边循环分区方案、霍尔分区方案


更形象的描述请参考：quick_sort.html

单边循环快排（lomuto 洛穆托分区方案）

选择最右元素作为基准点元素

j 指针负责找到比基准点小的元素，一旦找到则与 i 进行交换

i 指针维护小于基准点元素的边界，也是每次交换的目标索引

最后基准点与 i 交换，i 即为分区位置


public static void quick(int[] a, int l, int h) &#123;    if (l &gt;= h) &#123;        return;    &#125;    int p = partition(a, l, h); // p 索引值    quick(a, l, p - 1); // 左边分区的范围确定    quick(a, p + 1, h); // 左边分区的范围确定&#125;private static int partition(int[] a, int l, int h) &#123;    int pv = a[h]; // 基准点元素    int i = l;    for (int j = l; j &lt; h; j++) &#123;        if (a[j] &lt; pv) &#123;            if (i != j) &#123;                swap(a, i, j);            &#125;            i++;        &#125;    &#125;    if (i != h) &#123;        swap(a, h, i);    &#125;    System.out.println(Arrays.toString(a) + &quot; i=&quot; + i);    // 返回值代表了基准点元素所在的正确索引，用它确定下一轮分区的边界    return i;&#125;



双边循环快排（不完全等价于 hoare 霍尔分区方案）

选择最左元素作为基准点元素
j 指针负责从右向左找比基准点小的元素，i 指针负责从左向右找比基准点大的元素，一旦找到二者交换，直至 i，j 相交
最后基准点与 i（此时 i 与 j 相等）交换，i 即为分区位置

要点

基准点在左边，并且要先 j 后 i

while( i **&lt; j** &amp;&amp; a[j] &gt; pv ) j– 

while ( i &lt; j &amp;&amp; a[i] &lt;&#x3D; pv ) i++


private static void quick(int[] a, int l, int h) &#123;    if (l &gt;= h) &#123;        return;    &#125;    int p = partition(a, l, h);    quick(a, l, p - 1);    quick(a, p + 1, h);&#125;private static int partition(int[] a, int l, int h) &#123;    int pv = a[l];    int i = l;    int j = h;    while (i &lt; j) &#123;        // j 从右找小的        while (i &lt; j &amp;&amp; a[j] &gt; pv) &#123;            j--;        &#125;        // i 从左找大的        while (i &lt; j &amp;&amp; a[i] &lt;= pv) &#123;            i++;        &#125;        swap(a, i, j);    &#125;    swap(a, l, j);    System.out.println(Arrays.toString(a) + &quot; j=&quot; + j);    return j;&#125;



快排特点

平均时间复杂度是 $O(nlog_2⁡n )$，最坏时间复杂度 $O(n^2)$

数据量较大时，优势非常明显

属于不稳定排序


洛穆托分区方案 vs 霍尔分区方案

霍尔的移动次数平均来讲比洛穆托少3倍
https://qastack.cn/cs/11458/quicksort-partitioning-hoare-vs-lomuto


补充代码说明

day01.sort.QuickSort3 演示了空穴法改进的双边快排，比较次数更少
day01.sort.QuickSortHoare 演示了霍尔分区的实现
day01.sort.LomutoVsHoare 对四种分区实现的移动次数比较


7. ArrayList要求

掌握 ArrayList 扩容规则

扩容规则

ArrayList() 会使用长度为零的数组

ArrayList(int initialCapacity) 会使用指定容量的数组

public ArrayList(Collection&lt;? extends E&gt; c) 会使用 c 的大小作为数组容量

add(Object o) 首次扩容为 10，再次扩容为上次容量的 1.5 倍

addAll(Collection c) 没有元素时，扩容为 Math.max(10, 实际元素个数)，有元素时为 Math.max(原容量 1.5 倍, 实际元素个数)


其中第 4 点必须知道，其它几点视个人情况而定
提示

测试代码见 day01.list.TestArrayList ，这里不再列出
要注意的是，示例中用反射方式来更直观地反映 ArrayList 的扩容特征，但从 JDK 9 由于模块化的影响，对反射做了较多限制，需要在运行测试代码时添加 VM 参数 --add-opens java.base/java.util=ALL-UNNAMED 方能运行通过，后面的例子都有相同问题


代码说明

day01.list.TestArrayList#arrayListGrowRule 演示了 add(Object) 方法的扩容规则，输入参数 n 代表打印多少次扩容后的数组长度


8. Iterator要求

掌握什么是 Fail-Fast、什么是 Fail-Safe

Fail-Fast 与 Fail-Safe

ArrayList 是 fail-fast 的典型代表，遍历的同时不能修改，尽快失败

CopyOnWriteArrayList 是 fail-safe 的典型代表，遍历的同时可以修改，原理是读写分离


提示

测试代码见 day01.list.FailFastVsFailSafe，这里不再列出

9. LinkedList要求

能够说清楚 LinkedList 对比 ArrayList 的区别，并重视纠正部分错误的认知

LinkedList

基于双向链表，无需连续内存
随机访问慢（要沿着链表遍历）
头尾插入删除性能高
占用内存多

ArrayList

基于数组，需要连续内存
随机访问快（指根据下标访问）
尾部插入、删除性能可以，其它部分插入、删除都会移动数据，因此性能会低
可以利用 cpu 缓存，局部性原理


代码说明

day01.list.ArrayListVsLinkedList#randomAccess 对比随机访问性能
day01.list.ArrayListVsLinkedList#addMiddle 对比向中间插入性能
day01.list.ArrayListVsLinkedList#addFirst 对比头部插入性能
day01.list.ArrayListVsLinkedList#addLast 对比尾部插入性能
day01.list.ArrayListVsLinkedList#linkedListSize 打印一个 LinkedList 占用内存
day01.list.ArrayListVsLinkedList#arrayListSize 打印一个 ArrayList 占用内存


10. HashMap要求

掌握 HashMap 的基本数据结构
掌握树化
理解索引计算方法、二次 hash 的意义、容量对索引计算的影响
掌握 put 流程、扩容、扩容因子
理解并发使用 HashMap 可能导致的问题
理解 key 的设计

1）基本数据结构
1.7 数组 + 链表
1.8 数组 + （链表 | 红黑树）


更形象的演示，见资料中的 hash-demo.jar，运行需要 jdk14 以上环境，进入 jar 包目录，执行下面命令
java -jar --add-exports java.base/jdk.internal.misc=ALL-UNNAMED hash-demo.jar
2）树化与退化树化意义

红黑树用来避免 DoS 攻击，防止链表超长时性能下降，树化应当是偶然情况，是保底策略
hash 表的查找，更新的时间复杂度是 $O(1)$，而红黑树的查找，更新的时间复杂度是 $O(log_2⁡n )$，TreeNode 占用空间也比普通 Node 的大，如非必要，尽量还是使用链表
hash 值如果足够随机，则在 hash 表内按泊松分布，在负载因子 0.75 的情况下，长度超过 8 的链表出现概率是 0.00000006，树化阈值选择 8 就是为了让树化几率足够小

树化规则

当链表长度超过树化阈值 8 时，先尝试扩容来减少链表长度，如果数组容量已经 &gt;&#x3D;64，才会进行树化

退化规则

情况1：在扩容时如果拆分树时，树元素个数 &lt;&#x3D; 6 则会退化链表
情况2：remove 树节点时，若 root、root.left、root.right、root.left.left 有一个为 null ，也会退化为链表

3）索引计算索引计算方法

首先，计算对象的 hashCode()
再进行调用 HashMap 的 hash() 方法进行二次哈希
二次 hash() 是为了综合高位数据，让哈希分布更为均匀


最后 &amp; (capacity – 1) 得到索引

数组容量为何是 2 的 n 次幂

计算索引时效率更高：如果是 2 的 n 次幂可以使用位与运算代替取模
扩容时重新计算索引效率更高： hash &amp; oldCap &#x3D;&#x3D; 0 的元素留在原来位置 ，否则新位置 &#x3D; 旧位置 + oldCap

注意

二次 hash 是为了配合 容量是 2 的 n 次幂 这一设计前提，如果 hash 表的容量不是 2 的 n 次幂，则不必二次 hash
容量是 2 的 n 次幂 这一设计计算索引效率更好，但 hash 的分散性就不好，需要二次 hash 来作为补偿，没有采用这一设计的典型例子是 Hashtable

4）put 与扩容put 流程

HashMap 是懒惰创建数组的，首次使用才创建数组
计算索引（桶下标）
如果桶下标还没人占用，创建 Node 占位返回
如果桶下标已经有人占用
已经是 TreeNode 走红黑树的添加或更新逻辑
是普通 Node，走链表的添加或更新逻辑，如果链表长度超过树化阈值，走树化逻辑


返回前检查容量是否超过阈值，一旦超过进行扩容

1.7 与 1.8 的区别

链表插入节点时，1.7 是头插法，1.8 是尾插法

1.7 是大于等于阈值且没有空位时才扩容，而 1.8 是大于阈值就扩容

1.8 在扩容计算 Node 索引时，会优化


扩容（加载）因子为何默认是 0.75f

在空间占用与查询时间之间取得较好的权衡
大于这个值，空间节省了，但链表就会比较长影响性能
小于这个值，冲突减少了，但扩容就会更频繁，空间占用也更多

5）并发问题扩容死链（1.7 会存在）
1.7 源码如下：
void transfer(Entry[] newTable, boolean rehash) &#123;    int newCapacity = newTable.length;    for (Entry&lt;K,V&gt; e : table) &#123;        while(null != e) &#123;            Entry&lt;K,V&gt; next = e.next;            if (rehash) &#123;                e.hash = null == e.key ? 0 : hash(e.key);            &#125;            int i = indexFor(e.hash, newCapacity);            e.next = newTable[i];            newTable[i] = e;            e = next;        &#125;    &#125;&#125;


e 和 next 都是局部变量，用来指向当前节点和下一个节点
线程1（绿色）的临时变量 e 和 next 刚引用了这俩节点，还未来得及移动节点，发生了线程切换，由线程2（蓝色）完成扩容和迁移



线程2 扩容完成，由于头插法，链表顺序颠倒。但线程1 的临时变量 e 和 next 还引用了这俩节点，还要再来一遍迁移



第一次循环
循环接着线程切换前运行，注意此时 e 指向的是节点 a，next 指向的是节点 b
e 头插 a 节点，注意图中画了两份 a 节点，但事实上只有一个（为了不让箭头特别乱画了两份）
当循环结束是 e 会指向 next 也就是 b 节点





第二次循环
next 指向了节点 a
e 头插节点 b
当循环结束时，e 指向 next 也就是节点 a





第三次循环
next 指向了 null
e 头插节点 a，a 的 next 指向了 b（之前 a.next 一直是 null），b 的 next 指向 a，死链已成
当循环结束时，e 指向 next 也就是 null，因此第四次循环时会正常退出




数据错乱（1.7，1.8 都会存在）

代码参考 day01.map.HashMapMissData，具体调试步骤参考视频


补充代码说明

day01.map.HashMapDistribution 演示 map 中链表长度符合泊松分布
day01.map.DistributionAffectedByCapacity 演示容量及 hashCode 取值对分布的影响
day01.map.DistributionAffectedByCapacity#hashtableGrowRule 演示了 Hashtable 的扩容规律
day01.sort.Utils#randomArray 如果 hashCode 足够随机，容量是否是 2 的 n 次幂影响不大
day01.sort.Utils#lowSameArray 如果 hashCode 低位一样的多，容量是 2 的 n 次幂会导致分布不均匀
day01.sort.Utils#evenArray 如果 hashCode 偶数的多，容量是 2 的 n 次幂会导致分布不均匀
由此得出对于容量是 2 的 n 次幂的设计来讲，二次 hash 非常重要


day01.map.HashMapVsHashtable 演示了对于同样数量的单词字符串放入 HashMap 和 Hashtable 分布上的区别


6）key 的设计key 的设计要求

HashMap 的 key 可以为 null，但 Map 的其他实现则不然
作为 key 的对象，必须实现 hashCode 和 equals，并且 key 的内容不能修改（不可变）
key 的 hashCode 应该有良好的散列性

如果 key 可变，例如修改了 age 会导致再次查询时查询不到
public class HashMapMutableKey &#123;    public static void main(String[] args) &#123;        HashMap&lt;Student, Object&gt; map = new HashMap&lt;&gt;();        Student stu = new Student(&quot;张三&quot;, 18);        map.put(stu, new Object());        System.out.println(map.get(stu));        stu.age = 19;        System.out.println(map.get(stu));    &#125;    static class Student &#123;        String name;        int age;        public Student(String name, int age) &#123;            this.name = name;            this.age = age;        &#125;        public String getName() &#123;            return name;        &#125;        public void setName(String name) &#123;            this.name = name;        &#125;        public int getAge() &#123;            return age;        &#125;        public void setAge(int age) &#123;            this.age = age;        &#125;        @Override        public boolean equals(Object o) &#123;            if (this == o) return true;            if (o == null || getClass() != o.getClass()) return false;            Student student = (Student) o;            return age == student.age &amp;&amp; Objects.equals(name, student.name);        &#125;        @Override        public int hashCode() &#123;            return Objects.hash(name, age);        &#125;    &#125;&#125;



String 对象的 hashCode() 设计

目标是达到较为均匀的散列效果，每个字符串的 hashCode 足够独特
字符串中的每个字符都可以表现为一个数字，称为 $S_i$，其中 i 的范围是 0 ~ n - 1 
散列公式为： $S_0∗31^{(n-1)}+ S_1∗31^{(n-2)}+ … S_i ∗ 31^{(n-1-i)}+ …S_{(n-1)}∗31^0$
31 代入公式有较好的散列特性，并且 31 * h 可以被优化为 
即 $32 ∗h -h $
即 $2^5  ∗h -h$
即 $h≪5  -h$



11. 单例模式要求

掌握五种单例模式的实现方式
理解为何 DCL 实现时要使用 volatile 修饰静态变量
了解 jdk 中用到单例的场景

饿汉式
public class Singleton1 implements Serializable &#123;    private Singleton1() &#123;        if (INSTANCE != null) &#123;            throw new RuntimeException(&quot;单例对象不能重复创建&quot;);        &#125;        System.out.println(&quot;private Singleton1()&quot;);    &#125;    private static final Singleton1 INSTANCE = new Singleton1();    public static Singleton1 getInstance() &#123;        return INSTANCE;    &#125;    public static void otherMethod() &#123;        System.out.println(&quot;otherMethod()&quot;);    &#125;    public Object readResolve() &#123;        return INSTANCE;    &#125;&#125;


构造方法抛出异常是防止反射破坏单例
readResolve() 是防止反序列化破坏单例

枚举饿汉式
public enum Singleton2 &#123;    INSTANCE;    private Singleton2() &#123;        System.out.println(&quot;private Singleton2()&quot;);    &#125;    @Override    public String toString() &#123;        return getClass().getName() + &quot;@&quot; + Integer.toHexString(hashCode());    &#125;    public static Singleton2 getInstance() &#123;        return INSTANCE;    &#125;    public static void otherMethod() &#123;        System.out.println(&quot;otherMethod()&quot;);    &#125;&#125;


枚举饿汉式能天然防止反射、反序列化破坏单例

懒汉式
public class Singleton3 implements Serializable &#123;    private Singleton3() &#123;        System.out.println(&quot;private Singleton3()&quot;);    &#125;    private static Singleton3 INSTANCE = null;    // Singleton3.class    public static synchronized Singleton3 getInstance() &#123;        if (INSTANCE == null) &#123;            INSTANCE = new Singleton3();        &#125;        return INSTANCE;    &#125;    public static void otherMethod() &#123;        System.out.println(&quot;otherMethod()&quot;);    &#125;&#125;


其实只有首次创建单例对象时才需要同步，但该代码实际上每次调用都会同步
因此有了下面的双检锁改进

双检锁懒汉式
public class Singleton4 implements Serializable &#123;    private Singleton4() &#123;        System.out.println(&quot;private Singleton4()&quot;);    &#125;    private static volatile Singleton4 INSTANCE = null; // 可见性，有序性    public static Singleton4 getInstance() &#123;        if (INSTANCE == null) &#123;            synchronized (Singleton4.class) &#123;                if (INSTANCE == null) &#123;                    INSTANCE = new Singleton4();                &#125;            &#125;        &#125;        return INSTANCE;    &#125;    public static void otherMethod() &#123;        System.out.println(&quot;otherMethod()&quot;);    &#125;&#125;

为何必须加 volatile：

INSTANCE = new Singleton4() 不是原子的，分成 3 步：创建对象、调用构造、给静态变量赋值，其中后两步可能被指令重排序优化，变成先赋值、再调用构造
如果线程1 先执行了赋值，线程2 执行到第一个 INSTANCE == null 时发现 INSTANCE 已经不为 null，此时就会返回一个未完全构造的对象

内部类懒汉式
public class Singleton5 implements Serializable &#123;    private Singleton5() &#123;        System.out.println(&quot;private Singleton5()&quot;);    &#125;    private static class Holder &#123;        static Singleton5 INSTANCE = new Singleton5();    &#125;    public static Singleton5 getInstance() &#123;        return Holder.INSTANCE;    &#125;    public static void otherMethod() &#123;        System.out.println(&quot;otherMethod()&quot;);    &#125;&#125;


避免了双检锁的缺点

JDK 中单例的体现

Runtime 体现了饿汉式单例
Console 体现了双检锁懒汉式单例
Collections 中的 EmptyNavigableSet 内部类懒汉式单例
ReverseComparator.REVERSE_ORDER 内部类懒汉式单例
Comparators.NaturalOrderComparator.INSTANCE 枚举饿汉式单例

]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring Source</title>
    <url>/2026/06/04/java/Spring%20Source/</url>
    <content><![CDATA[容器与 bean1) 容器接口
BeanFactory 接口，典型功能有：

getBean


ApplicationContext 接口，是 BeanFactory 的子接口。它扩展了 BeanFactory 接口的功能，如：

国际化
通配符方式获取一组 Resource 资源
整合 Environment 环境（能通过它获取各种来源的配置信息）
事件发布与监听，实现组件之间的解耦



可以看到，我们课上讲的，都是 BeanFactory 提供的基本功能，ApplicationContext 中的扩展功能都没有用到。
演示1 - BeanFactory 与 ApplicationContext 的区别代码参考com.itheima.a01 包
收获💡通过这个示例结合 debug 查看 ApplicationContext 对象的内部结构，学到：

到底什么是 BeanFactory

它是 ApplicationContext 的父接口
它才是 Spring 的核心容器, 主要的 ApplicationContext 实现都【组合】了它的功能，【组合】是指 ApplicationContext 的一个重要成员变量就是 BeanFactory


BeanFactory 能干点啥

表面上只有 getBean
实际上控制反转、基本的依赖注入、直至 Bean 的生命周期的各种功能，都由它的实现类提供
例子中通过反射查看了它的成员变量 singletonObjects，内部包含了所有的单例 bean


ApplicationContext 比 BeanFactory 多点啥

ApplicationContext 组合并扩展了 BeanFactory 的功能
国际化、通配符方式获取一组 Resource 资源、整合 Environment 环境、事件发布与监听
新学一种代码之间解耦途径，事件解耦



建议练习：完成用户注册与发送短信之间的解耦，用事件方式、和 AOP 方式分别实现

注意

如果 jdk &gt; 8, 运行时请添加 –add-opens java.base&#x2F;java.lang&#x3D;ALL-UNNAMED，这是因为这些版本的 jdk 默认不允许跨 module 反射
事件发布还可以异步，这个视频中没有展示，请自行查阅 @EnableAsync，@Async 的用法


演示2 - 国际化public class TestMessageSource &#123;    public static void main(String[] args) &#123;        GenericApplicationContext context = new GenericApplicationContext();        context.registerBean(&quot;messageSource&quot;, MessageSource.class, () -&gt; &#123;            ResourceBundleMessageSource ms = new ResourceBundleMessageSource();            ms.setDefaultEncoding(&quot;utf-8&quot;);            ms.setBasename(&quot;messages&quot;);            return ms;        &#125;);        context.refresh();        System.out.println(context.getMessage(&quot;hi&quot;, null, Locale.ENGLISH));        System.out.println(context.getMessage(&quot;hi&quot;, null, Locale.CHINESE));        System.out.println(context.getMessage(&quot;hi&quot;, null, Locale.JAPANESE));    &#125;&#125;

国际化文件均在 src&#x2F;resources 目录下
messages.properties（空）
messages_en.properties
hi=Hello

messages_ja.properties
hi=こんにちは

messages_zh.properties
hi=你好


注意

ApplicationContext 中 MessageSource bean 的名字固定为 messageSource
使用 SpringBoot 时，国际化文件名固定为 messages
空的 messages.properties 也必须存在


2) 容器实现Spring 的发展历史较为悠久，因此很多资料还在讲解它较旧的实现，这里出于怀旧的原因，把它们都列出来，供大家参考

DefaultListableBeanFactory，是 BeanFactory 最重要的实现，像控制反转和依赖注入功能，都是它来实现
ClassPathXmlApplicationContext，从类路径查找 XML 配置文件，创建容器（旧）
FileSystemXmlApplicationContext，从磁盘路径查找 XML 配置文件，创建容器（旧）
XmlWebApplicationContext，传统 SSM 整合时，基于 XML 配置文件的容器（旧）
AnnotationConfigWebApplicationContext，传统 SSM 整合时，基于 java 配置类的容器（旧）
AnnotationConfigApplicationContext，Spring boot 中非 web 环境容器（新）
AnnotationConfigServletWebServerApplicationContext，Spring boot 中 servlet web 环境容器（新）
AnnotationConfigReactiveWebServerApplicationContext，Spring boot 中 reactive web 环境容器（新）

另外要注意的是，后面这些带有 ApplicationContext 的类都是 ApplicationContext 接口的实现，但它们是组合了 DefaultListableBeanFactory 的功能，并非继承而来
演示1 - DefaultListableBeanFactory代码参考com.itheima.a02.TestBeanFactory
收获💡
beanFactory 可以通过 registerBeanDefinition 注册一个 bean definition 对象
我们平时使用的配置类、xml、组件扫描等方式都是生成 bean definition 对象注册到 beanFactory 当中
bean definition 描述了这个 bean 的创建蓝图：scope 是什么、用构造还是工厂创建、初始化销毁方法是什么，等等


beanFactory 需要手动调用 beanFactory 后处理器对它做增强
例如通过解析 @Bean、@ComponentScan 等注解，来补充一些 bean definition


beanFactory 需要手动添加 bean 后处理器，以便对后续 bean 的创建过程提供增强
例如 @Autowired，@Resource 等注解的解析都是 bean 后处理器完成的
bean 后处理的添加顺序会对解析结果有影响，见视频中同时加 @Autowired，@Resource 的例子


beanFactory 需要手动调用方法来初始化单例
beanFactory 需要额外设置才能解析 ${} 与 #{}

演示2 - 常见 ApplicationContext 实现代码参考com.itheima.a02.A02
收获💡
常见的 ApplicationContext 容器实现
内嵌容器、DispatcherServlet 的创建方法、作用

3) Bean 的生命周期一个受 Spring 管理的 bean，生命周期主要阶段有

创建：根据 bean 的构造方法或者工厂方法来创建 bean 实例对象
依赖注入：根据 @Autowired，@Value 或其它一些手段，为 bean 的成员变量填充值、建立关系
初始化：回调各种 Aware 接口，调用对象的各种初始化方法
销毁：在容器关闭时，会销毁所有单例对象（即调用它们的销毁方法）
prototype 对象也能够销毁，不过需要容器这边主动调用



一些资料会提到，生命周期中还有一类 bean 后处理器：BeanPostProcessor，会在 bean 的初始化的前后，提供一些扩展逻辑。但这种说法是不完整的，见下面的演示1
演示1 - bean 生命周期代码参考com.itheima.a03 包
graph LR创建 --&gt; 依赖注入依赖注入 --&gt; 初始化初始化 --&gt; 可用可用 --&gt; 销毁

创建前后的增强

postProcessBeforeInstantiation
这里返回的对象若不为 null 会替换掉原本的 bean，并且仅会走 postProcessAfterInitialization 流程


postProcessAfterInstantiation
这里如果返回 false 会跳过依赖注入阶段



依赖注入前的增强

postProcessProperties
如 @Autowired、@Value、@Resource



初始化前后的增强

postProcessBeforeInitialization
这里返回的对象会替换掉原本的 bean
如 @PostConstruct、@ConfigurationProperties


postProcessAfterInitialization 
这里返回的对象会替换掉原本的 bean
如代理增强



销毁之前的增强

postProcessBeforeDestruction
如 @PreDestroy



收获💡
Spring bean 生命周期各个阶段
模板设计模式, 指大流程已经固定好了, 通过接口回调（bean 后处理器）在一些关键点前后提供扩展

演示2 - 模板方法设计模式关键代码public class TestMethodTemplate &#123;    public static void main(String[] args) &#123;        MyBeanFactory beanFactory = new MyBeanFactory();        beanFactory.addBeanPostProcessor(bean -&gt; System.out.println(&quot;解析 @Autowired&quot;));        beanFactory.addBeanPostProcessor(bean -&gt; System.out.println(&quot;解析 @Resource&quot;));        beanFactory.getBean();    &#125;    // 模板方法  Template Method Pattern    static class MyBeanFactory &#123;        public Object getBean() &#123;            Object bean = new Object();            System.out.println(&quot;构造 &quot; + bean);            System.out.println(&quot;依赖注入 &quot; + bean); // @Autowired, @Resource            for (BeanPostProcessor processor : processors) &#123;                processor.inject(bean);            &#125;            System.out.println(&quot;初始化 &quot; + bean);            return bean;        &#125;        private List&lt;BeanPostProcessor&gt; processors = new ArrayList&lt;&gt;();        public void addBeanPostProcessor(BeanPostProcessor processor) &#123;            processors.add(processor);        &#125;    &#125;        static interface BeanPostProcessor &#123;        public void inject(Object bean); // 对依赖注入阶段的扩展    &#125;&#125;



演示3 - bean 后处理器排序代码参考com.itheima.a03.TestProcessOrder
收获💡
实现了 PriorityOrdered 接口的优先级最高
实现了 Ordered 接口与加了 @Order 注解的平级, 按数字升序
其它的排在最后

4) Bean 后处理器演示1 - 后处理器作用代码参考com.itheima.a04 包
收获💡
@Autowired 等注解的解析属于 bean 生命周期阶段（依赖注入, 初始化）的扩展功能，这些扩展功能由 bean 后处理器来完成
每个后处理器各自增强什么功能
AutowiredAnnotationBeanPostProcessor 解析 @Autowired 与 @Value
CommonAnnotationBeanPostProcessor 解析 @Resource、@PostConstruct、@PreDestroy
ConfigurationPropertiesBindingPostProcessor 解析 @ConfigurationProperties


另外 ContextAnnotationAutowireCandidateResolver 负责获取 @Value 的值，解析 @Qualifier、泛型、@Lazy 等

演示2 - @Autowired bean 后处理器运行分析代码参考com.itheima.a04.DigInAutowired
收获💡
AutowiredAnnotationBeanPostProcessor.findAutowiringMetadata 用来获取某个 bean 上加了 @Value @Autowired 的成员变量，方法参数的信息，表示为 InjectionMetadata
InjectionMetadata 可以完成依赖注入
InjectionMetadata 内部根据成员变量，方法参数封装为 DependencyDescriptor 类型
有了 DependencyDescriptor，就可以利用 beanFactory.doResolveDependency 方法进行基于类型的查找

5) BeanFactory 后处理器演示1 - BeanFactory 后处理器的作用代码参考com.itheima.a05 包

ConfigurationClassPostProcessor 可以解析
@ComponentScan
@Bean
@Import
@ImportResource


MapperScannerConfigurer 可以解析
Mapper 接口



收获💡
@ComponentScan, @Bean, @Mapper 等注解的解析属于核心容器（即 BeanFactory）的扩展功能
这些扩展功能由不同的 BeanFactory 后处理器来完成，其实主要就是补充了一些 bean 定义

演示2 - 模拟解析 @ComponentScan代码参考com.itheima.a05.ComponentScanPostProcessor
收获💡
Spring 操作元数据的工具类 CachingMetadataReaderFactory
通过注解元数据（AnnotationMetadata）获取直接或间接标注的注解信息
通过类元数据（ClassMetadata）获取类名，AnnotationBeanNameGenerator 生成 bean 名
解析元数据是基于 ASM 技术

演示3 - 模拟解析 @Bean代码参考com.itheima.a05.AtBeanPostProcessor
收获💡
进一步熟悉注解元数据（AnnotationMetadata）获取方法上注解信息

演示4 - 模拟解析 Mapper 接口代码参考com.itheima.a05.MapperPostProcessor
收获💡
Mapper 接口被 Spring 管理的本质：实际是被作为 MapperFactoryBean 注册到容器中
Spring 的诡异做法，根据接口生成的 BeanDefinition 仅为根据接口名生成 bean 名

6) Aware 接口演示 - Aware 接口及 InitializingBean 接口代码参考com.itheima.a06 包
收获💡
Aware 接口提供了一种【内置】 的注入手段，例如
BeanNameAware 注入 bean 的名字
BeanFactoryAware 注入 BeanFactory 容器
ApplicationContextAware 注入 ApplicationContext 容器
EmbeddedValueResolverAware 注入 ${} 解析器


InitializingBean 接口提供了一种【内置】的初始化手段
对比
内置的注入和初始化不受扩展功能的影响，总会被执行
而扩展功能受某些情况影响可能会失效
因此 Spring 框架内部的类常用内置注入和初始化



配置类 @Autowired 失效分析Java 配置类不包含 BeanFactoryPostProcessor 的情况
sequenceDiagram participant ac as ApplicationContextparticipant bfpp as BeanFactoryPostProcessorparticipant bpp as BeanPostProcessorparticipant config as Java配置类ac -&gt;&gt; bfpp : 1. 执行 BeanFactoryPostProcessorac -&gt;&gt; bpp : 2. 注册 BeanPostProcessorac -&gt;&gt; +config : 3. 创建和初始化bpp -&gt;&gt; config : 3.1 依赖注入扩展(如 @Value 和 @Autowired)bpp -&gt;&gt; config : 3.2 初始化扩展(如 @PostConstruct)ac -&gt;&gt; config : 3.3 执行 Aware 及 InitializingBeanconfig --&gt;&gt; -ac : 3.4 创建成功

Java 配置类包含 BeanFactoryPostProcessor 的情况，因此要创建其中的 BeanFactoryPostProcessor 必须提前创建 Java 配置类，而此时的 BeanPostProcessor 还未准备好，导致 @Autowired 等注解失效
sequenceDiagram participant ac as ApplicationContextparticipant bfpp as BeanFactoryPostProcessorparticipant bpp as BeanPostProcessorparticipant config as Java配置类ac -&gt;&gt; +config : 3. 创建和初始化ac -&gt;&gt; config : 3.1 执行 Aware 及 InitializingBeanconfig --&gt;&gt; -ac : 3.2 创建成功ac -&gt;&gt; bfpp : 1. 执行 BeanFactoryPostProcessorac -&gt;&gt; bpp : 2. 注册 BeanPostProcessor

对应代码
@Configurationpublic class MyConfig1 &#123;    private static final Logger log = LoggerFactory.getLogger(MyConfig1.class);    @Autowired    public void setApplicationContext(ApplicationContext applicationContext) &#123;        log.debug(&quot;注入 ApplicationContext&quot;);    &#125;    @PostConstruct    public void init() &#123;        log.debug(&quot;初始化&quot;);    &#125;    @Bean //  ⬅️ 注释或添加 beanFactory 后处理器对应上方两种情况    public BeanFactoryPostProcessor processor1() &#123;        return beanFactory -&gt; &#123;            log.debug(&quot;执行 processor1&quot;);        &#125;;    &#125;&#125;


注意
解决方法：

用内置依赖注入和初始化取代扩展依赖注入和初始化
用静态工厂方法代替实例工厂方法，避免工厂对象提前被创建


7) 初始化与销毁演示 - 初始化销毁顺序代码参考com.itheima.a07 包
收获💡Spring 提供了多种初始化手段，除了课堂上讲的 @PostConstruct，@Bean(initMethod) 之外，还可以实现 InitializingBean 接口来进行初始化，如果同一个 bean 用了以上手段声明了 3 个初始化方法，那么它们的执行顺序是

@PostConstruct 标注的初始化方法
InitializingBean 接口的初始化方法
@Bean(initMethod) 指定的初始化方法

与初始化类似，Spring 也提供了多种销毁手段，执行顺序为

@PreDestroy 标注的销毁方法
DisposableBean 接口的销毁方法
@Bean(destroyMethod) 指定的销毁方法

8) Scope在当前版本的 Spring 和 Spring Boot 程序中，支持五种 Scope

singleton，容器启动时创建（未设置延迟），容器关闭时销毁
prototype，每次使用时创建，不会自动销毁，需要调用 DefaultListableBeanFactory.destroyBean(bean) 销毁
request，每次请求用到此 bean 时创建，请求结束时销毁
session，每个会话用到此 bean 时创建，会话结束时销毁
application，web 容器用到此 bean 时创建，容器停止时销毁

有些文章提到有 globalSession 这一 Scope，也是陈旧的说法，目前 Spring 中已废弃
但要注意，如果在 singleton 注入其它 scope 都会有问题，解决方法有

@Lazy
@Scope(proxyMode &#x3D; ScopedProxyMode.TARGET_CLASS)
ObjectFactory
ApplicationContext.getBean

演示1 - request, session, application 作用域代码参考com.itheima.a08 包

打开不同的浏览器, 刷新 http://localhost:8080/test 即可查看效果
如果 jdk &gt; 8, 运行时请添加 –add-opens java.base&#x2F;java.lang&#x3D;ALL-UNNAMED

收获💡
有几种 scope
在 singleton 中使用其它几种 scope 的方法
其它 scope 的销毁时机
可以将通过 server.servlet.session.timeout&#x3D;30s 观察 session bean 的销毁
ServletContextScope 销毁机制疑似实现有误



分析 - singleton 注入其它 scope 失效以单例注入多例为例
有一个单例对象 E
@Componentpublic class E &#123;    private static final Logger log = LoggerFactory.getLogger(E.class);    private F f;    public E() &#123;        log.info(&quot;E()&quot;);    &#125;    @Autowired    public void setF(F f) &#123;        this.f = f;        log.info(&quot;setF(F f) &#123;&#125;&quot;, f.getClass());    &#125;    public F getF() &#123;        return f;    &#125;&#125;

要注入的对象 F 期望是多例
@Component@Scope(&quot;prototype&quot;)public class F &#123;    private static final Logger log = LoggerFactory.getLogger(F.class);    public F() &#123;        log.info(&quot;F()&quot;);    &#125;&#125;

测试
E e = context.getBean(E.class);F f1 = e.getF();F f2 = e.getF();System.out.println(f1);System.out.println(f2);

输出
com.itheima.demo.cycle.F@6622fc65com.itheima.demo.cycle.F@6622fc65

发现它们是同一个对象，而不是期望的多例对象
对于单例对象来讲，依赖注入仅发生了一次，后续再没有用到多例的 F，因此 E 用的始终是第一次依赖注入的 F
graph LRe1(e 创建)e2(e set 注入 f)f1(f 创建)e1--&gt;f1--&gt;e2

解决

仍然使用 @Lazy 生成代理
代理对象虽然还是同一个，但当每次使用代理对象的任意方法时，由代理创建新的 f 对象

graph LRe1(e 创建)e2(e set 注入 f代理)f1(f 创建)f2(f 创建)f3(f 创建)e1--&gt;e2e2--使用f方法--&gt;f1e2--使用f方法--&gt;f2e2--使用f方法--&gt;f3

@Componentpublic class E &#123;    @Autowired    @Lazy    public void setF(F f) &#123;        this.f = f;        log.info(&quot;setF(F f) &#123;&#125;&quot;, f.getClass());    &#125;    // ...&#125;


注意

@Lazy 加在也可以加在成员变量上，但加在 set 方法上的目的是可以观察输出，加在成员变量上就不行了
@Autowired 加在 set 方法的目的类似


输出
E: setF(F f) class com.itheima.demo.cycle.F$$EnhancerBySpringCGLIB$$8b54f2bcF: F()com.itheima.demo.cycle.F@3a6f2de3F: F()com.itheima.demo.cycle.F@56303b57

从输出日志可以看到调用 setF 方法时，f 对象的类型是代理类型
演示2 - 4种解决方法代码参考com.itheima.a08.sub 包

如果 jdk &gt; 8, 运行时请添加 –add-opens java.base&#x2F;java.lang&#x3D;ALL-UNNAMED

收获💡
单例注入其它 scope 的四种解决方法
@Lazy
@Scope(value &#x3D; “prototype”, proxyMode &#x3D; ScopedProxyMode.TARGET_CLASS)
ObjectFactory
ApplicationContext


解决方法虽然不同，但理念上殊途同归: 都是推迟其它 scope bean 的获取

AOPAOP 底层实现方式之一是代理，由代理结合通知和目标，提供增强功能
除此以外，aspectj 提供了两种另外的 AOP 底层实现：

第一种是通过 ajc 编译器在编译 class 类文件时，就把通知的增强功能，织入到目标类的字节码中

第二种是通过 agent 在加载目标类时，修改目标类的字节码，织入增强功能

作为对比，之前学习的代理是运行时生成新的字节码


简单比较的话：

aspectj 在编译和加载时，修改目标字节码，性能较高
aspectj 因为不用代理，能突破一些技术上的限制，例如对构造、对静态方法、对 final 也能增强
但 aspectj 侵入性较强，且需要学习新的 aspectj 特有语法，因此没有广泛流行

9) AOP 实现之 ajc 编译器代码参考项目 demo6_advanced_aspectj_01
收获💡
编译器也能修改 class 实现增强
编译器增强能突破代理仅能通过方法重写增强的限制：可以对构造方法、静态方法等实现增强


注意

版本选择了 java 8, 因为目前的 aspectj-maven-plugin 1.14.0 最高只支持到 java 16
一定要用 maven 的 compile 来编译, idea 不会调用 ajc 编译器


10) AOP 实现之 agent 类加载代码参考项目 demo6_advanced_aspectj_02
收获💡
类加载时可以通过 agent 修改 class 实现增强

11) AOP 实现之 proxy演示1 - jdk 动态代理public class JdkProxyDemo &#123;    interface Foo &#123;        void foo();    &#125;    static class Target implements Foo &#123;        public void foo() &#123;            System.out.println(&quot;target foo&quot;);        &#125;    &#125;    public static void main(String[] param) &#123;        // 目标对象        Target target = new Target();        // 代理对象        Foo proxy = (Foo) Proxy.newProxyInstance(                Target.class.getClassLoader(), new Class[]&#123;Foo.class&#125;,                (p, method, args) -&gt; &#123;                    System.out.println(&quot;proxy before...&quot;);                    Object result = method.invoke(target, args);                    System.out.println(&quot;proxy after...&quot;);                    return result;                &#125;);        // 调用代理        proxy.foo();    &#125;&#125;

运行结果
proxy before...target fooproxy after...

收获💡
jdk 动态代理要求目标必须实现接口，生成的代理类实现相同接口，因此代理与目标之间是平级兄弟关系

演示2 - cglib 代理public class CglibProxyDemo &#123;    static class Target &#123;        public void foo() &#123;            System.out.println(&quot;target foo&quot;);        &#125;    &#125;    public static void main(String[] param) &#123;        // 目标对象        Target target = new Target();        // 代理对象        Target proxy = (Target) Enhancer.create(Target.class,                 (MethodInterceptor) (p, method, args, methodProxy) -&gt; &#123;            System.out.println(&quot;proxy before...&quot;);            Object result = methodProxy.invoke(target, args);            // 另一种调用方法，不需要目标对象实例//            Object result = methodProxy.invokeSuper(p, args);            System.out.println(&quot;proxy after...&quot;);            return result;        &#125;);        // 调用代理        proxy.foo();    &#125;&#125;

运行结果与 jdk 动态代理相同
收获💡
cglib 不要求目标实现接口，它生成的代理类是目标的子类，因此代理与目标之间是子父关系
限制⛔：根据上述分析 final 类无法被 cglib 增强

12) jdk 动态代理进阶演示1 - 模拟 jdk 动态代理public class A12 &#123;    interface Foo &#123;        void foo();        int bar();    &#125;    static class Target implements Foo &#123;        public void foo() &#123;            System.out.println(&quot;target foo&quot;);        &#125;        public int bar() &#123;            System.out.println(&quot;target bar&quot;);            return 100;        &#125;    &#125;    public static void main(String[] param) &#123;        // ⬇️1. 创建代理，这时传入 InvocationHandler        Foo proxy = new $Proxy0(new InvocationHandler() &#123;                // ⬇️5. 进入 InvocationHandler            public Object invoke(Object proxy, Method method, Object[] args) throws Throwable&#123;                // ⬇️6. 功能增强                System.out.println(&quot;before...&quot;);                // ⬇️7. 反射调用目标方法                return method.invoke(new Target(), args);            &#125;        &#125;);        // ⬇️2. 调用代理方法        proxy.foo();        proxy.bar();    &#125;&#125;

模拟代理实现
import java.lang.reflect.InvocationHandler;import java.lang.reflect.Method;import java.lang.reflect.Proxy;import java.lang.reflect.UndeclaredThrowableException;// ⬇️这就是 jdk 代理类的源码, 秘密都在里面public class $Proxy0 extends Proxy implements A12.Foo &#123;    public $Proxy0(InvocationHandler h) &#123;        super(h);    &#125;    // ⬇️3. 进入代理方法    public void foo() &#123;        try &#123;            // ⬇️4. 回调 InvocationHandler            h.invoke(this, foo, new Object[0]);        &#125; catch (RuntimeException | Error e) &#123;            throw e;        &#125; catch (Throwable e) &#123;            throw new UndeclaredThrowableException(e);        &#125;    &#125;    @Override    public int bar() &#123;        try &#123;            Object result = h.invoke(this, bar, new Object[0]);            return (int) result;        &#125; catch (RuntimeException | Error e) &#123;            throw e;        &#125; catch (Throwable e) &#123;            throw new UndeclaredThrowableException(e);        &#125;    &#125;    static Method foo;    static Method bar;    static &#123;        try &#123;            foo = A12.Foo.class.getMethod(&quot;foo&quot;);            bar = A12.Foo.class.getMethod(&quot;bar&quot;);        &#125; catch (NoSuchMethodException e) &#123;            throw new NoSuchMethodError(e.getMessage());        &#125;    &#125;&#125;

收获💡代理一点都不难，无非就是利用了多态、反射的知识

方法重写可以增强逻辑，只不过这【增强逻辑】千变万化，不能写死在代理内部
通过接口回调将【增强逻辑】置于代理类之外
配合接口方法反射（是多态调用），就可以再联动调用目标方法
会用 arthas 的 jad 工具反编译代理类
限制⛔：代理增强是借助多态来实现，因此成员变量、静态方法、final 方法均不能通过代理实现

演示2 - 方法反射优化代码参考com.itheima.a12.TestMethodInvoke
收获💡
前 16 次反射性能较低
第 17 次调用会生成代理类，优化为非反射调用
会用 arthas 的 jad 工具反编译第 17 次调用生成的代理类


注意
运行时请添加 –add-opens java.base&#x2F;java.lang.reflect&#x3D;ALL-UNNAMED –add-opens java.base&#x2F;jdk.internal.reflect&#x3D;ALL-UNNAMED

13) cglib 代理进阶演示 - 模拟 cglib 代理代码参考com.itheima.a13 包
收获💡和 jdk 动态代理原理查不多

回调的接口换了一下，InvocationHandler 改成了 MethodInterceptor
调用目标时有所改进，见下面代码片段
method.invoke 是反射调用，必须调用到足够次数才会进行优化
methodProxy.invoke 是不反射调用，它会正常（间接）调用目标对象的方法（Spring 采用）
methodProxy.invokeSuper 也是不反射调用，它会正常（间接）调用代理对象的方法，可以省略目标对象



public class A14Application &#123;    public static void main(String[] args) throws InvocationTargetException &#123;        Target target = new Target();        Proxy proxy = new Proxy();                proxy.setCallbacks(new Callback[]&#123;(MethodInterceptor) (p, m, a, mp) -&gt; &#123;            System.out.println(&quot;proxy before...&quot; + mp.getSignature());            // ⬇️调用目标方法(三种)//            Object result = m.invoke(target, a);  // ⬅️反射调用//            Object result = mp.invoke(target, a); // ⬅️非反射调用, 结合目标用            Object result = mp.invokeSuper(p, a);   // ⬅️非反射调用, 结合代理用            System.out.println(&quot;proxy after...&quot; + mp.getSignature());            return result;        &#125;&#125;);                // ⬇️调用代理方法        proxy.save();    &#125;&#125;


注意

调用 Object 的方法, 后两种在 jdk &gt;&#x3D; 9 时都有问题, 需要 –add-opens java.base&#x2F;java.lang&#x3D;ALL-UNNAMED


14) cglib 避免反射调用演示 - cglib 如何避免反射代码参考com.itheima.a13.ProxyFastClass，com.itheima.a13.TargetFastClass
收获💡
当调用 MethodProxy 的 invoke 或 invokeSuper 方法时, 会动态生成两个类
ProxyFastClass 配合代理对象一起使用, 避免反射
TargetFastClass 配合目标对象一起使用, 避免反射 (Spring 用的这种)


TargetFastClass 记录了 Target 中方法与编号的对应关系
save(long) 编号 2
save(int) 编号 1
save() 编号 0
首先根据方法名和参数个数、类型, 用 switch 和 if 找到这些方法编号
然后再根据编号去调用目标方法, 又用了一大堆 switch 和 if, 但避免了反射


ProxyFastClass 记录了 Proxy 中方法与编号的对应关系，不过 Proxy 额外提供了下面几个方法
saveSuper(long) 编号 2，不增强，仅是调用 super.save(long)
saveSuper(int) 编号 1，不增强, 仅是调用 super.save(int)
saveSuper() 编号 0，不增强, 仅是调用 super.save()
查找方式与 TargetFastClass 类似


为什么有这么麻烦的一套东西呢？
避免反射, 提高性能, 代价是一个代理类配两个 FastClass 类, 代理类中还得增加仅调用 super 的一堆方法
用编号处理方法对应关系比较省内存, 另外, 最初获得方法顺序是不确定的, 这个过程没法固定死



15) jdk 和 cglib 在 Spring 中的统一Spring 中对切点、通知、切面的抽象如下

切点：接口 Pointcut，典型实现 AspectJExpressionPointcut
通知：典型接口为 MethodInterceptor 代表环绕通知
切面：Advisor，包含一个 Advice 通知，PointcutAdvisor 包含一个 Advice 通知和一个 Pointcut

classDiagramclass Adviceclass MethodInterceptorclass Advisorclass PointcutAdvisorPointcut &lt;|-- AspectJExpressionPointcutAdvice &lt;|-- MethodInterceptorAdvisor &lt;|-- PointcutAdvisorPointcutAdvisor o-- &quot;一&quot; PointcutPointcutAdvisor o-- &quot;一&quot; Advice&lt;&lt;interface&gt;&gt; Advice&lt;&lt;interface&gt;&gt; MethodInterceptor&lt;&lt;interface&gt;&gt; Pointcut&lt;&lt;interface&gt;&gt; Advisor&lt;&lt;interface&gt;&gt; PointcutAdvisor

代理相关类图

AopProxyFactory 根据 proxyTargetClass 等设置选择 AopProxy 实现
AopProxy 通过 getProxy 创建代理对象
图中 Proxy 都实现了 Advised 接口，能够获得关联的切面集合与目标（其实是从 ProxyFactory 取得）
调用代理方法时，会借助 ProxyFactory 将通知统一转为环绕通知：MethodInterceptor

classDiagramAdvised &lt;|-- ProxyFactoryProxyFactory o-- TargetProxyFactory o-- &quot;多&quot; AdvisorProxyFactory --&gt; AopProxyFactory : 使用AopProxyFactory --&gt; AopProxyAdvised &lt;|-- 基于CGLIB的Proxy基于CGLIB的Proxy &lt;-- ObjenesisCglibAopProxy : 创建AopProxy &lt;|-- ObjenesisCglibAopProxyAopProxy &lt;|-- JdkDynamicAopProxy基于JDK的Proxy &lt;-- JdkDynamicAopProxy : 创建Advised &lt;|-- 基于JDK的Proxyclass AopProxy &#123;   +getProxy() Object&#125;class ProxyFactory &#123;	proxyTargetClass : boolean&#125;class ObjenesisCglibAopProxy &#123;	advised : ProxyFactory&#125;class JdkDynamicAopProxy &#123;	advised : ProxyFactory&#125;&lt;&lt;interface&gt;&gt; Advised&lt;&lt;interface&gt;&gt; AopProxyFactory&lt;&lt;interface&gt;&gt; AopProxy



演示 - 底层切点、通知、切面代码参考com.itheima.a15.A15
收获💡
底层的切点实现
底层的通知实现
底层的切面实现
ProxyFactory 用来创建代理
如果指定了接口，且 proxyTargetClass &#x3D; false，使用 JdkDynamicAopProxy
如果没有指定接口，或者 proxyTargetClass &#x3D; true，使用 ObjenesisCglibAopProxy
例外：如果目标是接口类型或已经是 Jdk 代理，使用 JdkDynamicAopProxy






注意

要区分本章节提到的 MethodInterceptor，它与之前 cglib 中用的的 MethodInterceptor 是不同的接口


16) 切点匹配演示 - 切点匹配代码参考com.itheima.a16.A16
收获💡
常见 aspectj 切点用法
aspectj 切点的局限性，实际的 @Transactional 切点实现

17) 从 @Aspect 到 Advisor演示1 - 代理创建器代码参考org.springframework.aop.framework.autoproxy 包
收获💡
AnnotationAwareAspectJAutoProxyCreator 的作用
将高级 @Aspect 切面统一为低级 Advisor 切面
在合适的时机创建代理


findEligibleAdvisors 找到有【资格】的 Advisors
有【资格】的 Advisor 一部分是低级的, 可以由自己编写, 如本例 A17 中的 advisor3
有【资格】的 Advisor 另一部分是高级的, 由解析 @Aspect 后获得


wrapIfNecessary
它内部调用 findEligibleAdvisors, 只要返回集合不空, 则表示需要创建代理
它的调用时机通常在原始对象初始化后执行, 但碰到循环依赖会提前至依赖注入之前执行



演示2 - 代理创建时机代码参考org.springframework.aop.framework.autoproxy.A17_1
收获💡
代理的创建时机
初始化之后 (无循环依赖时)
实例创建后, 依赖注入前 (有循环依赖时), 并暂存于二级缓存


依赖注入与初始化不应该被增强, 仍应被施加于原始对象

演示3 - @Before 对应的低级通知代码参考org.springframework.aop.framework.autoproxy.A17_2
收获💡
@Before 前置通知会被转换为原始的 AspectJMethodBeforeAdvice 形式, 该对象包含了如下信息
通知代码从哪儿来
切点是什么(这里为啥要切点, 后面解释)
通知对象如何创建, 本例共用同一个 Aspect 对象


类似的还有
AspectJAroundAdvice (环绕通知)
AspectJAfterReturningAdvice
AspectJAfterThrowingAdvice (环绕通知)
AspectJAfterAdvice (环绕通知)



18) 静态通知调用代理对象调用流程如下（以 JDK 动态代理实现为例）

从 ProxyFactory 获得 Target 和环绕通知链，根据他俩创建 MethodInvocation，简称 mi
首次执行 mi.proceed() 发现有下一个环绕通知，调用它的 invoke(mi)
进入环绕通知1，执行前增强，再次调用 mi.proceed() 发现有下一个环绕通知，调用它的 invoke(mi)
进入环绕通知2，执行前增强，调用 mi.proceed() 发现没有环绕通知，调用 mi.invokeJoinPoint() 执行目标方法
目标方法执行结束，将结果返回给环绕通知2，执行环绕通知2 的后增强
环绕通知2继续将结果返回给环绕通知1，执行环绕通知1 的后增强
环绕通知1返回最终的结果

图中不同颜色对应一次环绕通知或目标的调用起始至终结
sequenceDiagramparticipant Proxyparticipant ih as InvocationHandlerparticipant mi as MethodInvocationparticipant Factory as ProxyFactoryparticipant mi1 as MethodInterceptor1participant mi2 as MethodInterceptor2participant TargetProxy -&gt;&gt; +ih : invoke()ih -&gt;&gt; +Factory : 获得 TargetFactory --&gt;&gt; -ih : ih -&gt;&gt; +Factory : 获得 MethodInterceptor 链Factory --&gt;&gt; -ih : ih -&gt;&gt; +mi : 创建 mimi --&gt;&gt; -ih : rect rgb(200, 223, 255)ih -&gt;&gt; +mi : mi.proceed()mi -&gt;&gt; +mi1 : invoke(mi)mi1 -&gt;&gt; mi1 : 前增强rect rgb(200, 190, 255)mi1 -&gt;&gt; mi : mi.proceed()mi -&gt;&gt; +mi2 : invoke(mi)mi2 -&gt;&gt; mi2 : 前增强rect rgb(150, 190, 155)mi2 -&gt;&gt; mi : mi.proceed()mi -&gt;&gt; +Target : mi.invokeJoinPoint()Target -&gt;&gt; Target : Target --&gt;&gt; -mi2 : 结果endmi2 -&gt;&gt; mi2 : 后增强mi2 --&gt;&gt; -mi1 : 结果endmi1 -&gt;&gt; mi1 : 后增强mi1 --&gt;&gt; -mi : 结果mi --&gt;&gt; -ih : endih --&gt;&gt; -Proxy : 



演示1 - 通知调用过程代码参考org.springframework.aop.framework.A18
收获💡代理方法执行时会做如下工作

通过 proxyFactory 的 getInterceptorsAndDynamicInterceptionAdvice() 将其他通知统一转换为 MethodInterceptor 环绕通知
MethodBeforeAdviceAdapter 将 @Before AspectJMethodBeforeAdvice 适配为 MethodBeforeAdviceInterceptor
AfterReturningAdviceAdapter 将 @AfterReturning AspectJAfterReturningAdvice 适配为 AfterReturningAdviceInterceptor
这体现的是适配器设计模式


所谓静态通知，体现在上面方法的 Interceptors 部分，这些通知调用时无需再次检查切点，直接调用即可
结合目标与环绕通知链，创建 MethodInvocation 对象，通过它完成整个调用

演示2 - 模拟 MethodInvocation代码参考org.springframework.aop.framework.A18_1
收获💡
proceed() 方法调用链中下一个环绕通知
每个环绕通知内部继续调用 proceed()
调用到没有更多通知了, 就调用目标方法

MethodInvocation 的编程技巧在实现拦截器、过滤器时能用上
19) 动态通知调用演示 - 带参数绑定的通知方法调用代码参考org.springframework.aop.framework.autoproxy.A19
收获💡
通过 proxyFactory 的 getInterceptorsAndDynamicInterceptionAdvice() 将其他通知统一转换为 MethodInterceptor 环绕通知
所谓动态通知，体现在上面方法的 DynamicInterceptionAdvice 部分，这些通知调用时因为要为通知方法绑定参数，还需再次利用切点表达式
动态通知调用复杂程度高，性能较低

WEB20) RequestMappingHandlerMapping 与 RequestMappingHandlerAdapterRequestMappingHandlerMapping 与 RequestMappingHandlerAdapter 俩是一对，分别用来

处理 @RequestMapping 映射
调用控制器方法、并处理方法参数与方法返回值

演示1 - DispatcherServlet 初始化代码参考com.itheima.a20 包
收获💡
DispatcherServlet 是在第一次被访问时执行初始化, 也可以通过配置修改为 Tomcat 启动后就初始化
在初始化时会从 Spring 容器中找一些 Web 需要的组件, 如 HandlerMapping、HandlerAdapter 等，并逐一调用它们的初始化
RequestMappingHandlerMapping 初始化时，会收集所有 @RequestMapping 映射信息，封装为 Map，其中
key 是 RequestMappingInfo 类型，包括请求路径、请求方法等信息
value 是 HandlerMethod 类型，包括控制器方法对象、控制器对象
有了这个 Map，就可以在请求到达时，快速完成映射，找到 HandlerMethod 并与匹配的拦截器一起返回给 DispatcherServlet


RequestMappingHandlerAdapter 初始化时，会准备 HandlerMethod 调用时需要的各个组件，如：
HandlerMethodArgumentResolver 解析控制器方法参数
HandlerMethodReturnValueHandler 处理控制器方法返回值



演示2 - 自定义参数与返回值处理器代码参考com.itheima.a20.TokenArgumentResolver ，com.itheima.a20.YmlReturnValueHandler
收获💡
体会参数解析器的作用
体会返回值处理器的作用

21) 参数解析器演示 - 常见参数解析器代码参考com.itheima.a21 包
收获💡
初步了解 RequestMappingHandlerAdapter 的调用过程
控制器方法被封装为 HandlerMethod
准备对象绑定与类型转换
准备 ModelAndViewContainer 用来存储中间 Model 结果
解析每个参数值


解析参数依赖的就是各种参数解析器，它们都有两个重要方法
supportsParameter 判断是否支持方法参数
resolveArgument 解析方法参数


常见参数的解析
@RequestParam
省略 @RequestParam
@RequestParam(defaultValue)
MultipartFile
@PathVariable
@RequestHeader
@CookieValue
@Value
HttpServletRequest 等
@ModelAttribute
省略 @ModelAttribute
@RequestBody


组合模式在 Spring 中的体现
@RequestParam, @CookieValue 等注解中的参数名、默认值, 都可以写成活的, 即从 ${ } #{ }中获取

22) 参数名解析演示 - 两种方法获取参数名代码参考com.itheima.a22.A22
收获💡
如果编译时添加了 -parameters 可以生成参数表, 反射时就可以拿到参数名
如果编译时添加了 -g 可以生成调试信息, 但分为两种情况
普通类, 会包含局部变量表, 用 asm 可以拿到参数名
接口, 不会包含局部变量表, 无法获得参数名
这也是 MyBatis 在实现 Mapper 接口时为何要提供 @Param 注解来辅助获得参数名





23) 对象绑定与类型转换底层第一套转换接口与实现classDiagramFormatter --|&gt; PrinterFormatter --|&gt; Parserclass Converters &#123;   Set~GenericConverter~&#125;class Converterclass ConversionServiceclass FormattingConversionServiceConversionService &lt;|-- FormattingConversionServiceFormattingConversionService o-- ConvertersPrinter --&gt; Adapter1Adapter1 --&gt; ConvertersParser --&gt; Adapter2Adapter2 --&gt; ConvertersConverter --&gt; Adapter3Adapter3 --&gt; Converters&lt;&lt;interface&gt;&gt; Formatter&lt;&lt;interface&gt;&gt; Printer&lt;&lt;interface&gt;&gt; Parser&lt;&lt;interface&gt;&gt; Converter&lt;&lt;interface&gt;&gt; ConversionService


Printer 把其它类型转为 String
Parser 把 String 转为其它类型
Formatter 综合 Printer 与 Parser 功能
Converter 把类型 S 转为类型 T
Printer、Parser、Converter 经过适配转换成 GenericConverter 放入 Converters 集合
FormattingConversionService 利用其它们实现转换

底层第二套转换接口classDiagramPropertyEditorRegistry o-- &quot;多&quot; PropertyEditor&lt;&lt;interface&gt;&gt; PropertyEditorRegistry&lt;&lt;interface&gt;&gt; PropertyEditor


PropertyEditor 把 String 与其它类型相互转换
PropertyEditorRegistry 可以注册多个 PropertyEditor 对象
与第一套接口直接可以通过 FormatterPropertyEditorAdapter 来进行适配

高层接口与实现classDiagramTypeConverter &lt;|-- SimpleTypeConverterTypeConverter &lt;|-- BeanWrapperImplTypeConverter &lt;|-- DirectFieldAccessorTypeConverter &lt;|-- ServletRequestDataBinderSimpleTypeConverter --&gt; TypeConverterDelegateBeanWrapperImpl --&gt; TypeConverterDelegateDirectFieldAccessor --&gt; TypeConverterDelegateServletRequestDataBinder --&gt; TypeConverterDelegateTypeConverterDelegate --&gt; ConversionServiceTypeConverterDelegate --&gt; PropertyEditorRegistry&lt;&lt;interface&gt;&gt; TypeConverter&lt;&lt;interface&gt;&gt; ConversionService&lt;&lt;interface&gt;&gt; PropertyEditorRegistry


它们都实现了 TypeConverter 这个高层转换接口，在转换时，会用到 TypeConverter Delegate 委派ConversionService 与 PropertyEditorRegistry 真正执行转换（Facade 门面模式）
首先看是否有自定义转换器, @InitBinder 添加的即属于这种 (用了适配器模式把 Formatter 转为需要的 PropertyEditor)
再看有没有 ConversionService 转换
再利用默认的 PropertyEditor 转换
最后有一些特殊处理


SimpleTypeConverter 仅做类型转换
BeanWrapperImpl 为 bean 的属性赋值，当需要时做类型转换，走 Property
DirectFieldAccessor 为 bean 的属性赋值，当需要时做类型转换，走 Field
ServletRequestDataBinder 为 bean 的属性执行绑定，当需要时做类型转换，根据 directFieldAccess 选择走 Property 还是 Field，具备校验与获取校验结果功能

演示1 - 类型转换与数据绑定代码参考com.itheima.a23 包
收获💡基本的类型转换与数据绑定用法

SimpleTypeConverter
BeanWrapperImpl
DirectFieldAccessor
ServletRequestDataBinder

演示2 - 数据绑定工厂代码参考com.itheima.a23.TestServletDataBinderFactory
收获💡ServletRequestDataBinderFactory 的用法和扩展点

可以解析控制器的 @InitBinder 标注方法作为扩展点，添加自定义转换器
控制器私有范围


可以通过 ConfigurableWebBindingInitializer 配置 ConversionService 作为扩展点，添加自定义转换器
公共范围


同时加了 @InitBinder 和 ConversionService 的转换优先级
优先采用 @InitBinder 的转换器
其次使用 ConversionService 的转换器
使用默认转换器
特殊处理（例如有参构造）



演示3 - 获取泛型参数代码参考com.itheima.a23.sub 包
收获💡
java api 获取泛型参数
spring api 获取泛型参数

24) @ControllerAdvice 之 @InitBinder演示 - 准备 @InitBinder准备 @InitBinder 在整个 HandlerAdapter 调用过程中所处的位置
sequenceDiagramparticipant adapter as HandlerAdapterparticipant bf as WebDataBinderFactoryparticipant mf as ModelFactoryparticipant ihm as ServletInvocableHandlerMethodparticipant ar as ArgumentResolvers participant rh as ReturnValueHandlersparticipant container as ModelAndViewContainerrect rgb(200, 150, 255)adapter -&gt;&gt; +bf: 准备 @InitBinderbf --&gt;&gt; -adapter: endadapter -&gt;&gt; +mf: 准备 @ModelAttributemf -&gt;&gt; +container: 添加Model数据container --&gt;&gt; -mf: mf --&gt;&gt; -adapter: adapter -&gt;&gt; +ihm: invokeAndHandleihm -&gt;&gt; +ar: 获取 argsar -&gt;&gt; ar: 有的解析器涉及 RequestBodyAdvicear -&gt;&gt; container: 有的解析器涉及数据绑定生成Model数据ar --&gt;&gt; -ihm: argsihm -&gt;&gt; ihm: method.invoke(bean,args) 得到 returnValueihm -&gt;&gt; +rh: 处理 returnValuerh -&gt;&gt; rh: 有的处理器涉及 ResponseBodyAdvicerh -&gt;&gt; +container: 添加Model数据,处理视图名,是否渲染等container --&gt;&gt; -rh: rh --&gt;&gt; -ihm: ihm --&gt;&gt; -adapter: adapter -&gt;&gt; +container: 获取 ModelAndViewcontainer --&gt;&gt; -adapter: 


RequestMappingHandlerAdapter 在图中缩写为 HandlerAdapter
HandlerMethodArgumentResolverComposite 在图中缩写为 ArgumentResolvers
HandlerMethodReturnValueHandlerComposite 在图中缩写为 ReturnValueHandlers

收获💡
RequestMappingHandlerAdapter 初始化时会解析 @ControllerAdvice 中的 @InitBinder 方法
RequestMappingHandlerAdapter 会以类为单位，在该类首次使用时，解析此类的 @InitBinder 方法
以上两种 @InitBinder 的解析结果都会缓存来避免重复解析
控制器方法调用时，会综合利用本类的 @InitBinder 方法和 @ControllerAdvice 中的 @InitBinder 方法创建绑定工厂

25) 控制器方法执行流程图1classDiagramclass ServletInvocableHandlerMethod &#123;	+invokeAndHandle(ServletWebRequest,ModelAndViewContainer)&#125;HandlerMethod &lt;|-- ServletInvocableHandlerMethodHandlerMethod o-- beanHandlerMethod o-- methodServletInvocableHandlerMethod o-- WebDataBinderFactoryServletInvocableHandlerMethod o-- ParameterNameDiscovererServletInvocableHandlerMethod o-- HandlerMethodArgumentResolverCompositeServletInvocableHandlerMethod o-- HandlerMethodReturnValueHandlerComposite

HandlerMethod 需要

bean 即是哪个 Controller
method 即是 Controller 中的哪个方法

ServletInvocableHandlerMethod 需要

WebDataBinderFactory 负责对象绑定、类型转换
ParameterNameDiscoverer 负责参数名解析
HandlerMethodArgumentResolverComposite 负责解析参数
HandlerMethodReturnValueHandlerComposite 负责处理返回值

图2sequenceDiagramparticipant adapter as RequestMappingHandlerAdapterparticipant bf as WebDataBinderFactoryparticipant mf as ModelFactoryparticipant container as ModelAndViewContaineradapter -&gt;&gt; +bf: 准备 @InitBinderbf --&gt;&gt; -adapter: adapter -&gt;&gt; +mf: 准备 @ModelAttributemf -&gt;&gt; +container: 添加Model数据container --&gt;&gt; -mf: mf --&gt;&gt; -adapter: 

图3sequenceDiagramparticipant adapter as RequestMappingHandlerAdapterparticipant ihm as ServletInvocableHandlerMethodparticipant ar as ArgumentResolversparticipant rh as ReturnValueHandlersparticipant container as ModelAndViewContaineradapter -&gt;&gt; +ihm: invokeAndHandleihm -&gt;&gt; +ar: 获取 argsar -&gt;&gt; ar: 有的解析器涉及 RequestBodyAdvicear -&gt;&gt; container: 有的解析器涉及数据绑定生成模型数据container --&gt;&gt; ar: ar --&gt;&gt; -ihm: argsihm -&gt;&gt; ihm: method.invoke(bean,args) 得到 returnValueihm -&gt;&gt; +rh: 处理 returnValuerh -&gt;&gt; rh: 有的处理器涉及 ResponseBodyAdvicerh -&gt;&gt; +container: 添加Model数据,处理视图名,是否渲染等container --&gt;&gt; -rh: rh --&gt;&gt; -ihm: ihm --&gt;&gt; -adapter: adapter -&gt;&gt; +container: 获取 ModelAndViewcontainer --&gt;&gt; -adapter: 



26) @ControllerAdvice 之 @ModelAttribute演示 - 准备 @ModelAttribute代码参考com.itheima.a26 包
准备 @ModelAttribute 在整个 HandlerAdapter 调用过程中所处的位置
sequenceDiagramparticipant adapter as HandlerAdapterparticipant bf as WebDataBinderFactoryparticipant mf as ModelFactoryparticipant ihm as ServletInvocableHandlerMethodparticipant ar as ArgumentResolvers participant rh as ReturnValueHandlersparticipant container as ModelAndViewContaineradapter -&gt;&gt; +bf: 准备 @InitBinderbf --&gt;&gt; -adapter: rect rgb(200, 150, 255)adapter -&gt;&gt; +mf: 准备 @ModelAttributemf -&gt;&gt; +container: 添加Model数据container --&gt;&gt; -mf: mf --&gt;&gt; -adapter: endadapter -&gt;&gt; +ihm: invokeAndHandleihm -&gt;&gt; +ar: 获取 argsar -&gt;&gt; ar: 有的解析器涉及 RequestBodyAdvicear -&gt;&gt; container: 有的解析器涉及数据绑定生成Model数据ar --&gt;&gt; -ihm: argsihm -&gt;&gt; ihm: method.invoke(bean,args) 得到 returnValueihm -&gt;&gt; +rh: 处理 returnValuerh -&gt;&gt; rh: 有的处理器涉及 ResponseBodyAdvicerh -&gt;&gt; +container: 添加Model数据,处理视图名,是否渲染等container --&gt;&gt; -rh: rh --&gt;&gt; -ihm: ihm --&gt;&gt; -adapter: adapter -&gt;&gt; +container: 获取 ModelAndViewcontainer --&gt;&gt; -adapter: 

收获💡
RequestMappingHandlerAdapter 初始化时会解析 @ControllerAdvice 中的 @ModelAttribute 方法
RequestMappingHandlerAdapter 会以类为单位，在该类首次使用时，解析此类的 @ModelAttribute 方法
以上两种 @ModelAttribute 的解析结果都会缓存来避免重复解析
控制器方法调用时，会综合利用本类的 @ModelAttribute 方法和 @ControllerAdvice 中的 @ModelAttribute 方法创建模型工厂

27) 返回值处理器演示 - 常见返回值处理器代码参考com.itheima.a27 包
收获💡
常见的返回值处理器
ModelAndView，分别获取其模型和视图名，放入 ModelAndViewContainer
返回值类型为 String 时，把它当做视图名，放入 ModelAndViewContainer
返回值添加了 @ModelAttribute 注解时，将返回值作为模型，放入 ModelAndViewContainer
此时需找到默认视图名


返回值省略 @ModelAttribute 注解且返回非简单类型时，将返回值作为模型，放入 ModelAndViewContainer
此时需找到默认视图名


返回值类型为 ResponseEntity 时
此时走 MessageConverter，并设置 ModelAndViewContainer.requestHandled 为 true


返回值类型为 HttpHeaders 时
会设置 ModelAndViewContainer.requestHandled 为 true


返回值添加了 @ResponseBody 注解时
此时走 MessageConverter，并设置 ModelAndViewContainer.requestHandled 为 true




组合模式在 Spring 中的体现 + 1

28) MessageConverter演示 - MessageConverter 的作用代码参考com.itheima.a28.A28
收获💡
MessageConverter 的作用
@ResponseBody 是返回值处理器解析的
但具体转换工作是 MessageConverter 做的


如何选择 MediaType
首先看 @RequestMapping 上有没有指定
其次看 request 的 Accept 头有没有指定
最后按 MessageConverter 的顺序, 谁能谁先转换



29) @ControllerAdvice 之 ResponseBodyAdvice演示 - ResponseBodyAdvice 增强代码参考com.itheima.a29 包
ResponseBodyAdvice 增强 在整个 HandlerAdapter 调用过程中所处的位置
sequenceDiagramparticipant adapter as HandlerAdapterparticipant bf as WebDataBinderFactoryparticipant mf as ModelFactoryparticipant ihm as ServletInvocableHandlerMethodparticipant ar as ArgumentResolvers participant rh as ReturnValueHandlersparticipant container as ModelAndViewContaineradapter -&gt;&gt; +bf: 准备 @InitBinderbf --&gt;&gt; -adapter: adapter -&gt;&gt; +mf: 准备 @ModelAttributemf -&gt;&gt; +container: 添加Model数据container --&gt;&gt; -mf: mf --&gt;&gt; -adapter: adapter -&gt;&gt; +ihm: invokeAndHandleihm -&gt;&gt; +ar: 获取 argsar -&gt;&gt; ar: 有的解析器涉及 RequestBodyAdvicear -&gt;&gt; container: 有的解析器涉及数据绑定生成Model数据ar --&gt;&gt; -ihm: argsihm -&gt;&gt; ihm: method.invoke(bean,args) 得到 returnValueihm -&gt;&gt; +rh: 处理 returnValuerect rgb(200, 150, 255)rh -&gt;&gt; rh: 有的处理器涉及 ResponseBodyAdviceendrh -&gt;&gt; +container: 添加Model数据,处理视图名,是否渲染等container --&gt;&gt; -rh: rh --&gt;&gt; -ihm: ihm --&gt;&gt; -adapter: adapter -&gt;&gt; +container: 获取 ModelAndViewcontainer --&gt;&gt; -adapter: 

收获💡
ResponseBodyAdvice 返回响应体前包装

30) 异常解析器演示 - ExceptionHandlerExceptionResolver代码参考com.itheima.a30.A30
收获💡
它能够重用参数解析器、返回值处理器，实现组件重用
它能够支持嵌套异常

31) @ControllerAdvice 之 @ExceptionHandler演示 - 准备 @ExceptionHandler代码参考com.itheima.a31 包
收获💡
ExceptionHandlerExceptionResolver 初始化时会解析 @ControllerAdvice 中的 @ExceptionHandler 方法
ExceptionHandlerExceptionResolver 会以类为单位，在该类首次处理异常时，解析此类的 @ExceptionHandler 方法
以上两种 @ExceptionHandler 的解析结果都会缓存来避免重复解析

32) Tomcat 异常处理
我们知道 @ExceptionHandler 只能处理发生在 mvc 流程中的异常，例如控制器内、拦截器内，那么如果是 Filter 出现了异常，如何进行处理呢？

在 Spring Boot 中，是这么实现的：

因为内嵌了 Tomcat 容器，因此可以配置 Tomcat 的错误页面，Filter 与 错误页面之间是通过请求转发跳转的，可以在这里做手脚
先通过 ErrorPageRegistrarBeanPostProcessor 这个后处理器配置错误页面地址，默认为 /error 也可以通过 ${server.error.path} 进行配置
当 Filter 发生异常时，不会走 Spring 流程，但会走 Tomcat 的错误处理，于是就希望转发至 /error 这个地址
当然，如果没有 @ExceptionHandler，那么最终也会走到 Tomcat 的错误处理


Spring Boot 又提供了一个 BasicErrorController，它就是一个标准 @Controller，@RequestMapping 配置为 /error，所以处理异常的职责就又回到了 Spring
异常信息由于会被 Tomcat 放入 request 作用域，因此 BasicErrorController 里也能获取到
具体异常信息会由 DefaultErrorAttributes 封装好
BasicErrorController 通过 Accept 头判断需要生成哪种 MediaType 的响应
如果要的不是 text&#x2F;html，走 MessageConverter 流程
如果需要 text&#x2F;html，走 mvc 流程，此时又分两种情况
配置了 ErrorViewResolver，根据状态码去找 View
没配置或没找到，用 BeanNameViewResolver 根据一个固定为 error 的名字找到 View，即所谓的 WhitelabelErrorView








评价

一个错误处理搞得这么复杂，就问恶心不？


演示1 - 错误页处理关键代码@Bean // ⬅️修改了 Tomcat 服务器默认错误地址, 出错时使用请求转发方式跳转public ErrorPageRegistrar errorPageRegistrar() &#123;    return webServerFactory -&gt; webServerFactory.addErrorPages(new ErrorPage(&quot;/error&quot;));&#125;@Bean // ⬅️TomcatServletWebServerFactory 初始化前用它增强, 注册所有 ErrorPageRegistrarpublic ErrorPageRegistrarBeanPostProcessor errorPageRegistrarBeanPostProcessor() &#123;    return new ErrorPageRegistrarBeanPostProcessor();&#125;

收获💡
Tomcat 的错误页处理手段

演示2 - BasicErrorController关键代码@Bean // ⬅️ErrorProperties 封装环境键值, ErrorAttributes 控制有哪些错误信息public BasicErrorController basicErrorController() &#123;    ErrorProperties errorProperties = new ErrorProperties();    errorProperties.setIncludeException(true);    return new BasicErrorController(new DefaultErrorAttributes(), errorProperties);&#125;@Bean // ⬅️名称为 error 的视图, 作为 BasicErrorController 的 text/html 响应结果public View error() &#123;    return new View() &#123;        @Override        public void render(            Map&lt;String, ?&gt; model,             HttpServletRequest request,             HttpServletResponse response        ) throws Exception &#123;            System.out.println(model);            response.setContentType(&quot;text/html;charset=utf-8&quot;);            response.getWriter().print(&quot;&quot;&quot;                    &lt;h3&gt;服务器内部错误&lt;/h3&gt;                    &quot;&quot;&quot;);        &#125;    &#125;;&#125;@Bean // ⬅️收集容器中所有 View 对象, bean 的名字作为视图名public ViewResolver viewResolver() &#123;    return new BeanNameViewResolver();&#125;

收获💡
Spring Boot 中 BasicErrorController 如何工作

33) BeanNameUrlHandlerMapping 与 SimpleControllerHandlerAdapter演示 - 本组映射器和适配器关键代码@Beanpublic BeanNameUrlHandlerMapping beanNameUrlHandlerMapping() &#123;    return new BeanNameUrlHandlerMapping();&#125;@Beanpublic SimpleControllerHandlerAdapter simpleControllerHandlerAdapter() &#123;    return new SimpleControllerHandlerAdapter();&#125;@Bean(&quot;/c3&quot;)public Controller controller3() &#123;    return (request, response) -&gt; &#123;        response.getWriter().print(&quot;this is c3&quot;);        return null;    &#125;;&#125;

收获💡
BeanNameUrlHandlerMapping，以 &#x2F; 开头的 bean 的名字会被当作映射路径
这些 bean 本身当作 handler，要求实现 Controller 接口
SimpleControllerHandlerAdapter，调用 handler
模拟实现这组映射器和适配器

34) RouterFunctionMapping 与 HandlerFunctionAdapter演示 - 本组映射器和适配器关键代码@Beanpublic RouterFunctionMapping routerFunctionMapping() &#123;    return new RouterFunctionMapping();&#125;@Beanpublic HandlerFunctionAdapter handlerFunctionAdapter() &#123;    return new HandlerFunctionAdapter();&#125;@Beanpublic RouterFunction&lt;ServerResponse&gt; r1() &#123;    //           ⬇️映射条件   ⬇️handler    return route(GET(&quot;/r1&quot;), request -&gt; ok().body(&quot;this is r1&quot;));&#125;

收获💡
RouterFunctionMapping, 通过 RequestPredicate 条件映射
handler 要实现 HandlerFunction 接口
HandlerFunctionAdapter, 调用 handler

35) SimpleUrlHandlerMapping 与 HttpRequestHandlerAdapter演示1 - 本组映射器和适配器代码参考org.springframework.boot.autoconfigure.web.servlet.A35
关键代码@Beanpublic SimpleUrlHandlerMapping simpleUrlHandlerMapping(ApplicationContext context) &#123;    SimpleUrlHandlerMapping handlerMapping = new SimpleUrlHandlerMapping();    Map&lt;String, ResourceHttpRequestHandler&gt; map         = context.getBeansOfType(ResourceHttpRequestHandler.class);    handlerMapping.setUrlMap(map);    return handlerMapping;&#125;@Beanpublic HttpRequestHandlerAdapter httpRequestHandlerAdapter() &#123;    return new HttpRequestHandlerAdapter();&#125;@Bean(&quot;/**&quot;)public ResourceHttpRequestHandler handler1() &#123;    ResourceHttpRequestHandler handler = new ResourceHttpRequestHandler();    handler.setLocations(List.of(new ClassPathResource(&quot;static/&quot;)));    return handler;&#125;@Bean(&quot;/img/**&quot;)public ResourceHttpRequestHandler handler2() &#123;    ResourceHttpRequestHandler handler = new ResourceHttpRequestHandler();    handler.setLocations(List.of(new ClassPathResource(&quot;images/&quot;)));    return handler;&#125;

收获💡
SimpleUrlHandlerMapping 不会在初始化时收集映射信息，需要手动收集
SimpleUrlHandlerMapping 映射路径
ResourceHttpRequestHandler 作为静态资源 handler
HttpRequestHandlerAdapter, 调用此 handler

演示2 - 静态资源解析优化关键代码@Bean(&quot;/**&quot;)public ResourceHttpRequestHandler handler1() &#123;    ResourceHttpRequestHandler handler = new ResourceHttpRequestHandler();    handler.setLocations(List.of(new ClassPathResource(&quot;static/&quot;)));    handler.setResourceResolvers(List.of(        	// ⬇️缓存优化            new CachingResourceResolver(new ConcurrentMapCache(&quot;cache1&quot;)),        	// ⬇️压缩优化            new EncodedResourceResolver(),        	// ⬇️原始资源解析            new PathResourceResolver()    ));    return handler;&#125;

收获💡
责任链模式体现
压缩文件需要手动生成

演示3 - 欢迎页关键代码@Beanpublic WelcomePageHandlerMapping welcomePageHandlerMapping(ApplicationContext context) &#123;    Resource resource = context.getResource(&quot;classpath:static/index.html&quot;);    return new WelcomePageHandlerMapping(null, context, resource, &quot;/**&quot;);&#125;@Beanpublic SimpleControllerHandlerAdapter simpleControllerHandlerAdapter() &#123;    return new SimpleControllerHandlerAdapter();&#125;

收获💡
欢迎页支持静态欢迎页与动态欢迎页
WelcomePageHandlerMapping 映射欢迎页（即只映射 ‘&#x2F;‘）
它内置的 handler ParameterizableViewController 作用是不执行逻辑，仅根据视图名找视图
视图名固定为 forward:index.html


SimpleControllerHandlerAdapter, 调用 handler
转发至 &#x2F;index.html
处理 &#x2F;index.html 又会走上面的静态资源处理流程



映射器与适配器小结
HandlerMapping 负责建立请求与控制器之间的映射关系
RequestMappingHandlerMapping (与 @RequestMapping 匹配)
WelcomePageHandlerMapping    (&#x2F;)
BeanNameUrlHandlerMapping    (与 bean 的名字匹配 以 &#x2F; 开头)
RouterFunctionMapping        (函数式 RequestPredicate, HandlerFunction)
SimpleUrlHandlerMapping      (静态资源 通配符 &#x2F;** &#x2F;img&#x2F;**)
之间也会有顺序问题, boot 中默认顺序如上


HandlerAdapter 负责实现对各种各样的 handler 的适配调用
RequestMappingHandlerAdapter 处理：@RequestMapping 方法
参数解析器、返回值处理器体现了组合模式


SimpleControllerHandlerAdapter 处理：Controller 接口
HandlerFunctionAdapter 处理：HandlerFunction 函数式接口
HttpRequestHandlerAdapter 处理：HttpRequestHandler 接口 (静态资源处理)
这也是典型适配器模式体现



36) mvc 处理流程当浏览器发送一个请求 http://localhost:8080/hello 后，请求到达服务器，其处理流程是：

服务器提供了 DispatcherServlet，它使用的是标准 Servlet 技术

路径：默认映射路径为 /，即会匹配到所有请求 URL，可作为请求的统一入口，也被称之为前控制器
jsp 不会匹配到 DispatcherServlet
其它有路径的 Servlet 匹配优先级也高于 DispatcherServlet


创建：在 Boot 中，由 DispatcherServletAutoConfiguration 这个自动配置类提供 DispatcherServlet 的 bean
初始化：DispatcherServlet 初始化时会优先到容器里寻找各种组件，作为它的成员变量
HandlerMapping，初始化时记录映射关系
HandlerAdapter，初始化时准备参数解析器、返回值处理器、消息转换器
HandlerExceptionResolver，初始化时准备参数解析器、返回值处理器、消息转换器
ViewResolver




DispatcherServlet 会利用 RequestMappingHandlerMapping 查找控制器方法

例如根据 &#x2F;hello 路径找到 @RequestMapping(“&#x2F;hello”) 对应的控制器方法

控制器方法会被封装为 HandlerMethod 对象，并结合匹配到的拦截器一起返回给 DispatcherServlet 

HandlerMethod 和拦截器合在一起称为 HandlerExecutionChain（调用链）对象



DispatcherServlet 接下来会：

调用拦截器的 preHandle 方法
RequestMappingHandlerAdapter 调用 handle 方法，准备数据绑定工厂、模型工厂、ModelAndViewContainer、将 HandlerMethod 完善为 ServletInvocableHandlerMethod
@ControllerAdvice 全局增强点1️⃣：补充模型数据
@ControllerAdvice 全局增强点2️⃣：补充自定义类型转换器
使用 HandlerMethodArgumentResolver 准备参数
@ControllerAdvice 全局增强点3️⃣：RequestBody 增强


调用 ServletInvocableHandlerMethod 
使用 HandlerMethodReturnValueHandler 处理返回值
@ControllerAdvice 全局增强点4️⃣：ResponseBody 增强


根据 ModelAndViewContainer 获取 ModelAndView
如果返回的 ModelAndView 为 null，不走第 4 步视图解析及渲染流程
例如，有的返回值处理器调用了 HttpMessageConverter 来将结果转换为 JSON，这时 ModelAndView 就为 null


如果返回的 ModelAndView 不为 null，会在第 4 步走视图解析及渲染流程




调用拦截器的 postHandle 方法
处理异常或视图渲染
如果 1~3 出现异常，走 ExceptionHandlerExceptionResolver 处理异常流程
@ControllerAdvice 全局增强点5️⃣：@ExceptionHandler 异常处理


正常，走视图解析及渲染流程


调用拦截器的 afterCompletion 方法



Boot37) Boot 骨架项目如果是 linux 环境，用以下命令即可获取 spring boot 的骨架 pom.xml
curl -G https://start.spring.io/pom.xml -d dependencies=web,mysql,mybatis -o pom.xml

也可以使用 Postman 等工具实现
若想获取更多用法，请参考
curl https://start.spring.io



38) Boot War项目步骤1：创建模块，区别在于打包方式选择 war


接下来勾选 Spring Web 支持


步骤2：编写控制器
@Controllerpublic class MyController &#123;    @RequestMapping(&quot;/hello&quot;)    public String abc() &#123;        System.out.println(&quot;进入了控制器&quot;);        return &quot;hello&quot;;    &#125;&#125;

步骤3：编写 jsp 视图，新建 webapp 目录和一个 hello.jsp 文件，注意文件名与控制器方法返回的视图逻辑名一致
src	|- main		|- java		|- resources		|- webapp			|- hello.jsp

步骤4：配置视图路径，打开 application.properties 文件
spring.mvc.view.prefix=/spring.mvc.view.suffix=.jsp


将来 prefix + 控制器方法返回值 + suffix 即为视图完整路径

测试如果用 mvn 插件 mvn spring-boot:run 或 main 方法测试

必须添加如下依赖，因为此时用的还是内嵌 tomcat，而内嵌 tomcat 默认不带 jasper（用来解析 jsp）

&lt;dependency&gt;    &lt;groupId&gt;org.apache.tomcat.embed&lt;/groupId&gt;    &lt;artifactId&gt;tomcat-embed-jasper&lt;/artifactId&gt;    &lt;scope&gt;provided&lt;/scope&gt;&lt;/dependency&gt;

也可以使用 Idea 配置 tomcat 来测试，此时用的是外置 tomcat

骨架生成的代码中，多了一个 ServletInitializer，它的作用就是配置外置 Tomcat 使用的，在外置 Tomcat 启动后，去调用它创建和运行 SpringApplication

启示对于 jar 项目，若要支持 jsp，也可以在加入 jasper 依赖的前提下，把 jsp 文件置入 META-INF/resources 
39) Boot 启动过程阶段一：SpringApplication 构造

记录 BeanDefinition 源
推断应用类型
记录 ApplicationContext 初始化器
记录监听器
推断主启动类

阶段二：执行 run 方法

得到 SpringApplicationRunListeners，名字取得不好，实际是事件发布器

发布 application starting 事件1️⃣


封装启动 args

准备 Environment 添加命令行参数（*）

ConfigurationPropertySources 处理（*）

发布 application environment 已准备事件2️⃣


通过 EnvironmentPostProcessorApplicationListener 进行 env 后处理（*）

application.properties，由 StandardConfigDataLocationResolver 解析
spring.application.json


绑定 spring.main 到 SpringApplication 对象（*）

打印 banner（*）

创建容器

准备容器

发布 application context 已初始化事件3️⃣


加载 bean 定义

发布 application prepared 事件4️⃣


refresh 容器

发布 application started 事件5️⃣


执行 runner

发布 application ready 事件6️⃣

这其中有异常，发布 application failed 事件7️⃣





带 * 的有独立的示例

演示 - 启动过程com.itheima.a39.A39_1 对应 SpringApplication 构造
com.itheima.a39.A39_2 对应第1步，并演示 7 个事件
com.itheima.a39.A39_3 对应第2、8到12步
org.springframework.boot.Step3
org.springframework.boot.Step4
org.springframework.boot.Step5
org.springframework.boot.Step6
org.springframework.boot.Step7
收获💡
SpringApplication 构造方法中所做的操作
可以有多种源用来加载 bean 定义
应用类型推断
添加容器初始化器
添加监听器
演示主类推断


如何读取 spring.factories 中的配置
从配置中获取重要的事件发布器：SpringApplicationRunListeners
容器的创建、初始化器增强、加载 bean 定义等
CommandLineRunner、ApplicationRunner 的作用
环境对象
命令行 PropertySource
ConfigurationPropertySources 规范环境键名称
EnvironmentPostProcessor 后处理增强
由 EventPublishingRunListener 通过监听事件2️⃣来调用


绑定 spring.main 前缀的 key value 至 SpringApplication


Banner

40) Tomcat 内嵌容器Tomcat 基本结构
Server└───Service    ├───Connector (协议, 端口)    └───Engine        └───Host(虚拟主机 localhost)            ├───Context1 (应用1, 可以设置虚拟路径, / 即 url 起始路径; 项目磁盘路径, 即 docBase )            │   │   index.html            │   └───WEB-INF            │       │   web.xml (servlet, filter, listener) 3.0            │       ├───classes (servlet, controller, service ...)            │       ├───jsp            │       └───lib (第三方 jar 包)            └───Context2 (应用2)                │   index.html                └───WEB-INF                        web.xml

演示1 - Tomcat 内嵌容器关键代码public static void main(String[] args) throws LifecycleException, IOException &#123;    // 1.创建 Tomcat 对象    Tomcat tomcat = new Tomcat();    tomcat.setBaseDir(&quot;tomcat&quot;);    // 2.创建项目文件夹, 即 docBase 文件夹    File docBase = Files.createTempDirectory(&quot;boot.&quot;).toFile();    docBase.deleteOnExit();    // 3.创建 Tomcat 项目, 在 Tomcat 中称为 Context    Context context = tomcat.addContext(&quot;&quot;, docBase.getAbsolutePath());    // 4.编程添加 Servlet    context.addServletContainerInitializer(new ServletContainerInitializer() &#123;        @Override        public void onStartup(Set&lt;Class&lt;?&gt;&gt; c, ServletContext ctx) throws ServletException &#123;            HelloServlet helloServlet = new HelloServlet();            ctx.addServlet(&quot;aaa&quot;, helloServlet).addMapping(&quot;/hello&quot;);        &#125;    &#125;, Collections.emptySet());    // 5.启动 Tomcat    tomcat.start();    // 6.创建连接器, 设置监听端口    Connector connector = new Connector(new Http11Nio2Protocol());    connector.setPort(8080);    tomcat.setConnector(connector);&#125;



演示2 - 集成 Spring 容器关键代码WebApplicationContext springContext = getApplicationContext();// 4.编程添加 Servletcontext.addServletContainerInitializer(new ServletContainerInitializer() &#123;    @Override    public void onStartup(Set&lt;Class&lt;?&gt;&gt; c, ServletContext ctx) throws ServletException &#123;        // ⬇️通过 ServletRegistrationBean 添加 DispatcherServlet 等        for (ServletRegistrationBean registrationBean :              springContext.getBeansOfType(ServletRegistrationBean.class).values()) &#123;            registrationBean.onStartup(ctx);        &#125;    &#125;&#125;, Collections.emptySet());



41) Boot 自动配置AopAutoConfigurationSpring Boot 是利用了自动配置类来简化了 aop 相关配置

AOP 自动配置类为 org.springframework.boot.autoconfigure.aop.AopAutoConfiguration
可以通过 spring.aop.auto=false 禁用 aop 自动配置
AOP 自动配置的本质是通过 @EnableAspectJAutoProxy 来开启了自动代理，如果在引导类上自己添加了 @EnableAspectJAutoProxy 那么以自己添加的为准
@EnableAspectJAutoProxy 的本质是向容器中添加了 AnnotationAwareAspectJAutoProxyCreator 这个 bean 后处理器，它能够找到容器中所有切面，并为匹配切点的目标类创建代理，创建代理的工作一般是在 bean 的初始化阶段完成的

DataSourceAutoConfiguration
对应的自动配置类为：org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
它内部采用了条件装配，通过检查容器的 bean，以及类路径下的 class，来决定该 @Bean 是否生效

简单说明一下，Spring Boot 支持两大类数据源：

EmbeddedDatabase - 内嵌数据库连接池
PooledDataSource - 非内嵌数据库连接池

PooledDataSource 又支持如下数据源

hikari 提供的 HikariDataSource
tomcat-jdbc 提供的 DataSource
dbcp2 提供的 BasicDataSource
oracle 提供的 PoolDataSourceImpl

如果知道数据源的实现类类型，即指定了 spring.datasource.type，理论上可以支持所有数据源，但这样做的一个最大问题是无法订制每种数据源的详细配置（如最大、最小连接数等）
MybatisAutoConfiguration
MyBatis 自动配置类为 org.mybatis.spring.boot.autoconfigure.MybatisAutoConfiguration
它主要配置了两个 bean
SqlSessionFactory - MyBatis 核心对象，用来创建 SqlSession
SqlSessionTemplate - SqlSession 的实现，此实现会与当前线程绑定
用 ImportBeanDefinitionRegistrar 的方式扫描所有标注了 @Mapper 注解的接口
用 AutoConfigurationPackages 来确定扫描的包


还有一个相关的 bean：MybatisProperties，它会读取配置文件中带 mybatis. 前缀的配置项进行定制配置

@MapperScan 注解的作用与 MybatisAutoConfiguration 类似，会注册 MapperScannerConfigurer 有如下区别

@MapperScan 扫描具体包（当然也可以配置关注哪个注解）
@MapperScan 如果不指定扫描具体包，则会把引导类范围内，所有接口当做 Mapper 接口
MybatisAutoConfiguration 关注的是所有标注 @Mapper 注解的接口，会忽略掉非 @Mapper 标注的接口

这里有同学有疑问，之前介绍的都是将具体类交给 Spring 管理，怎么到了 MyBatis 这儿，接口就可以被管理呢？

其实并非将接口交给 Spring 管理，而是每个接口会对应一个 MapperFactoryBean，是后者被 Spring 所管理，接口只是作为 MapperFactoryBean 的一个属性来配置

TransactionAutoConfiguration
事务自动配置类有两个：

org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
org.springframework.boot.autoconfigure.transaction.TransactionAutoConfiguration


前者配置了 DataSourceTransactionManager 用来执行事务的提交、回滚操作

后者功能上对标 @EnableTransactionManagement，包含以下三个 bean

BeanFactoryTransactionAttributeSourceAdvisor 事务切面类，包含通知和切点
TransactionInterceptor 事务通知类，由它在目标方法调用前后加入事务操作
AnnotationTransactionAttributeSource 会解析 @Transactional 及事务属性，也包含了切点功能


如果自己配置了 DataSourceTransactionManager 或是在引导类加了 @EnableTransactionManagement，则以自己配置的为准


ServletWebServerFactoryAutoConfiguration
提供 ServletWebServerFactory

DispatcherServletAutoConfiguration
提供 DispatcherServlet
提供 DispatcherServletRegistrationBean

WebMvcAutoConfiguration
配置 DispatcherServlet 的各项组件，提供的 bean 见过的有
多项 HandlerMapping
多项 HandlerAdapter
HandlerExceptionResolver



ErrorMvcAutoConfiguration
提供的 bean 有 BasicErrorController

MultipartAutoConfiguration
它提供了 org.springframework.web.multipart.support.StandardServletMultipartResolver
该 bean 用来解析 multipart&#x2F;form-data 格式的数据

HttpEncodingAutoConfiguration
POST 请求参数如果有中文，无需特殊设置，这是因为 Spring Boot 已经配置了 org.springframework.boot.web.servlet.filter.OrderedCharacterEncodingFilter
对应配置 server.servlet.encoding.charset&#x3D;UTF-8，默认就是 UTF-8
当然，它只影响非 json 格式的数据

演示 - 自动配置类原理关键代码假设已有第三方的两个自动配置类
@Configuration // ⬅️第三方的配置类static class AutoConfiguration1 &#123;    @Bean    public Bean1 bean1() &#123;        return new Bean1();    &#125;&#125;@Configuration // ⬅️第三方的配置类static class AutoConfiguration2 &#123;    @Bean    public Bean2 bean2() &#123;        return new Bean2();    &#125;&#125;

提供一个配置文件 META-INF&#x2F;spring.factories，key 为导入器类名，值为多个自动配置类名，用逗号分隔
MyImportSelector=\AutoConfiguration1,\AutoConfiguration2


注意

上述配置文件中 MyImportSelector 与 AutoConfiguration1，AutoConfiguration2 为简洁均省略了包名，自己测试时请将包名根据情况补全


引入自动配置
@Configuration // ⬅️本项目的配置类@Import(MyImportSelector.class)static class Config &#123; &#125;static class MyImportSelector implements DeferredImportSelector &#123;    // ⬇️该方法从 META-INF/spring.factories 读取自动配置类名，返回的 String[] 即为要导入的配置类    public String[] selectImports(AnnotationMetadata importingClassMetadata) &#123;        return SpringFactoriesLoader            .loadFactoryNames(MyImportSelector.class, null).toArray(new String[0]);    &#125;&#125;

收获💡
自动配置类本质上就是一个配置类而已，只是用 META-INF&#x2F;spring.factories 管理，与应用配置类解耦
@Enable 打头的注解本质是利用了 @Import
@Import 配合 DeferredImportSelector 即可实现导入，selectImports 方法的返回值即为要导入的配置类名
DeferredImportSelector 的导入会在最后执行，为的是让其它配置优先解析

42) 条件装配底层条件装配的底层是本质上是 @Conditional 与 Condition，这两个注解。引入自动配置类时，期望满足一定条件才能被 Spring 管理，不满足则不管理，怎么做呢？
比如条件是【类路径下必须有 dataSource】这个 bean ，怎么做呢？
首先编写条件判断类，它实现 Condition 接口，编写条件判断逻辑
static class MyCondition1 implements Condition &#123;     // ⬇️如果存在 Druid 依赖，条件成立    public boolean matches(ConditionContext context, AnnotatedTypeMetadata metadata) &#123;        return ClassUtils.isPresent(&quot;com.alibaba.druid.pool.DruidDataSource&quot;, null);    &#125;&#125;

其次，在要导入的自动配置类上添加 @Conditional(MyCondition1.class)，将来此类被导入时就会做条件检查
@Configuration // 第三方的配置类@Conditional(MyCondition1.class) // ⬅️加入条件static class AutoConfiguration1 &#123;    @Bean    public Bean1 bean1() &#123;        return new Bean1();    &#125;&#125;

分别测试加入和去除 druid 依赖，观察 bean1 是否存在于容器
&lt;dependency&gt;    &lt;groupId&gt;com.alibaba&lt;/groupId&gt;    &lt;artifactId&gt;druid&lt;/artifactId&gt;    &lt;version&gt;1.1.17&lt;/version&gt;&lt;/dependency&gt;

收获💡
学习一种特殊的 if - else

其它43) FactoryBean演示 - FactoryBean代码参考com.itheima.a43 包
收获💡
它的作用是用制造创建过程较为复杂的产品, 如 SqlSessionFactory, 但 @Bean 已具备等价功能
使用上较为古怪, 一不留神就会用错
被 FactoryBean 创建的产品
会认为创建、依赖注入、Aware 接口回调、前初始化这些都是 FactoryBean 的职责, 这些流程都不会走
唯有后初始化的流程会走, 也就是产品可以被代理增强
单例的产品不会存储于 BeanFactory 的 singletonObjects 成员中, 而是另一个 factoryBeanObjectCache 成员中


按名字去获取时, 拿到的是产品对象, 名字前面加 &amp; 获取的是工厂对象



44) @Indexed 原理真实项目中，只需要加入以下依赖即可
&lt;dependency&gt;    &lt;groupId&gt;org.springframework&lt;/groupId&gt;    &lt;artifactId&gt;spring-context-indexer&lt;/artifactId&gt;    &lt;optional&gt;true&lt;/optional&gt;&lt;/dependency&gt;



演示 - @Indexed代码参考com.itheima.a44 包
收获💡
在编译时就根据 @Indexed 生成 META-INF&#x2F;spring.components 文件
扫描时
如果发现 META-INF&#x2F;spring.components 存在, 以它为准加载 bean definition
否则, 会遍历包下所有 class 资源 (包括 jar 内的)


解决的问题，在编译期就找到 @Component 组件，节省运行期间扫描 @Component 的时间

45) 代理进一步理解演示 - 代理代码参考com.itheima.a45 包
收获💡
spring 代理的设计特点

依赖注入和初始化影响的是原始对象

因此 cglib 不能用 MethodProxy.invokeSuper()


代理与目标是两个对象，二者成员变量并不共用数据



static 方法、final 方法、private 方法均无法增强

进一步理解代理增强基于方法重写



46) @Value 装配底层按类型装配的步骤
查看需要的类型是否为 Optional，是，则进行封装（非延迟），否则向下走
查看需要的类型是否为 ObjectFactory 或 ObjectProvider，是，则进行封装（延迟），否则向下走
查看需要的类型（成员或参数）上是否用 @Lazy 修饰，是，则返回代理，否则向下走
解析 @Value 的值
如果需要的值是字符串，先解析 ${ }，再解析 #{ }
不是字符串，需要用 TypeConverter 转换


看需要的类型是否为 Stream、Array、Collection、Map，是，则按集合处理，否则向下走
在 BeanFactory 的 resolvableDependencies 中找有没有类型合适的对象注入，没有向下走
在 BeanFactory 及父工厂中找类型匹配的 bean 进行筛选，筛选时会考虑 @Qualifier 及泛型
结果个数为 0 抛出 NoSuchBeanDefinitionException 异常 
如果结果 &gt; 1，再根据 @Primary 进行筛选
如果结果仍 &gt; 1，再根据成员名或变量名进行筛选
结果仍 &gt; 1，抛出 NoUniqueBeanDefinitionException 异常

演示 - @Value 装配过程代码参考com.itheima.a46 包
收获💡
ContextAnnotationAutowireCandidateResolver 作用之一，获取 @Value 的值
了解 ${ } 对应的解析器
了解 #{ } 对应的解析器
TypeConvert 的一项体现

47) @Autowired 装配底层演示 - @Autowired 装配过程代码参考com.itheima.a47 包
收获💡
@Autowired 本质上是根据成员变量或方法参数的类型进行装配
如果待装配类型是 Optional，需要根据 Optional 泛型找到 bean，再封装为 Optional 对象装配
如果待装配的类型是 ObjectFactory，需要根据 ObjectFactory 泛型创建 ObjectFactory 对象装配
此方法可以延迟 bean 的获取


如果待装配的成员变量或方法参数上用 @Lazy 标注，会创建代理对象装配
此方法可以延迟真实 bean 的获取
被装配的代理不作为 bean


如果待装配类型是数组，需要获取数组元素类型，根据此类型找到多个 bean 进行装配
如果待装配类型是 Collection 或其子接口，需要获取 Collection 泛型，根据此类型找到多个 bean
如果待装配类型是 ApplicationContext 等特殊类型
会在 BeanFactory 的 resolvableDependencies 成员按类型查找装配
resolvableDependencies 是 map 集合，key 是特殊类型，value 是其对应对象
不能直接根据 key 进行查找，而是用 isAssignableFrom 逐一尝试右边类型是否可以被赋值给左边的 key 类型


如果待装配类型有泛型参数
需要利用 ContextAnnotationAutowireCandidateResolver 按泛型参数类型筛选


如果待装配类型有 @Qualifier
需要利用 ContextAnnotationAutowireCandidateResolver 按注解提供的 bean 名称筛选


有 @Primary 标注的 @Component 或 @Bean 的处理
与成员变量名或方法参数名同名 bean 的处理

48) 事件监听器演示 - 事件监听器代码参考com.itheima.a48 包
收获💡事件监听器的两种方式

实现 ApplicationListener 接口
根据接口泛型确定事件类型


@EventListener 标注监听方法
根据监听器方法参数确定事件类型
解析时机：在 SmartInitializingSingleton（所有单例初始化完成后），解析每个单例 bean



49) 事件发布器演示 - 事件发布器代码参考com.itheima.a49 包
收获💡事件发布器模拟实现

addApplicationListenerBean 负责收集容器中的监听器
监听器会统一转换为 GenericApplicationListener 对象，以支持判断事件类型


multicastEvent 遍历监听器集合，发布事件
发布前先通过 GenericApplicationListener.supportsEventType 判断支持该事件类型才发事件
可以利用线程池进行异步发事件优化


如果发送的事件对象不是 ApplicationEvent 类型，Spring 会把它包装为 PayloadApplicationEvent 并用泛型技术解析事件对象的原始类型
视频中未讲解



]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>vueBasic</title>
    <url>/2026/06/07/web/Vue%20Basic/</url>
    <content><![CDATA[
 

]]></content>
      <categories>
        <category>web</category>
      </categories>
      <tags>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>concurrency</title>
    <url>/2026/06/04/java/Concurrency/</url>
    <content><![CDATA[并发篇1. 线程状态要求

掌握 Java 线程六种状态
掌握 Java 线程状态转换
能理解五种状态与六种状态两种说法的区别

六种状态及转换
分别是

新建
当一个线程对象被创建，但还未调用 start 方法时处于新建状态
此时未与操作系统底层线程关联


可运行
调用了 start 方法，就会由新建进入可运行
此时与底层线程关联，由操作系统调度执行


终结
线程内代码已经执行完毕，由可运行进入终结
此时会取消与底层线程关联


阻塞
当获取锁失败后，由可运行进入 Monitor 的阻塞队列阻塞，此时不占用 cpu 时间
当持锁线程释放锁时，会按照一定规则唤醒阻塞队列中的阻塞线程，唤醒后的线程进入可运行状态


等待
当获取锁成功后，但由于条件不满足，调用了 wait() 方法，此时从可运行状态释放锁进入 Monitor 等待集合等待，同样不占用 cpu 时间
当其它持锁线程调用 notify() 或 notifyAll() 方法，会按照一定规则唤醒等待集合中的等待线程，恢复为可运行状态


有时限等待
当获取锁成功后，但由于条件不满足，调用了 wait(long) 方法，此时从可运行状态释放锁进入 Monitor 等待集合进行有时限等待，同样不占用 cpu 时间
当其它持锁线程调用 notify() 或 notifyAll() 方法，会按照一定规则唤醒等待集合中的有时限等待线程，恢复为可运行状态，并重新去竞争锁
如果等待超时，也会从有时限等待状态恢复为可运行状态，并重新去竞争锁
还有一种情况是调用 sleep(long) 方法也会从可运行状态进入有时限等待状态，但与 Monitor 无关，不需要主动唤醒，超时时间到自然恢复为可运行状态




其它情况（只需了解）

可以用 interrupt() 方法打断等待、有时限等待的线程，让它们恢复为可运行状态
park，unpark 等方法也可以让线程等待和唤醒


五种状态
五种状态的说法来自于操作系统层面的划分


运行态：分到 cpu 时间，能真正执行线程内代码的
就绪态：有资格分到 cpu 时间，但还未轮到它的
阻塞态：没资格分到 cpu 时间的
涵盖了 java 状态中提到的阻塞、等待、有时限等待
多出了阻塞 I&#x2F;O，指线程在调用阻塞 I&#x2F;O 时，实际活由 I&#x2F;O 设备完成，此时线程无事可做，只能干等


新建与终结态：与 java 中同名状态类似，不再啰嗦

2. 线程池要求

掌握线程池的 7 大核心参数

七大参数

corePoolSize 核心线程数目 - 池中会保留的最多线程数
maximumPoolSize 最大线程数目 - 核心线程+救急线程的最大数目
keepAliveTime 生存时间 - 救急线程的生存时间，生存时间内没有新任务，此线程资源会释放
unit 时间单位 - 救急线程的生存时间单位，如秒、毫秒等
workQueue - 当没有空闲核心线程时，新来任务会加入到此队列排队，队列满会创建救急线程执行任务
threadFactory 线程工厂 - 可以定制线程对象的创建，例如设置线程名字、是否是守护线程等
handler 拒绝策略 - 当所有线程都在繁忙，workQueue 也放满时，会触发拒绝策略
抛异常 java.util.concurrent.ThreadPoolExecutor.AbortPolicy
由调用者执行任务 java.util.concurrent.ThreadPoolExecutor.CallerRunsPolicy
丢弃任务 java.util.concurrent.ThreadPoolExecutor.DiscardPolicy
丢弃最早排队任务 java.util.concurrent.ThreadPoolExecutor.DiscardOldestPolicy





代码说明
day02.TestThreadPoolExecutor 以较为形象的方式演示了线程池的核心组成

3. wait vs sleep要求

能够说出二者区别

一个共同点，三个不同点
共同点

wait() ，wait(long) 和 sleep(long) 的效果都是让当前线程暂时放弃 CPU 的使用权，进入阻塞状态

不同点

方法归属不同

sleep(long) 是 Thread 的静态方法
而 wait()，wait(long) 都是 Object 的成员方法，每个对象都有


醒来时机不同

执行 sleep(long) 和 wait(long) 的线程都会在等待相应毫秒后醒来
wait(long) 和 wait() 还可以被 notify 唤醒，wait() 如果不唤醒就一直等下去
它们都可以被打断唤醒


锁特性不同（重点）

wait 方法的调用必须先获取 wait 对象的锁，而 sleep 则无此限制
wait 方法执行后会释放对象锁，允许其它线程获得该对象锁（我放弃 cpu，但你们还可以用）
而 sleep 如果在 synchronized 代码块中执行，并不会释放对象锁（我放弃 cpu，你们也用不了）



4. lock vs synchronized要求

掌握 lock 与 synchronized 的区别
理解 ReentrantLock 的公平、非公平锁
理解 ReentrantLock 中的条件变量

三个层面
不同点

语法层面
synchronized 是关键字，源码在 jvm 中，用 c++ 语言实现
Lock 是接口，源码由 jdk 提供，用 java 语言实现
使用 synchronized 时，退出同步代码块锁会自动释放，而使用 Lock 时，需要手动调用 unlock 方法释放锁


功能层面
二者均属于悲观锁、都具备基本的互斥、同步、锁重入功能
Lock 提供了许多 synchronized 不具备的功能，例如获取等待状态、公平锁、可打断、可超时、多条件变量
Lock 有适合不同场景的实现，如 ReentrantLock， ReentrantReadWriteLock


性能层面
在没有竞争时，synchronized 做了很多优化，如偏向锁、轻量级锁，性能不赖
在竞争激烈时，Lock 的实现通常会提供更好的性能



公平锁

公平锁的公平体现
已经处在阻塞队列中的线程（不考虑超时）始终都是公平的，先进先出
公平锁是指未处于阻塞队列中的线程来争抢锁，如果队列不为空，则老实到队尾等待
非公平锁是指未处于阻塞队列中的线程来争抢锁，与队列头唤醒的线程去竞争，谁抢到算谁的


公平锁会降低吞吐量，一般不用

条件变量

ReentrantLock 中的条件变量功能类似于普通 synchronized 的 wait，notify，用在当线程获得锁后，发现条件不满足时，临时等待的链表结构
与 synchronized 的等待集合不同之处在于，ReentrantLock 中的条件变量可以有多个，可以实现更精细的等待、唤醒控制


代码说明

day02.TestReentrantLock 用较为形象的方式演示 ReentrantLock 的内部结构


5. volatile要求

掌握线程安全要考虑的三个问题
掌握 volatile 能解决哪些问题

原子性

起因：多线程下，不同线程的指令发生了交错导致的共享变量的读写混乱
解决：用悲观锁或乐观锁解决，volatile 并不能解决原子性

可见性

起因：由于编译器优化、或缓存优化、或 CPU 指令重排序优化导致的对共享变量所做的修改另外的线程看不到
解决：用 volatile 修饰共享变量，能够防止编译器等优化发生，让一个线程对共享变量的修改对另一个线程可见

有序性

起因：由于编译器优化、或缓存优化、或 CPU 指令重排序优化导致指令的实际执行顺序与编写顺序不一致
解决：用 volatile 修饰共享变量会在读、写共享变量时加入不同的屏障，阻止其他读写操作越过屏障，从而达到阻止重排序的效果
注意：
volatile 变量写加的屏障是阻止上方其它写操作越过屏障排到 volatile 变量写之下
volatile 变量读加的屏障是阻止下方其它读操作越过屏障排到 volatile 变量读之上
volatile 读写加入的屏障只能防止同一线程内的指令重排




代码说明

day02.threadsafe.AddAndSubtract 演示原子性
day02.threadsafe.ForeverLoop 演示可见性
注意：本例经实践检验是编译器优化导致的可见性问题


day02.threadsafe.Reordering 演示有序性
需要打成 jar 包后测试


请同时参考视频讲解


6. 悲观锁 vs 乐观锁要求

掌握悲观锁和乐观锁的区别

对比悲观锁与乐观锁

悲观锁的代表是 synchronized 和 Lock 锁

其核心思想是【线程只有占有了锁，才能去操作共享变量，每次只有一个线程占锁成功，获取锁失败的线程，都得停下来等待】
线程从运行到阻塞、再从阻塞到唤醒，涉及线程上下文切换，如果频繁发生，影响性能
实际上，线程在获取 synchronized 和 Lock 锁时，如果锁已被占用，都会做几次重试操作，减少阻塞的机会


乐观锁的代表是 AtomicInteger，使用 cas 来保证原子性

其核心思想是【无需加锁，每次只有一个线程能成功修改共享变量，其它失败的线程不需要停止，不断重试直至成功】
由于线程一直运行，不需要阻塞，因此不涉及线程上下文切换
它需要多核 cpu 支持，且线程数不应超过 cpu 核数




代码说明

day02.SyncVsCas 演示了分别使用乐观锁和悲观锁解决原子赋值
请同时参考视频讲解


7. Hashtable vs ConcurrentHashMap要求

掌握 Hashtable 与 ConcurrentHashMap 的区别
掌握 ConcurrentHashMap 在不同版本的实现区别


更形象的演示，见资料中的 hash-demo.jar，运行需要 jdk14 以上环境，进入 jar 包目录，执行下面命令
java -jar --add-exports java.base/jdk.internal.misc=ALL-UNNAMED hash-demo.jar
Hashtable 对比 ConcurrentHashMap

Hashtable 与 ConcurrentHashMap 都是线程安全的 Map 集合
Hashtable 并发度低，整个 Hashtable 对应一把锁，同一时刻，只能有一个线程操作它
ConcurrentHashMap 并发度高，整个 ConcurrentHashMap 对应多把锁，只要线程访问的是不同锁，那么不会冲突

ConcurrentHashMap 1.7

数据结构：Segment(大数组) + HashEntry(小数组) + 链表，每个 Segment 对应一把锁，如果多个线程访问不同的 Segment，则不会冲突
并发度：Segment 数组大小即并发度，决定了同一时刻最多能有多少个线程并发访问。Segment 数组不能扩容，意味着并发度在 ConcurrentHashMap 创建时就固定了
索引计算
假设大数组长度是 $2^m$，key 在大数组内的索引是 key 的二次 hash 值的高 m 位
假设小数组长度是 $2^n$，key 在小数组内的索引是 key 的二次 hash 值的低 n 位


扩容：每个小数组的扩容相对独立，小数组在超过扩容因子时会触发扩容，每次扩容翻倍
Segment[0] 原型：首次创建其它小数组时，会以此原型为依据，数组长度，扩容因子都会以原型为准

ConcurrentHashMap 1.8

数据结构：Node 数组 + 链表或红黑树，数组的每个头节点作为锁，如果多个线程访问的头节点不同，则不会冲突。首次生成头节点时如果发生竞争，利用 cas 而非 syncronized，进一步提升性能
并发度：Node 数组有多大，并发度就有多大，与 1.7 不同，Node 数组可以扩容
扩容条件：Node 数组满 3&#x2F;4 时就会扩容
扩容单位：以链表为单位从后向前迁移链表，迁移完成的将旧数组头节点替换为 ForwardingNode
扩容时并发 get
根据是否为 ForwardingNode 来决定是在新数组查找还是在旧数组查找，不会阻塞
如果链表长度超过 1，则需要对节点进行复制（创建新节点），怕的是节点迁移后 next 指针改变
如果链表最后几个元素扩容后索引不变，则节点无需复制


扩容时并发 put
如果 put 的线程与扩容线程操作的链表是同一个，put 线程会阻塞
如果 put 的线程操作的链表还未迁移完成，即头节点不是 ForwardingNode，则可以并发执行
如果 put 的线程操作的链表已经迁移完成，即头结点是 ForwardingNode，则可以协助扩容


与 1.7 相比是懒惰初始化
capacity 代表预估的元素个数，capacity &#x2F; factory 来计算出初始数组大小，需要贴近 $2^n$ 
loadFactor 只在计算初始数组大小时被使用，之后扩容固定为 3&#x2F;4
超过树化阈值时的扩容问题，如果容量已经是 64，直接树化，否则在原来容量基础上做 3 轮扩容

8. ThreadLocal要求

掌握 ThreadLocal 的作用与原理
掌握 ThreadLocal 的内存释放时机

作用

ThreadLocal 可以实现【资源对象】的线程隔离，让每个线程各用各的【资源对象】，避免争用引发的线程安全问题
ThreadLocal 同时实现了线程内的资源共享

原理
每个线程内有一个 ThreadLocalMap 类型的成员变量，用来存储资源对象

调用 set 方法，就是以 ThreadLocal 自己作为 key，资源对象作为 value，放入当前线程的 ThreadLocalMap 集合中
调用 get 方法，就是以 ThreadLocal 自己作为 key，到当前线程中查找关联的资源值
调用 remove 方法，就是以 ThreadLocal 自己作为 key，移除当前线程关联的资源值

ThreadLocalMap 的一些特点

key 的 hash 值统一分配
初始容量 16，扩容因子 2&#x2F;3，扩容容量翻倍
key 索引冲突后用开放寻址法解决冲突

弱引用 key
ThreadLocalMap 中的 key 被设计为弱引用，原因如下

Thread 可能需要长时间运行（如线程池中的线程），如果 key 不再使用，需要在内存不足（GC）时释放其占用的内存

内存释放时机

被动 GC 释放 key
仅是让 key 的内存释放，关联 value 的内存并不会释放


懒惰被动释放 value
get key 时，发现是 null key，则释放其 value 内存
set key 时，会使用启发式扫描，清除临近的 null key 的 value 内存，启发次数与元素个数，是否发现 null key 有关


主动 remove 释放 key，value
会同时释放 key，value 的内存，也会清除临近的 null key 的 value 内存
推荐使用它，因为一般使用 ThreadLocal 时都把它作为静态变量（即强引用），因此无法被动依靠 GC 回收



]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2026/06/04/java/Hello%20World/</url>
    <content><![CDATA[hello,编程
 亲爱的“对象”   我能抽象出整个世界，但是我不能抽象出你，因为你在我心中是那么的具体，所以我的世界并不完整。


   我可以重载甚至覆盖这个世界里的任何一种方法，但是我却不能重载对你的思念，也许命中注定了，你在我的世界里永远的烙上了静态的属性.而我不慎调用了爱你这个方法，当我义无返顾的把自己作为参数传进这个方法时，我才发现爱上你是一个死循环……



  它不停的返回对你的思念压入我心里的堆栈，在这无尽的黑夜中，我的内存里已经再也装不下别人……



   我不停的向系统申请空间，但却捕获一个异常：我爱的人不爱我。


   为了解决这个异常，我愿意虚拟出最后一点内存，把所有我能实现的方法地址压入堆栈，并且在栈尾压入最后一个方法：将字符串“我爱你，你爱我吗？”传递给你……    如果返回值为真，我将用尽一生去爱你，否则，我将释放掉所有系统资源。

                                             编程的本质就是将现实的逻辑用代码实现！



 
]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>JVM</title>
    <url>/2026/06/04/java/JVM/</url>
    <content><![CDATA[虚拟机篇1. JVM 内存结构要求

掌握 JVM 内存结构划分
尤其要知道方法区、永久代、元空间的关系

结合一段 java 代码的执行理解内存划分


执行 javac 命令编译源代码为字节码
执行 java 命令
创建 JVM，调用类加载子系统加载 class，将类的信息存入方法区
创建 main 线程，使用的内存区域是 JVM 虚拟机栈，开始执行 main 方法代码
如果遇到了未见过的类，会继续触发类加载过程，同样会存入方法区
需要创建对象，会使用堆内存来存储对象
不再使用的对象，会由垃圾回收器在内存不足时回收其内存
调用方法时，方法内的局部变量、方法参数所使用的是  JVM 虚拟机栈中的栈帧内存
调用方法时，先要到方法区获得到该方法的字节码指令，由解释器将字节码指令解释为机器码执行
调用方法时，会将要执行的指令行号读到程序计数器，这样当发生了线程切换，恢复时就可以从中断的位置继续
对于非 java 实现的方法调用，使用内存称为本地方法栈（见说明）
对于热点方法调用，或者频繁的循环代码，由 JIT 即时编译器将这些代码编译成机器码缓存，提高执行性能



说明

加粗字体代表了 JVM 虚拟机组件
对于 Oracle 的 Hotspot 虚拟机实现，不区分虚拟机栈和本地方法栈

会发生内存溢出的区域

不会出现内存溢出的区域 – 程序计数器
出现 OutOfMemoryError 的情况
堆内存耗尽 – 对象越来越多，又一直在使用，不能被垃圾回收
方法区内存耗尽 – 加载的类越来越多，很多框架都会在运行期间动态产生新的类
虚拟机栈累积 – 每个线程最多会占用 1 M 内存，线程个数越来越多，而又长时间运行不销毁时


出现 StackOverflowError 的区域
JVM 虚拟机栈，原因有方法递归调用未正确结束、反序列化 json 时循环引用



方法区、永久代、元空间

方法区是 JVM 规范中定义的一块内存区域，用来存储类元数据、方法字节码、即时编译器需要的信息等
永久代是 Hotspot 虚拟机对 JVM 规范的实现（1.8 之前）
元空间是 Hotspot 虚拟机对 JVM 规范的另一种实现（1.8 以后），使用本地内存作为这些信息的存储空间


从这张图学到三点

当第一次用到某个类是，由类加载器将 class 文件的类元信息读入，并存储于元空间
X，Y 的类元信息是存储于元空间中，无法直接访问
可以用 X.class，Y.class 间接访问类元信息，它们俩属于 java 对象，我们的代码中可以使用


从这张图可以学到

堆内存中：当一个类加载器对象，这个类加载器对象加载的所有类对象，这些类对象对应的所有实例对象都没人引用时，GC 时就会对它们占用的对内存进行释放
元空间中：内存释放以类加载器为单位，当堆中类加载器内存释放时，对应的元空间中的类元信息也会释放

2. JVM 内存参数要求 

熟悉常见的 JVM 参数，尤其和大小相关的

堆内存，按大小设置

解释：

-Xms 最小堆内存（包括新生代和老年代）
-Xmx 最大对内存（包括新生代和老年代）
通常建议将 -Xms 与 -Xmx 设置为大小相等，即不需要保留内存，不需要从小到大增长，这样性能较好
-XX:NewSize 与 -XX:MaxNewSize 设置新生代的最小与最大值，但一般不建议设置，由 JVM 自己控制
-Xmn 设置新生代大小，相当于同时设置了 -XX:NewSize 与 -XX:MaxNewSize 并且取值相等
保留是指，一开始不会占用那么多内存，随着使用内存越来越多，会逐步使用这部分保留内存。下同

堆内存，按比例设置

解释：

-XX:NewRatio&#x3D;2:1 表示老年代占两份，新生代占一份
-XX:SurvivorRatio&#x3D;4:1 表示新生代分成六份，伊甸园占四份，from 和 to 各占一份

元空间内存设置

解释：

class space 存储类的基本信息，最大值受 -XX:CompressedClassSpaceSize 控制
non-class space 存储除类的基本信息以外的其它信息（如方法字节码、注解等）
class space 和 non-class space 总大小受 -XX:MaxMetaspaceSize 控制

注意：

这里 -XX:CompressedClassSpaceSize 这段空间还与是否开启了指针压缩有关，这里暂不深入展开，可以简单认为指针压缩默认开启

代码缓存内存设置

解释：

如果 -XX:ReservedCodeCacheSize &lt; 240m，所有优化机器代码不加区分存在一起
否则，分成三个区域（图中笔误 mthod 拼写错误，少一个 e）
non-nmethods - JVM 自己用的代码
profiled nmethods - 部分优化的机器码
non-profiled nmethods - 完全优化的机器码



线程内存设置


官方参考文档

https://docs.oracle.com/en/java/javase/11/tools/java.html#GUID-3B1CE181-CD30-4178-9602-230B800D4FAE


3. JVM 垃圾回收要求

掌握垃圾回收算法
掌握分代回收思想
理解三色标记及漏标处理
了解常见垃圾回收器

三种垃圾回收算法
标记清除法

解释：

找到 GC Root 对象，即那些一定不会被回收的对象，如正执行方法内局部变量引用的对象、静态变量引用的对象
标记阶段：沿着 GC Root 对象的引用链找，直接或间接引用到的对象加上标记
清除阶段：释放未加标记的对象占用的内存

要点：

标记速度与存活对象线性关系
清除速度与内存大小线性关系
缺点是会产生内存碎片

标记整理法

解释：

前面的标记阶段、清理阶段与标记清除法类似
多了一步整理的动作，将存活对象向一端移动，可以避免内存碎片产生

特点：

标记速度与存活对象线性关系

清除与整理速度与内存大小成线性关系

缺点是性能上较慢


标记复制法

解释：

将整个内存分成两个大小相等的区域，from 和 to，其中 to 总是处于空闲，from 存储新创建的对象
标记阶段与前面的算法类似
在找出存活对象后，会将它们从 from 复制到 to 区域，复制的过程中自然完成了碎片整理
复制完成后，交换 from 和 to 的位置即可

特点：

标记与复制速度与存活对象成线性关系
缺点是会占用成倍的空间

GC 与分代回收算法
GC 的目的在于实现无用对象内存自动释放，减少内存碎片、加快分配速度
GC 要点：

回收区域是堆内存，不包括虚拟机栈
判断无用对象，使用可达性分析算法，三色标记法标记存活对象，回收未标记对象
GC 具体的实现称为垃圾回收器
GC 大都采用了分代回收思想
理论依据是大部分对象朝生夕灭，用完立刻就可以回收，另有少部分对象会长时间存活，每次很难回收
根据这两类对象的特性将回收区域分为新生代和老年代，新生代采用标记复制法、老年代一般采用标记整理法


根据 GC 的规模可以分成 Minor GC，Mixed GC，Full GC

分代回收

伊甸园 eden，最初对象都分配到这里，与幸存区 survivor（分成 from 和 to）合称新生代，



当伊甸园内存不足，标记伊甸园与 from（现阶段没有）的存活对象



将存活对象采用复制算法复制到 to 中，复制完毕后，伊甸园和 from 内存都得到释放



将 from 和 to 交换位置



经过一段时间后伊甸园的内存又出现不足



标记伊甸园与 from（现阶段没有）的存活对象



将存活对象采用复制算法复制到 to 中



复制完毕后，伊甸园和 from 内存都得到释放



将 from 和 to 交换位置



老年代 old，当幸存区对象熬过几次回收（最多15次），晋升到老年代（幸存区内存不足或大对象会导致提前晋升）

GC 规模

Minor GC 发生在新生代的垃圾回收，暂停时间短

Mixed GC 新生代 + 老年代部分区域的垃圾回收，G1 收集器特有

Full GC 新生代 + 老年代完整垃圾回收，暂停时间长，应尽力避免


三色标记
即用三种颜色记录对象的标记状态

黑色 – 已标记
灰色 – 标记中
白色 – 还未标记


起始的三个对象还未处理完成，用灰色表示

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215016566.png” alt&#x3D;”image-20210831215016566” style&#x3D;”zoom:50%;” &#x2F;&gt;

该对象的引用已经处理完成，用黑色表示，黑色引用的对象变为灰色

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215033510.png” alt&#x3D;”image-20210831215033510” style&#x3D;”zoom:50%;” &#x2F;&gt;

依次类推

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215105280.png” alt&#x3D;”image-20210831215105280” style&#x3D;”zoom:50%;” &#x2F;&gt;

沿着引用链都标记了一遍

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215146276.png” alt&#x3D;”image-20210831215146276” style&#x3D;”zoom:50%;” &#x2F;&gt;

最后为标记的白色对象，即为垃圾

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215158311.png” alt&#x3D;”image-20210831215158311” style&#x3D;”zoom:50%;” &#x2F;&gt;
并发漏标问题
比较先进的垃圾回收器都支持并发标记，即在标记过程中，用户线程仍然能工作。但这样带来一个新的问题，如果用户线程修改了对象引用，那么就存在漏标问题。例如：

如图所示标记工作尚未完成

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215846876.png” alt&#x3D;”image-20210831215846876” style&#x3D;”zoom:50%;” &#x2F;&gt;

用户线程同时在工作，断开了第一层 3、4 两个对象之间的引用，这时对于正在处理 3 号对象的垃圾回收线程来讲，它会将 4 号对象当做是白色垃圾

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215904073.png” alt&#x3D;”image-20210831215904073” style&#x3D;”zoom:50%;” &#x2F;&gt;

但如果其他用户线程又建立了 2、4 两个对象的引用，这时因为 2 号对象是黑色已处理对象了，因此垃圾回收线程不会察觉到这个引用关系的变化，从而产生了漏标

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831215919493.png” alt&#x3D;”image-20210831215919493” style&#x3D;”zoom:50%;” &#x2F;&gt;

如果用户线程让黑色对象引用了一个新增对象，一样会存在漏标问题

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831220004062.png” alt&#x3D;”image-20210831220004062” style&#x3D;”zoom:50%;” &#x2F;&gt;
因此对于并发标记而言，必须解决漏标问题，也就是要记录标记过程中的变化。有两种解决方法：

Incremental Update 增量更新法，CMS 垃圾回收器采用
思路是拦截每次赋值动作，只要赋值发生，被赋值的对象就会被记录下来，在重新标记阶段再确认一遍


Snapshot At The Beginning，SATB 原始快照法，G1 垃圾回收器采用
思路也是拦截每次赋值动作，不过记录的对象不同，也需要在重新标记阶段对这些对象二次处理
新加对象会被记录
被删除引用关系的对象也被记录



垃圾回收器 - Parallel GC

eden 内存不足发生 Minor GC，采用标记复制算法，需要暂停用户线程

old 内存不足发生 Full GC，采用标记整理算法，需要暂停用户线程

注重吞吐量


垃圾回收器 - ConcurrentMarkSweep GC

它是工作在 old 老年代，支持并发标记的一款回收器，采用并发清除算法

并发标记时不需暂停用户线程
重新标记时仍需暂停用户线程


如果并发失败（即回收速度赶不上创建新对象速度），会触发 Full GC

注重响应时间


垃圾回收器 - G1 GC

响应时间与吞吐量兼顾
划分成多个区域，每个区域都可以充当 eden，survivor，old， humongous，其中 humongous 专为大对象准备
分成三个阶段：新生代回收、并发标记、混合收集
如果并发失败（即回收速度赶不上创建新对象速度），会触发 Full GC

G1 回收阶段 - 新生代回收

初始时，所有区域都处于空闲状态

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222639754.png” alt&#x3D;”image-20210831222639754” style&#x3D;”zoom:50%;” &#x2F;&gt;

创建了一些对象，挑出一些空闲区域作为伊甸园区存储这些对象

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222653802.png” alt&#x3D;”image-20210831222653802” style&#x3D;”zoom:50%;” &#x2F;&gt;

当伊甸园需要垃圾回收时，挑出一个空闲区域作为幸存区，用复制算法复制存活对象，需要暂停用户线程

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222705814.png” alt&#x3D;”image-20210831222705814” style&#x3D;”zoom:50%;” &#x2F;&gt;

复制完成，将之前的伊甸园内存释放

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222724999.png” alt&#x3D;”image-20210831222724999” style&#x3D;”zoom:50%;” &#x2F;&gt;

随着时间流逝，伊甸园的内存又有不足

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222737928.png” alt&#x3D;”image-20210831222737928” style&#x3D;”zoom:50%;” &#x2F;&gt;

将伊甸园以及之前幸存区中的存活对象，采用复制算法，复制到新的幸存区，其中较老对象晋升至老年代

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222752787.png” alt&#x3D;”image-20210831222752787” style&#x3D;”zoom:50%;” &#x2F;&gt;

释放伊甸园以及之前幸存区的内存

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222803281.png” alt&#x3D;”image-20210831222803281” style&#x3D;”zoom:50%;” &#x2F;&gt;
G1 回收阶段 - 并发标记与混合收集

当老年代占用内存超过阈值后，触发并发标记，这时无需暂停用户线程

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222813959.png” alt&#x3D;”image-20210831222813959” style&#x3D;”zoom:50%;” &#x2F;&gt;

并发标记之后，会有重新标记阶段解决漏标问题，此时需要暂停用户线程。这些都完成后就知道了老年代有哪些存活对象，随后进入混合收集阶段。此时不会对所有老年代区域进行回收，而是根据暂停时间目标优先回收价值高（存活对象少）的区域（这也是 Gabage First 名称的由来）。

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222828104.png” alt&#x3D;”image-20210831222828104” style&#x3D;”zoom:50%;” &#x2F;&gt;

混合收集阶段中，参与复制的有 eden、survivor、old，下图显示了伊甸园和幸存区的存活对象复制

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222841096.png” alt&#x3D;”image-20210831222841096” style&#x3D;”zoom:50%;” &#x2F;&gt;

下图显示了老年代和幸存区晋升的存活对象的复制

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222859760.png” alt&#x3D;”image-20210831222859760” style&#x3D;”zoom:50%;” &#x2F;&gt;

复制完成，内存得到释放。进入下一轮的新生代回收、并发标记、混合收集

&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210831222919182.png” alt&#x3D;”image-20210831222919182” style&#x3D;”zoom:50%;” &#x2F;&gt;
4. 内存溢出要求

能够说出几种典型的导致内存溢出的情况

典型情况

误用线程池导致的内存溢出
参考 day03.TestOomThreadPool


查询数据量太大导致的内存溢出
参考 day03.TestOomTooManyObject


动态生成类导致的内存溢出
参考 day03.TestOomTooManyClass



5. 类加载要求

掌握类加载阶段
掌握类加载器
理解双亲委派机制

类加载过程的三个阶段

加载

将类的字节码载入方法区，并创建类.class 对象

如果此类的父类没有加载，先加载父类

加载是懒惰执行



链接

验证 – 验证类是否符合 Class 规范，合法性、安全性检查
准备 – 为 static 变量分配空间，设置默认值
解析 – 将常量池的符号引用解析为直接引用


初始化

静态代码块、static 修饰的变量赋值、static final 修饰的引用类型变量赋值，会被合并成一个 &lt;cinit&gt; 方法，在初始化时被调用
static final 修饰的基本类型变量赋值，在链接阶段就已完成
初始化是懒惰执行




验证手段

使用 jps 查看进程号
使用 jhsdb 调试，执行命令 jhsdb.exe hsdb 打开它的图形界面
Class Browser 可以查看当前 jvm 中加载了哪些类
控制台的 universe 命令查看堆内存范围
控制台的 g1regiondetails 命令查看 region 详情
scanoops 起始地址 结束地址 对象类型 可以根据类型查找某个区间内的对象地址
控制台的 inspect 地址 指令能够查看这个地址对应的对象详情


使用 javap 命令可以查看 class 字节码



代码说明

day03.loader.TestLazy - 验证类的加载是懒惰的，用到时才触发类加载
day03.loader.TestFinal - 验证使用 final 修饰的变量不会触发类加载


jdk 8 的类加载器



名称
加载哪的类
说明



Bootstrap ClassLoader
JAVA_HOME&#x2F;jre&#x2F;lib
无法直接访问


Extension ClassLoader
JAVA_HOME&#x2F;jre&#x2F;lib&#x2F;ext
上级为 Bootstrap，显示为  null


Application ClassLoader
classpath
上级为 Extension


自定义类加载器
自定义
上级为 Application


双亲委派机制
所谓的双亲委派，就是指优先委派上级类加载器进行加载，如果上级类加载器

能找到这个类，由上级加载，加载后该类也对下级加载器可见
找不到这个类，则下级类加载器才有资格执行加载

双亲委派的目的有两点

让上级类加载器中的类对下级共享（反之不行），即能让你的类能依赖到 jdk 提供的核心类

让类的加载有优先次序，保证核心类优先加载


对双亲委派的误解
下面面试题的回答是错误的

错在哪了？

自己编写类加载器就能加载一个假冒的 java.lang.System 吗? 答案是不行。

假设你自己的类加载器用双亲委派，那么优先由启动类加载器加载真正的 java.lang.System，自然不会加载假冒的

假设你自己的类加载器不用双亲委派，那么你的类加载器加载假冒的 java.lang.System 时，它需要先加载父类 java.lang.Object，而你没有用委派，找不到 java.lang.Object 所以加载会失败

以上也仅仅是假设。事实上操作你就会发现，自定义类加载器加载以 java. 打头的类时，会抛安全异常，在 jdk9 以上版本这些特殊包名都与模块进行了绑定，更连编译都过不了



代码说明

day03.loader.TestJdk9ClassLoader - 演示类加载器与模块的绑定关系


6. 四种引用要求

掌握四种引用

强引用

普通变量赋值即为强引用，如 A a &#x3D; new A();

通过 GC Root 的引用链，如果强引用不到该对象，该对象才能被回收


&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210901111903574.png” alt&#x3D;”image-20210901111903574” style&#x3D;”zoom:80%;” &#x2F;&gt;
软引用（SoftReference）

例如：SoftReference a &#x3D; new SoftReference(new A());

如果仅有软引用该对象时，首次垃圾回收不会回收该对象，如果内存仍不足，再次回收时才会释放对象

软引用自身需要配合引用队列来释放

典型例子是反射数据


&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210901111957328.png” alt&#x3D;”image-20210901111957328” style&#x3D;”zoom:80%;” &#x2F;&gt;
弱引用（WeakReference）

例如：WeakReference a &#x3D; new WeakReference(new A());

如果仅有弱引用引用该对象时，只要发生垃圾回收，就会释放该对象

弱引用自身需要配合引用队列来释放

典型例子是 ThreadLocalMap 中的 Entry 对象


&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210901112107707.png” alt&#x3D;”image-20210901112107707” style&#x3D;”zoom:80%;” &#x2F;&gt;
虚引用（PhantomReference）

例如： PhantomReference a &#x3D; new PhantomReference(new A(), referenceQueue);

必须配合引用队列一起使用，当虚引用所引用的对象被回收时，由 Reference Handler 线程将虚引用对象入队，这样就可以知道哪些对象被回收，从而对它们关联的资源做进一步处理

典型例子是 Cleaner 释放 DirectByteBuffer 关联的直接内存


&lt;&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm src&#x3D;”&#x2F;assets&#x2F;img&#x2F;java&#x2F;jvm&#x2F;image-20210901112157901.png” alt&#x3D;”image-20210901112157901” style&#x3D;”zoom:80%;” &#x2F;&gt;

代码说明

day03.reference.TestPhantomReference - 演示虚引用的基本用法
day03.reference.TestWeakReference - 模拟 ThreadLocalMap, 采用引用队列释放 entry 内存


7. finalize要求

掌握 finalize 的工作原理与缺点

finalize

它是 Object 中的一个方法，如果子类重写它，垃圾回收时此方法会被调用，可以在其中进行资源释放和清理工作
将资源释放和清理放在 finalize 方法中非常不好，非常影响性能，严重时甚至会引起 OOM，从 Java9 开始就被标注为 @Deprecated，不建议被使用了

finalize 原理

对 finalize 方法进行处理的核心逻辑位于 java.lang.ref.Finalizer 类中，它包含了名为 unfinalized 的静态变量（双向链表结构），Finalizer 也可被视为另一种引用对象（地位与软、弱、虚相当，只是不对外，无法直接使用）
当重写了 finalize 方法的对象，在构造方法调用之时，JVM 都会将其包装成一个 Finalizer 对象，并加入 unfinalized 链表中



Finalizer 类中还有另一个重要的静态变量，即 ReferenceQueue 引用队列，刚开始它是空的。当狗对象可以被当作垃圾回收时，就会把这些狗对象对应的 Finalizer 对象加入此引用队列
但此时 Dog 对象还没法被立刻回收，因为 unfinalized -&gt; Finalizer 这一引用链还在引用它嘛，为的是【先别着急回收啊，等我调完 finalize 方法，再回收】
FinalizerThread 线程会从 ReferenceQueue 中逐一取出每个 Finalizer 对象，把它们从链表断开并真正调用 finallize 方法



由于整个 Finalizer 对象已经从 unfinalized 链表中断开，这样没谁能引用到它和狗对象，所以下次 gc 时就被回收了

finalize 缺点

无法保证资源释放：FinalizerThread 是守护线程，代码很有可能没来得及执行完，线程就结束了
无法判断是否发生错误：执行 finalize 方法时，会吞掉任意异常（Throwable）
内存释放不及时：重写了 finalize 方法的对象在第一次被 gc 时，并不能及时释放它占用的内存，因为要等着 FinalizerThread 调用完 finalize，把它从 unfinalized 队列移除后，第二次 gc 时才能真正释放内存
有的文章提到【Finalizer 线程会和我们的主线程进行竞争，不过由于它的优先级较低，获取到的CPU时间较少，因此它永远也赶不上主线程的步伐】这个显然是错误的，FinalizerThread 的优先级较普通线程更高，原因应该是 finalize 串行执行慢等原因综合导致


代码说明

day03.reference.TestFinalize - finalize 的测试代码


]]></content>
      <categories>
        <category>java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>HBase 理论篇</title>
    <url>/2026/06/06/bigdata/hbase/Hbase/</url>
    <content><![CDATA[HBase 分布式列式数据库一、HBase 基础介绍1. 核心定义HBase 是基于Hadoop的KV格式、面向列存储的分布式数据库，非内存数据库、非传统列式数据库。
2. 五大核心特点
高可靠性
可伸缩水平扩容
面向列（列族）存储
支持海量数据实时读写
高性能高并发吞吐

3. 数据规模适配
行：十亿级别海量行
列：百万级别稀疏列

4. 适用业务场景
存储非结构化&#x2F;半结构化稀疏数据
高并发客户端读写业务
低延迟、实时性要求高的海量数据业务




二、HBase 集群架构
  


1. 集群角色分工ZooKeeper 协调组件
存储元数据表hbase:meta位置入口
保存Table表元数据
实时监控RegionServer上下线，通知Master处理故障

Client 客户端
提交HBase读写请求
本地缓存元数据，减少ZK频繁交互

Master（HMaster）
管理集群全部RegionServer节点
分配Region到对应RegionServer，维护负载均衡
负责Region拆分、迁移、故障重分配
集群高可用：同一时间仅一台Active Master，其余Standby备用

RegionServer（数据节点）负责承载客户端读写请求，托管所有Region分片，是真正存储数据的节点。
Region 分片概念
建表初期整张表仅1个Region，写入数据后自动扩容拆分
Region数据量达到阈值，自动切分为两个新Region

Store 存储单元（对应单个列族）
MemStore：内存有序缓冲区
StoreFile（HFile）：落地磁盘持久化文件
MemStore + StoreFile 共同组成LSM树存储结构

HLog（WAL预写日志）宕机恢复的日志文件，所有写入操作先落WAL再写入内存。
2. HBase 数据模型示意HBase表数据逻辑模型示意图
  


HBase表数据存储模型示意图
  


核心字段概念
RowKey（行键）
唯一标识单行数据，等价MySQL主键
全局字典序排序，分片、查询核心依据
最大长度64K，工程建议10~100字节


Column Family（列族）
一组列的集合，建表时提前定义Schema
列族为物理存储隔离单元，权限控制粒度


Qualifier（列限定符）
列族下具体字段，无需提前定义，插入时动态新增


Timestamp（时间戳）
版本号，区分单元格多版本数据
默认系统自动生成，支持自定义（不推荐）


Cell（单元格）
最小存储单元，坐标 RowKey + 列族:列 + 时间戳
底层纯字节KV存储，无固定数据类型
自动保留多版本历史数据




三、HBase 读写流程1. 读流程（Get&#x2F;Scan）
  



Client 请求ZooKeeper，查询hbase:meta，定位RowKey所属RegionServer
访问目标RegionServer，读取表元数据，找到对应Store
优先读取BlockCache读缓存；缓存未命中读取MemStore内存最新数据
内存无数据则遍历磁盘StoreFile，读取后缓存至BlockCache
合并多版本数据，过滤过期删除标记，返回结果给客户端

2. 写流程（Put）
  


  



Client请求ZK获取元数据，定位目标RegionServer
写入WAL预写日志，保障宕机数据不丢失
写入对应列族MemStore内存缓冲区
MemStore达到阈值（默认64M）触发Flush刷盘，生成StoreFile写入HDFS
后台自动Compaction合并大量小StoreFile，优化读查询效率


四、底层存储结构
WAL（HLog）预写日志
写入操作先持久化日志再写入内存
LogAsync异步落盘，定期滚动生成新日志文件


MemStore：内存有序缓冲，LSM树内存层
StoreFile（HFile）：磁盘持久化有序KV文件，LSM树磁盘层


MemStore + StoreFile 共同构成LSM树，将随机写转换为顺序写，大幅提升写入吞吐量


五、HBase API1. 管理API（HBaseAdmin）
createTable：创建表
disableTable：禁用表
deleteTable：删除表

2. 数据读写API（HTable）
get：单行查询
put：单行写入
scan：范围批量扫描
delete：删除数据

3. 过滤器 Filter
SingleColumnValueFilter：单列值过滤
PrefixFilter：RowKey前缀过滤
FilterList：多过滤器组合
专用比较过滤器、附加过滤器


六、HBase Shell 常用命令状态与版本
status：查看集群状态
whoami：查看当前登录用户
version：查看HBase版本

表操作
create：建表
drop：删除表
disable&#x2F;enable：禁用&#x2F;启用表

数据操作
put：写入数据
get：单行查询
scan：全表&#x2F;范围扫描
delete：删除单元格

运维操作
list：查看所有表
flush：手动刷写MemStore到磁盘
split：手动拆分Region


七、HBase MapReduce 集成
MR读取HBase：从HBase表提取数据作为Map输入
MR写入HBase：将MR计算结果落地写入HBase
两类核心任务：
TableMapReduceUtil.initTableMapJob（仅读表）
TableMapReduceUtil.initTableReduceJob（读写表）




八、表设计核心规范
预分区：提前拆分Region，规避写入热点
RowKey设计五大原则
长度越短越好
加盐Hash打散热点
前缀时间戳方便范围查询
禁止过长RowKey


列族数量控制：1~2个为宜，不建议过多列族
SetMaxVersion：设置单元格最大保留版本数
SetTTL：设置数据过期自动删除时间
inMemory：开启内存优先存储，提升热点查询速度


九、合并与拆分机制Compaction 文件合并
minor 小合并：3~10个小文件自动合并
major 大合并：该Region全部StoreFile合并为单文件，建议关闭自动合并，手动定时执行

Split Region拆分单个Region数据量超限，自动切分为两个Region，分散到不同RegionServer均衡压力

十、HBase 读写优化写入优化
多客户端并发创建独立HTable对象
客户端属性配置
关闭autoAutoFlush批量提交
设置客户端写缓冲区大小
WAL Flag可临时关闭（数据丢失风险，不推荐）


多线程批量写入

读取优化
Scan扫描参数优化
设置批量读取行数
关闭缓存Scanner
指定查询列族，禁止全列读取


多线程并发读取
BlockCache调优
所有RegionServer共享BlockCache
默认占用堆内存0.2，读多业务可适当调大
LRU淘汰策略，优先淘汰冷数据




十一、HBase 索引与ES整合整合步骤
程序同步HBase数据至Elasticsearch
ES构建二级索引，索引存储RowKey
客户端查询先检索ES，获取匹配RowKey列表
根据RowKey回查HBase获取完整业务数据


十二、HBase 优缺点优点
海量实时随机读写：十亿级数据毫秒级单行查询，适配在线业务
无限水平扩容：RegionServer、DataNode在线扩容，无容量上限
稀疏列式存储：空单元格不占用磁盘，支持百万动态列
高可靠容错：底层HDFS三副本，WAL保障宕机数据不丢失
多版本数据自动保存，支持历史数据回溯
LSM树消除随机IO，支撑百万级高并发写入

缺点
无多表JOIN、跨行事务，仅单行操作原子性
大范围Scan扫描性能差，不适合全量离线分析
Compaction合并会占用大量CPU、磁盘IO，写入高峰期压力大
RowKey设计门槛高，设计不合理极易出现Region热点
不支持复杂SQL统计分析，离线计算优先Hive&#x2F;Spark


十三、HBase &#x2F; Hive &#x2F; HDFS 核心区别
HDFS：底层分布式文件存储，仅支持文件追加，无随机读写能力
Hive：离线数据仓库，类SQL批处理引擎，高延迟，用于离线统计分析
HBase：分布式NoSQL在线数据库，低延迟随机读写，面向实时明细业务存储


十四、集群核心角色总结1. Client客户端本地缓存Region元数据，减少ZK重复查询；直连RegionServer读写，不经过HMaster；提供Java、Shell等多套操作API
2. ZooKeeper集群协调中心，不存储业务数据；负责Master选举、元数据表寻址、节点存活监控，所有客户端与服务端均依赖ZK寻址。
3. HMaster集群管理主节点，负责表创建删除、Region调度、故障转移；依赖ZK实现多主高可用，不处理真实读写请求。
4. RegionServer数据承载节点，所有Put&#x2F;Get&#x2F;Scan请求全部由该节点处理；内部包含WAL、MemStore、BlockCache、Store(HFile)；定时向ZK上报心跳，宕机后自动迁移Region。
]]></content>
      <categories>
        <category>bigdata</category>
      </categories>
      <tags>
        <tag>大数据</tag>
      </tags>
  </entry>
  <entry>
    <title>窗口</title>
    <url>/2026/06/03/story/Window/</url>
    <content><![CDATA[窗口
在一家医院的病房里，曾住过两位病人，他们的病情都很严重。这间病房十分窄小，仅能容下他们俩人。病房有一扇门和一个窗户，门通向走廊，透过窗户可以看到外面的世界。


其中一位病人经允许，可以分别在每天上午和下午起身坐上一个小时。这位病人的病床靠近窗口。而另一位病人则不得不日夜躺在床上。当然，两位病人都需要静养治疗。使他们感到痛苦的是，两人的病情不允许他们做任何事情借以消遣，只有静静的躺着。而且只有他们两个人。两人经常谈天，一谈就是几个小时。他们谈起各自的家庭，各自的工作，还有各自的生活。。。等等。


每天上午和下午，时间一到，靠近窗的病人就被扶起身来，开始一小时的仰坐。每当这时，他就开始为同伴描述起他所见到的窗外的一切。渐渐地，每天的这两个小时，几乎就成了他和同伴生活中的全部内容了。


很显然，这个窗户俯视着一座公园，公园里面有一泓湖水，湖面上照例漫游着一群群野鸭、天鹅。公园里的孩子们有的在扔面包喂这些水禽，有的在摆弄游艇模型。一对对年轻的情侣手挽着手在树阴下散步。公园里鲜花盛开，主要有玫瑰花，但四周还有五彩斑斓、争相斗艳的花草。在公园的一角，有一块网球场，有时那儿进行的比赛确实精彩，虽然球艺够不上正式决赛的水平，但有的看总比没有强。公园的尽头是一排商店，在这些商店的后边闹市区隐约可见。


躺着的病人津津有味地听这一切。这个时刻的每一分钟对他来说都是一种享受。 描述仍在继续：一个孩童怎样差一点跌到湖中，身着夏装的姑娘是多么美丽、动人。接着又是一场扣人心弦的网球赛。他听着这栩栩如生的描述，仿佛亲眼看到了窗外所发生的一切。一天下午，当他听到靠窗的病人说到一名球员正慢悠悠地把球击得四处皆是时，不靠窗的病人突然产生了一个想法：为什么偏是他有幸能观赏到窗外的一切？为什么自己不应得到这种机会呢？他为自己会有这种想法而感到惭愧。可是，他愈加克制，这种想法却变得愈加强烈，直至几天以后，这个想法已经进一步变为紧挨着窗口的为什么不该是我呢？


他在白天时刻为这一想法所困扰，晚上，又彻夜难眠。结果，病情一天天加重了，医生们对其病因不得所知。一天晚上，他照例睁着双眼盯着天花板，这时，他的同伴突然醒来，开始大声咳嗽，呼吸急促，时断时续，液体已经充塞了他的肺腔，他两手摸索着，在找电铃的按钮，只要电铃一响，值班的护士就立即赶来。


但是，另一位病人却纹丝不动地看着。心想，他凭什么要占据窗口那张床位呢？


痛苦的咳嗽声打破了黑夜的沉静。一声又一声···卡住了···停止了···直至最后呼吸声也停止了。


另一位病人仍然继续盯着天花板。


第二天早晨，医护人员送来了漱洗水，发现那个病人早已咽气了，他们静悄悄地将尸体抬了出去，丝毫没有大惊小怪。稍过几天，似乎这时开口已经正当得体。剩下的这位病人就立刻提出是否能让他挪到窗口的那张床上去。医护人员把他抬了过去，将他舒舒服服地安顿在那张病床上。接着他们离开了病房，剩下他一个静静地躺在那儿。


医生刚一离开，这位病人就十分痛苦地挣扎着，用一支胳膊支起了身子，口中气喘吁吁。他探头朝窗口望去。他看到的只是光秃秃的一堵墙，什么也没有。。。。

]]></content>
      <categories>
        <category>essay</category>
      </categories>
      <tags>
        <tag>随笔杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>Hadoop 理论篇</title>
    <url>/2026/06/06/bigdata/hdfs/Hadoop/</url>
    <content><![CDATA[Hadoop HDFS 架构一、HDFS 整体架构模型HDFS 数据分为两类：文件元数据 MetaData、文件实际数据

主节点 NameNode：统一保存文件元数据，单节点部署
从节点 DataNode：分布式存储文件 Block 数据，集群多节点
通信机制
DataNode 与 NameNode 维持心跳，定时上报自身 Block 列表
HdfsClient ↔ NameNode：交互元数据信息（文件名、权限、块位置等）
HdfsClient ↔ DataNode：传输文件真实 Block 数据



二、NameNode（NN 名称节点）1. 存储特性
元数据基于内存存储，不与磁盘交换，读写速度快
内存元数据提供实时查询，同时磁盘做持久化保存

2. 核心功能
接收客户端读写请求服务
收集所有 DataNode 定时上报的 Block 块列表信息

3. 内存保存的 Metadata 元数据内容
文件归属者（owner）、访问权限 permissions
文件大小、创建&#x2F;修改时间
文件对应的 Block 列表、块偏移量
每个 Block 的所有副本位置（由 DataNode 心跳上报）

4. NameNode 元数据持久化机制
NameNode 启动时会将全部元数据加载至内存
fsimage：磁盘持久化镜像文件，保存完整文件目录结构、权限、文件大小等静态元数据
不存储 Block 块位置信息（块位置仅存于内存，由DN实时上报）


edits：编辑日志，记录所有增删改元数据的操作流水
NN 重启流程：合并 fsimage + edits，还原最新完整元数据至内存

5. NameNode 安全模式
NN 启动流程：加载 fsimage 到内存 → 回放 edits 日志构建完整元数据 → 生成新 fsimage + 空 edits
启动后自动进入安全模式：文件系统只读
允许：查看目录、读取文件内容
禁止：写、删除、重命名文件


安全模式退出条件
DataNode 上报本机所有 Block 信息
集群内达到配置比例的数据块副本数满足最小副本要求
等待指定延时后自动退出安全模式


副本修复：检测到副本数不足的 Block，NN 自动调度复制至其他DN，补齐副本数

三、DataNode（DN 数据节点）
存储介质：本地磁盘目录，以文件形式存储 Block 数据，同时保存对应Block简易元数据文件
启动行为：DN 启动后主动向 NN 上报本机全部 Block 信息
心跳机制
默认3秒一次心跳，持续和 NN 保持通信
NN 超过10分钟未收到DN心跳，判定该DN宕机丢失
自动复制该DN上所有Block副本至其他存活DN，保证副本数量



四、SecondaryNameNode（SNN 第二名称节点）
定位：不是 NameNode 高可用备份节点，核心作用是辅助NN合并 edits 日志，缩短NN重启加载元数据耗时；仅可手动作为临时备份使用
触发合并 Checkpoint 时机（满足其一即执行）
时间阈值：配置 fs.checkpoint.period，默认3600秒（1小时）
日志大小阈值：配置 fs.checkpoint.size，edits 文件最大64MB



五、Block 块副本放置策略（默认3副本）
第1副本：
客户端本集群上传：放置当前上传文件的DN
集群外部提交文件：随机挑选磁盘、CPU负载较低节点


第2副本：放置与第一副本不同机架的节点
第3副本：放置与第二副本同一机架的其他节点
超过3个副本：随机分配集群任意节点

六、HDFS 读写流程1. HDFS 写流程（Client 上传文件）

  




Client 先和 NameNode 通信，传递元数据请求，获取第一个Block对应的3个副本DN节点列表（按副本策略分配）
建立 Pipeline 流水线传输
Client 与第一个DN建立Socket通道；第一个DN与第二个DN建立Socket，依次串联所有副本节点


分片传输：数据切分为4K小包
Client发送小包给首DN；DN本地缓存同时同步向下游DN转发


单个Block传输完成后，对应DN向NN上报新增Block信息
Client 向NN申请下一个Block的节点存储信息，循环直至全部数据上传完成
全部Block上传完毕，NN更新元数据，标记文件状态为可用

2. HDFS 读流程（Client 读取文件）
Client 向 NN 请求文件对应部分Block的副本位置列表
就近优选规则：优先读取同节点、同机架DN的数据，减少网络开销
线性逐个读取Block数据，最终在客户端合并为完整文件
NN 不会一次性返回全部Block位置，分段分批下发

七、HDFS 文件权限模型
遵循 POSIX 权限标准，逻辑和 Linux 文件权限一致
r：读权限；w：写权限；x：执行权限
文件的x权限无作用；文件夹x权限代表能否访问文件夹内部内容


文件归属规则：Linux 系统登录用户执行hadoop命令创建文件，文件owner为该登录用户
权限设计定位：仅防止误操作，不做安全认证；HDFS默认信任客户端上报身份

八、HDFS 优缺点优点
高容错性
数据自动多副本存储
节点&#x2F;磁盘故障后，副本自动恢复复制


适配大数据批处理
计算向数据移动，减少大量数据传输
对外暴露Block偏移量，供计算框架优化调度


支持超大尺度数据
支持GB&#x2F;TB&#x2F;PB级文件
可存储百万级文件
集群支持上万节点（10K+）


低成本集群
可搭建在普通廉价服务器
多副本机制保障数据可靠，内置容错恢复机制



缺点
不适合低延迟毫秒级实时数据访问，高吞吐牺牲低延迟
大量小文件场景性能极差
每个文件元数据占用NN内存，海量小文件耗尽NN内存
磁盘寻道耗时大于文件读取耗时，效率极低


文件修改限制严格
同一文件仅支持单一写入者，不支持多并发写入
仅支持文件追加append，不支持随机修改、随机覆盖



九、集群三大核心角色（进程）1. NameNode
负责管理全集群文件元数据
元数据常驻内存，无磁盘交换，磁盘通过 fsimage + edits 持久化
磁盘不持久化Block位置信息，块位置由DN心跳实时上报
Block有固定偏移量，Block大小固定不可调整，HDFS不支持文件随机修改

2. DataNode
存储真实Block数据块，数据落地本地磁盘
默认块大小128M，统一固定尺寸不可调整，配套环形缓冲区100M
支持自定义副本数量，依靠多副本实现高可用、容错
实现“计算向数据移动”的底层数据存储载体

3. SecondaryNameNode
辅助合并edits日志，做Checkpoint，减轻NN压力

NN与DN交互核心机制
心跳机制定时通信
DN定期向NN上报本机所有Block清单
NN启动存在安全模式机制

十、Client 客户端整体作用写操作逻辑
分块线性上传Block
先与NN交互获取元数据、Block存储节点列表
通过Pipeline流水线串行向多个DN同步副本数据
小包异步转发，利用时间重叠优化网络IO耗时
单块传输完成后申请下一块存储节点，全部完成后NN更新元数据

读操作逻辑
线性依次读取各个Block，单网卡串行读取
依据网络距离优选最近DN读取数据
NN分段返回Block位置信息，不会一次性下发全部块信息

]]></content>
      <categories>
        <category>bigdata</category>
      </categories>
      <tags>
        <tag>大数据</tag>
      </tags>
  </entry>
  <entry>
    <title>HBase Mind map</title>
    <url>/2026/06/06/bigdata/hbase/HBaseMind/</url>
    <content><![CDATA[HBase 思维导图
  
]]></content>
      <categories>
        <category>bigdata</category>
      </categories>
      <tags>
        <tag>大数据</tag>
      </tags>
  </entry>
  <entry>
    <title>人可怕的不是死亡,而是从未真正活过</title>
    <url>/2026/06/02/fun/movie/NeverTrulyLived/</url>
    <content><![CDATA[
Never Truly Lived

(function(){var player = new DPlayer({"container":document.getElementById("dplayer0"),"video":{"url":"/assets/video/NeverTruly.mp4"}});window.dplayers||(window.dplayers=[]);window.dplayers.push(player);})()]]></content>
      <categories>
        <category>movie</category>
      </categories>
      <tags>
        <tag>电影</tag>
      </tags>
  </entry>
  <entry>
    <title>与你同在</title>
    <url>/2026/06/01/fun/music/BeWithYou/</url>
    <content><![CDATA[
Guitar Be with you


  
  
]]></content>
      <categories>
        <category>music</category>
      </categories>
      <tags>
        <tag>音乐</tag>
      </tags>
  </entry>
  <entry>
    <title>传奇世界回忆录</title>
    <url>/2026/06/03/fun/game/index/</url>
    <content><![CDATA[最爱GAME-网络游戏-传奇世界-回忆录
  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  


  

]]></content>
      <categories>
        <category>game</category>
      </categories>
      <tags>
        <tag>游戏</tag>
      </tags>
  </entry>
  <entry>
    <title>自学吉他:真的爱你</title>
    <url>/2026/06/01/fun/music/TrulyLoveYou/</url>
    <content><![CDATA[
自学吉他: Truly love you 


  
  
]]></content>
      <categories>
        <category>music</category>
      </categories>
      <tags>
        <tag>音乐</tag>
      </tags>
  </entry>
  <entry>
    <title>光辉岁月</title>
    <url>/2026/06/01/fun/music/GloriousYears/</url>
    <content><![CDATA[
Glorious Years


  
  
]]></content>
      <categories>
        <category>music</category>
      </categories>
      <tags>
        <tag>音乐</tag>
      </tags>
  </entry>
  <entry>
    <title>小猪妖的夏天</title>
    <url>/2026/06/02/fun/movie/SummerOfPig/</url>
    <content><![CDATA[
小猪妖的夏天


  
  
]]></content>
      <categories>
        <category>movie</category>
      </categories>
      <tags>
        <tag>电影</tag>
      </tags>
  </entry>
  <entry>
    <title>自学吉他:天使的翅膀</title>
    <url>/2026/06/01/fun/music/WingsofAngels/</url>
    <content><![CDATA[
自学吉他: Wings of Angels 


  
  
]]></content>
      <categories>
        <category>music</category>
      </categories>
      <tags>
        <tag>音乐</tag>
      </tags>
  </entry>
  <entry>
    <title>努力的理由</title>
    <url>/2026/06/03/fun/photo/LearnMandarin/</url>
    <content><![CDATA[漫画-努力的理由
  


  


  


  


  


  


  
]]></content>
      <categories>
        <category>photo</category>
      </categories>
      <tags>
        <tag>图片</tag>
      </tags>
  </entry>
  <entry>
    <title>学普通话</title>
    <url>/2026/06/03/fun/photo/ReasonsForWorkingHard/</url>
    <content><![CDATA[漫画-学普通话严须
  


麻雀
  


活辣子
  


赵鸡儿
  


缺块儿
  
]]></content>
      <categories>
        <category>photo</category>
      </categories>
      <tags>
        <tag>图片</tag>
      </tags>
  </entry>
  <entry>
    <title>Mysql面试题</title>
    <url>/2026/06/05/database/Mysql%20Interview/</url>
    <content><![CDATA[Mysql面试题[toc]
一 Mysql索引001 Mysql如何实现的索引机制？MySQL中索引分三类：B+树索引、Hash索引、全文索引
002 InnoDB索引与MyISAM索引实现的区别是什么？
MyISAM的索引方式都是非聚簇的，与InnoDB包含1个聚簇索引是不同的。

在InnoDB存储引擎中，我们只需要根据主键值对聚簇索引进行一次查找就能找到对应的记录，而在MyISAM中却需要进行一次回表操作，意味着MyISAM中建立的索引相当于全部都是二级索引 。 

InnoDB的数据文件本身就是索引文件，而MyISAM索引文件和数据文件是分离的 ，索引文件仅保存数据记录的地址。

MyISAM的表在磁盘上存储在以下文件中：  *.sdi（描述表结构）、*.MYD（数据），*.MYI（索引）
InnoDB的表在磁盘上存储在以下文件中： .ibd（表结构、索引和数据都存在一起）


InnoDB的非聚簇索引data域存储相应记录主键的值 ，而MyISAM索引记录的是地址 。换句话说，InnoDB的所有非聚簇索引都引用主键作为data域。

MyISAM的回表操作是十分快速的，因为是拿着地址偏移量直接到文件中取数据的，反观InnoDB是通过获取主键之后再去聚簇索引里找记录，虽然说也不慢，但还是比不上直接用地址去访问。

InnoDB要求表必须有主键 （ MyISAM可以没有 ）。如果没有显式指定，则MySQL系统会自动选择一个可以非空且唯一标识数据记录的列作为主键。如果不存在这种列，则MySQL自动为InnoDB表生成一个隐含字段作为主键，这个字段长度为6个字节，类型为长整型。





003 一个表中如果没有创建索引，那么还会创建B+树吗？会

如果有主键会创建聚簇索引

如果没有主键会生成rowid作为隐式主键


004 说一下B+树索引实现原理（数据结构）讲义假设有一个表index_demo，表中有2个INT类型的列，1个CHAR(1)类型的列，c1列为主键：
CREATE TABLE index_demo(c1 INT,c2 INT,c3 CHAR(1),PRIMARY KEY(c1)) ;



index_demo表的简化的行格式示意图如下：

我们只在示意图里展示记录的这几个部分：

record_type：表示记录的类型， 0是普通记录、 2是最小记录、 3 是最大记录、1是B+树非叶子节点记录。
next_record：表示下一条记录的相对位置，我们用箭头来表明下一条记录。
各个列的值：这里只记录在 index_demo 表中的三个列，分别是 c1 、 c2 和 c3 。 
其他信息：除了上述3种信息以外的所有信息，包括其他隐藏列的值以及记录的额外信息。

将其他信息项暂时去掉并把它竖起来的效果就是这样：

把一些记录放到页里的示意图就是（这里一页就是一个磁盘块，代表一次IO）：

name age sex
MySQL InnoDB的默认的页大小是16KB，因此数据存储在磁盘中，可能会占用多个数据页。如果各个页中的记录没有规律，我们就不得不依次遍历所有的数据页。如果我们想快速的定位到需要查找的记录在哪些数据页中，我们可以这样做 ：

下一个数据页中用户记录的主键值必须大于上一个页中用户记录的主键值
给所有的页建立目录项


以页28为例，它对应目录项2 ，这个目录项中包含着该页的页号28以及该页中用户记录的最小主键值 5。我们只需要把几个目录项在物理存储器上连续存储（比如：数组），就可以实现根据主键值快速查找某条记录的功能了。比如：查找主键值为 20 的记录，具体查找过程分两步：

先从目录项中根据二分法快速确定出主键值为20的记录在目录项3中（因为 12 ≤ 20 &lt; 209 ），对应页9。 
再到页9中根据二分法快速定位到主键值为 20 的用户记录。

至此，针对数据页做的简易目录就搞定了。这个目录有一个别名，称为索引 。 
InnoDB中的索引方案我们新分配一个编号为30的页来专门存储目录项记录，页10、28、9、20专门存储用户记录： 


目录项记录和普通的用户记录的不同点： 

目录项记录 的 record_type 值是1，而 普通用户记录 的 record_type 值是0。
目录项记录只有主键值和页的编号两个列，而普通的用户记录的列是用户自己定义的，包含很多列，另外还有InnoDB自己添加的隐藏列。

现在查找主键值为 20 的记录，具体查找过程分两步：

先到页30中通过二分法快速定位到对应目录项，因为 12 ≤ 20 &lt; 209 ，就是页9。 
再到页9中根据二分法快速定位到主键值为 20 的用户记录。

更复杂的情况如下：
我们生成了一个存储更高级目录项的 页33 ，这个页中的两条记录分别代表页30和页32，如果用户记录的主键值在 [1, 320) 之间，则到页30中查找更详细的目录项记录，如果主键值 不小于320 的话，就到页32中查找更详细的目录项记录。这个数据结构，它的名称是 B+树 。 

005	聚簇索引与非聚簇索引b+树实现有什么区别？聚簇索引特点：

索引和数据保存在同一个B+树中

页内的记录是按照主键的大小顺序排成一个单向链表 。

页和页之间也是根据页中记录的主键的大小顺序排成一个双向链表 。

非叶子节点存储的是记录的主键+页号。

叶子节点存储的是完整的用户记录。


优点：

数据访问更快 ，因为索引和数据保存在同一个B+树中，因此从聚簇索引中获取数据比非聚簇索引更快。
聚簇索引对于主键的排序查找和范围查找速度非常快。
按照聚簇索引排列顺序，查询显示一定范围数据的时候，由于数据都是紧密相连，数据库可以从更少的数据块中提取数据，节省了大量的IO操作 。

缺点：

插入速度严重依赖于插入顺序 ，按照主键的顺序插入是最快的方式，否则将会出现页分裂，严重影响性能。因此，对于InnoDB表，我们一般都会定义一个自增的ID列为主键。
更新主键的代价很高 ，因为将会导致被更新的行移动。因此，对于InnoDB表，我们一般定义主键为不可更新。

限制：

只有InnoDB引擎支持聚簇索引，MyISAM不支持聚簇索引。
由于数据的物理存储排序方式只能有一种，所以每个MySQL的表只能有一个聚簇索引。
如果没有为表定义主键，InnoDB会选择非空的唯一索引列代替。如果没有这样的列，InnoDB会隐式的定义一个主键作为聚簇索引。
为了充分利用聚簇索引的聚簇特性，InnoDB中表的主键应选择有序的id，不建议使用无序的id，比如UUID、MD5、HASH、字符串作为主键，无法保证数据的顺序增长。

非聚簇索引（二级索引、辅助索引）
聚簇索引，只能在搜索条件是主键值时才发挥作用，因为B+树中的数据都是按照主键进行排序的，如果我们想以别的列作为搜索条件，那么需要创建非聚簇索引。
例如，以c2列作为搜索条件，那么需要使用c2列创建一棵B+树，如下所示：

这个B+树与聚簇索引有几处不同：

页内的记录是按照从c2列的大小顺序排成一个单向链表 。

页和页之间也是根据页中记录的c2列的大小顺序排成一个双向链表 。

非叶子节点存储的是记录的c2列+页号。

叶子节点存储的并不是完整的用户记录，而只是c2列+主键这两个列的值。


一张表可以有多个非聚簇索引：

006	说一下B+树中聚簇索引的查找（匹配）逻辑
007	说一下B+树中非聚簇索引的查找（匹配）逻辑**例如：**根据c2列的值查找c2&#x3D;4的记录，查找过程如下：

根据根页面44定位到页42（因为2 ≤  4 &lt; 9）
由于c2列没有唯一性约束，所以c2&#x3D;4的记录可能分布在多个数据页中，又因为 2 ≤ 4  ≤ 4，所以确定实际存储用户记录的页在页34和页35中。
在页34和35中定位到具体的记录。
但是这个B+树的叶子节点只存储了c2和c1（主键）两个列，所以我们必须再根据主键值去聚簇索引中再查找一遍完整的用户记录。
like 张%


008	平衡二叉树，红黑树，B树和B+树的区别是什么？都有哪些应用场景？平衡二叉树

基础数据结构
左右平衡
高度差大于1会自旋
每个节点记录一个数据

平衡二叉树（AVL）
AVL树全称G.M. Adelson-Velsky和E.M. Landis，这是两个人的人名。
平衡二叉树也叫平衡二叉搜索树（Self-balancing binary search tree）又被称为AVL树， 可以保证查询效率较高。
具有以下特点：

它是一棵空树或它的左右两个子树的高度差的绝对值不超过1
并且左右两个子树都是一棵平衡二叉树。


AVL的生成演示：https://www.cs.usfca.edu/~galles/visualization/AVLtree.html
AVL的问题
众所周知，IO操作的效率很低，在大量数据存储中，查询时我们不能一下子将所有数据加载到内存中，只能逐节点加载（一个节点一次IO）。如果我们利用二叉树作为索引结构，那么磁盘的IO次数和索引树的高度是相关的。平衡二叉树由于树深度过大而造成磁盘IO读写过于频繁，进而导致效率低下。

为了提高查询效率，就需要 减少磁盘IO数 。为了减少磁盘IO的次数，就需要尽量降低树的高度 ，需要把原来“瘦高”的树结构变的“矮胖”，树的每层的分叉越多越好。针对同样的数据，如果我们把二叉树改成 三叉树：

上面的例子中，我们将二叉树变成了三叉树，降低了树的高度。如果能够在一个节点中存放更多的数据，我们还可以进一步减少节点的数量，从而进一步降低树的高度。这就是多叉树。
普通树的问题

左子树全部为空，从形式上看，更像一个单链表，不能发挥BST的优势。
解决方案：平衡二叉树(AVL)


红黑树

hashmap存储
两次旋转达到平衡
分为红黑节点

在这个棵严格的平台树上又进化为“红黑树”{是一个非严格的平衡树 左子树与右子树的高度差不能超过1}，红黑树的长子树只要不超过短子树的两倍即可！

当再次插入7的时候，这棵树就会发生旋转

B+ 树和 B 树的差异：

B+树中非叶子节点的关键字也会同时存在在子节点中，并且是在子节点中所有关键字的最大值（或最小）。
B+树中非叶子节点仅用于索引，不保存数据记录，跟记录有关的信息都放在叶子节点中。而B树中， 非叶子节点既保存索引，也保存数据记录 。 
B+树中所有关键字都在叶子节点出现，叶子节点构成一个有序链表，而且叶子节点本身按照关键字的大小从小到大顺序链接。

009	一个b+树中大概能存放多少条索引记录？
真实环境中一个页存放的记录数量是非常大的（默认16KB），假设指针与键值忽略不计（或看做10个字节），数据占 1 kb 的空间：
如果B+树只有1层，也就是只有1个用于存放用户记录的节点，最多能存放 16 条记录。
如果B+树有2层，最多能存放 1600×16=25600 条记录。
如果B+树有3层，最多能存放 1600×1600×16=40960000 条记录。
如果存储千万级别的数据，只需要三层就够了

B+树的非叶子节点不存储用户记录，只存储目录记录，相对B树每个节点可以存储更多的记录，树的高度会更矮胖，IO次数也会更少。
010	使用B+树存储的索引crud执行效率如何？c 新增
O(lognN)
N &#x3D; 高度
011	什么是自适应哈希索引？自适应哈希索引是Innodb引擎的一个特殊功能，当它注意到某些索引值被使用的非常频繁时，会在内存中基于B-Tree所有之上再创建一个哈希索引，这就让B-Tree索引也具有哈希索引的一些优点，比如快速哈希查找。这是一个完全自动的内部行为，用户无法控制或配置
使用命令
SHOW ENGINE INNODB STATUS \G ;



查看INSERT BUFFER AND ADAPTIVE HASH INDEX
012	什么是2-3树 2-3-4树？多叉树（multiway tree）允许每个节点可以有更多的数据项和更多的子节点。2-3树，2-3-4树就是多叉树，多叉树通过重新组织节点，减少节点数量，增加分叉，减少树的高度，能对二叉树进行优化。
2-3树
下面2-3树就是一颗多叉树

2-3树具有如下特点：

2-3树的所有叶子节点都在同一层。
有两个子节点的节点叫二节点，二节点要么没有子节点，要么有两个子节点。
有三个子节点的节点叫三节点，三节点要么没有子节点，要么有三个子节点。
2-3树是由二节点和三节点构成的树。
对于三节点的子树的值大小仍然遵守 BST 二叉排序树的规则。


2-3-4树

013 为什么官方建议使用自增长主键作为索引？（说一下自增主键和字符串类型主键的区别和影响）
自增主键能够维持底层数据顺序写入
读取可以由b+树的二分查找定位
支持范围查找，范围数据自带顺序

字符串无法完成以上操作
014 使用int自增主键后 最大id是10，删除id 10和9，再添加一条记录，最后添加的id是几？删除后重启mysql然后添加一条记录最后id是几？删除之后

如果重启，会从最大的id开始递增
如果没重启，会延续删除之前最大的id开始递增

015 索引的优缺点是什么？优点
聚簇（主键）索引：

顺序读写
范围快速查找
范围查找自带顺序

非聚簇索引：

条件查询避免全表扫描scan
范围，排序，分组查询返回行id，排序分组后，再回表查询完整数据，有可能利用顺序读写
覆盖索引不需要回表操作

索引的代价
索引是个好东西，可不能乱建，它在空间和时间上都会有消耗：

空间上的代价

每建立一个索引都要为它建立一棵B+树，每一棵B+树的每一个节点都是一个数据页，一个页默认会占用 16KB 的存储空间，一棵很大的B+树由许多数据页组成，那就是很大的一片存储空间。

时间上的代价

每次对表中的数据进行 增、删、改 操作时，都需要去修改各个B+树索引。而增、删、改操作可能会对节点和记录的排序造成破坏，所以存储引擎需要额外的时间进行一些记录移位、页面分裂、页面回收等操作来维护好节点和记录的排序。如果我们建了许多索引，每个索引对应的B+树都要进行相关的维护操作，会给性能拖后腿。
B 树和 B+ 树都可以作为索引的数据结构，在 MySQL 中采用的是 B+ 树。
但B树和B+树各有自己的应用场景，不能说B+树完全比B树好，反之亦然。
016	使用索引一定能提升效率吗？不一定

少量数据全表扫描也很快，可以直接获取到全量数据
唯一索引会影响插入速度，但建议使用
索引过多会影响更新，插入，删除数据速度

017	如果是大段文本内容，如何创建（优化）索引？B 树和 B+ 树都可以作为     索引的数据结构，**在 MySQL 中采用的是 B+ 树。**



第一种方式是分表存储，然后创建索引
第二是使用es为大文本创建索引
018 什么是聚簇索引？聚簇索引数据和索引存放在一起组成一个b+树
参考005题
019	一个表中可以有多个（非）聚簇索引吗？聚簇索引只能有一个
非聚簇索引可以有多个
020	聚簇索引与非聚集索引的特点是什么？参考005题
021	CRUD时聚簇索引与非聚簇索引的区别是什么？
聚簇索引插入新值比采用非聚簇索引插入新值的速度要慢很多，因为插入要保证主键不能重复
聚簇索引范围，排序查找效率高，因为是有序的
非聚簇索引访问需要两次索引查找，第一次找到主键值，第二次根据主键值找到行数据

022 非聚簇索引为什么不存数据地址值而存储主键？因为聚簇索引中有时会引发分页操作、重排操作数据有可能会移动
023 什么是回表操作？id age name sex
age -&gt; index
select * from user where age &gt;20 ;
第一次 取回id，第二次（回表）根据id拿到完整数据
select * from user where age &gt;20 ;
024	什么是覆盖索引？id age name sex
age -&gt; index
select * from user where age &gt;20 ;
第一次 取回id，第二次（回表）根据id拿到完整数据
age,name -&gt; index
select age from user where age &gt;20 and name like”张%” ;
覆盖索引不会回表查询，查询效率也是比较高的
025	非聚集索引一定回表查询吗?不一定，只要b+树中包含的字段（创建索引的字段），覆盖（包含）想要select 的字段，那么就不会回表查询了。
026	为什么要回表查询？直接存储数据不可以吗？为了控制非聚簇索引的大小
027	如果把一个 InnoDB 表的主键删掉，是不是就没有主键，就没办法进行回表查询了？不是，InnoDB会生成rowid辅助回表查询
028 什么是联合索引，组合索引，复合索引？为c2和c3列建立联合索引，如下所示：
c2，c3 - &gt; index
c3,c2 -&gt; index
where c3&#x3D;? 
全职匹配
最左前缀

029 复合索引创建时字段顺序不一样使用效果一样吗？我们也可以同时以多个列的大小作为排序规则，也就是同时为多个列建立索引，比方说我们想让B+树按照 c2和c3列 的大小进行排序，这个包含两层含义：

先把各个记录和页按照c2列进行排序。
在记录的c2列相同的情况下，采用c3列进行排序
B+树叶子节点处的记录由c2列、c3列和主键c1列组成
本质上也是二级索引

create index idx_c2_c3 on user (c2,c3); 
030 什么是唯一索引？
随表一起创建索引：

CREATE TABLE customer (      id INT UNSIGNED AUTO_INCREMENT,  customer_no VARCHAR(200),  customer_name VARCHAR(200),      PRIMARY KEY(id), -- 主键索引：列设定为主键后会自动建立索引，唯一且不能为空。  UNIQUE INDEX uk_no (customer_no), -- 唯一索引：索引列值必须唯一，允许有NULL值，且NULL可能会出现多次。  KEY idx_name (customer_name), -- 普通索引：既不是主键，列值也不需要唯一，单纯的为了提高查询速度而创建。  KEY idx_no_name (customer_no,customer_name) -- 复合索引：即一个索引包含多个列。);




单独建创索引：

CREATE TABLE customer1 (  id INT UNSIGNED,  customer_no VARCHAR(200),  customer_name VARCHAR(200));ALTER TABLE customer1 ADD PRIMARY KEY customer1(id); -- 主键索引CREATE UNIQUE INDEX uk_no ON customer1(customer_no); -- 唯一索引CREATE INDEX idx_name ON customer1(customer_name);  -- 普通索引CREATE INDEX idx_no_name ON customer1(customer_no,customer_name); -- 复合索引



031 唯一索引是否影响性能？是
032 什么时候使用唯一索引？业务需求唯一字段的时候，一般不考虑性能问题
. 【强制】业务上具有唯一特性的字段，即使是多个字段的组合，也必须建成唯一索引。 说明：不要以为唯一索引影响了 insert 速度，这个速度损耗可以忽略，但提高查找速度是明 显的；另外，即使在应用层做了非常完善的校验控制，只要没有唯一索引，根据墨菲定律，必 然有脏数据产生。
033 什么时候适合创建索引，什么时候不适合创建索引？适合创建索引

频繁作为where条件语句查询字段

关联字段需要建立索引

排序字段可以建立索引

分组字段可以建立索引(因为分组前提是排序)

统计字段可以建立索引（如.count(),max()）


不适合创建索引

频繁更新的字段不适合建立索引

where，分组，排序中用不到的字段不必要建立索引

可以确定表数据非常少不需要建立索引

参与mysql函数计算的列不适合建索引


创建索引时避免有如下极端误解：
 1）宁滥勿缺。认为一个查询就需要建一个索引。 
2）宁缺勿滥。认为索引会消耗空间、严重拖慢更新和新增速度。 
3）抵制惟一索引。认为业务的惟一性一律需要在应用层通过“先查后插”方式解决。
034 什么是索引下推？5.6之前的版本是没有索引下推这个优化的
**Using index condition：**叫作  Index Condition Pushdown Optimization （索引下推优化）

如果没有索引下推（ICP），那么MySQL在存储引擎层找到满足content1 &gt; &#39;z&#39;条件的第一条二级索引记录。主键值进行回表，返回完整的记录给server层，server层再判断其他的搜索条件是否成立。如果成立则保留该记录，否则跳过该记录，然后向存储引擎层要下一条记录。
如果使用了索引下推（ICP），那么MySQL在存储引擎层找到满足content1 &gt; &#39;z&#39;条件的第一条二级索引记录。不着急执行回表，而是在这条记录上先判断一下所有关于idx_content1索引中包含的条件是否成立，也就是content1 &gt; &#39;z&#39; AND content1 LIKE &#39;%a&#39;是否成立。如果这些条件不成立，则直接跳过该二级索引记录，去找下一条二级索引记录；如果这些条件成立，则执行回表操作，返回完整的记录给server层。

总结：
未开启索引下推：

根据筛选条件在索引树中筛选第一个条件
获得结果集后回表操作
进行其他条件筛选
再次回表查询

开启索引下推：在条件查询时，当前索引树如果满足全部筛选条件，可以在当前树中完成全部筛选过滤，得到比较小的结果集再进行回表操作
035 有哪些情况会导致索引失效？
计算、函数导致索引失效

-- 显示查询分析EXPLAIN SELECT * FROM emp WHERE emp.name  LIKE &#x27;abc%&#x27;;EXPLAIN SELECT * FROM emp WHERE LEFT(emp.name,3) = &#x27;abc&#x27;; --索引失效




LIKE以%，_ 开头索引失效


拓展：Alibaba《Java开发手册》
【强制】页面搜索严禁左模糊或者全模糊，如果需要请走搜索引擎来解决。

EXPLAIN SELECT * FROM emp WHERE name LIKE &#x27;%ab%&#x27;; --索引失效





不等于(!&#x3D; 或者&lt;&gt;)索引失效

EXPLAIN SELECT SQL_NO_CACHE * FROM emp WHERE emp.name = &#x27;abc&#x27; ;EXPLAIN SELECT SQL_NO_CACHE * FROM emp WHERE emp.name &lt;&gt; &#x27;abc&#x27; ; --索引失效





IS NOT NULL 失效 和 IS NULL

EXPLAIN SELECT * FROM emp WHERE emp.name IS NULL;EXPLAIN SELECT * FROM emp WHERE emp.name IS NOT NULL; --索引失效

**注意：**当数据库中的数据的索引列的NULL值达到比较高的比例的时候，即使在IS NOT NULL 的情况下 MySQL的查询优化器会选择使用索引，此时type的值是range（范围查询）
-- 将 id&gt;20000 的数据的 name 值改为 NULLUPDATE emp SET `name` = NULL WHERE `id` &gt; 20000;-- 执行查询分析，可以发现 IS NOT NULL 使用了索引-- 具体多少条记录的值为NULL可以使索引在IS NOT NULL的情况下生效，由查询优化器的算法决定EXPLAIN SELECT * FROM emp WHERE emp.name IS NOT NULL


类型转换导致索引失效

EXPLAIN SELECT * FROM emp WHERE name=&#x27;123&#x27;; EXPLAIN SELECT * FROM emp WHERE name= 123; --索引失效


复合索引未用左列字段失效
如果mysql觉得全表扫描更快时（数据少）;

036  为什么LIKE以%开头索引会失效？id,name,age
name 创建索引
select *  from user where  name like ‘%明’
type&#x3D;all
select name,id  from user where  name like ‘%明’
type&#x3D;index
张明
(name,age)
其实并不会完全失效，覆盖索引下会出现type&#x3D;index，表示遍历了索引树，再回表查询，
覆盖索引没有生效的时会直接type&#x3D;all
没有高效使用索引是因为字符串索引会逐个转换成accii码，生成b+树时按首个字符串顺序排序，类似复合索引未用左列字段失效一样，跳过开始部分也就无法使用生成的b+树了
037  一个表有多个索引的时候，能否手动选择使用哪个索引？不可用手动直接干预，只能通过mysql优化器自动选择
038  	如何查看一个表的索引？show index from t_emp; // 显示表上的索引explain select * from t_emp where id=1; // 显示可能会用到的索引及最终使用的索引



039  	能否查看到索引选择的逻辑？是否使用过optimizer_trace？set session optimizer_trace=&quot;enabled=on&quot;,end_markers_in_json=on;SELECT * FROM information_schema.OPTIMIZER_TRACE;set session optimizer_trace=&quot;enabled=off&quot;;



040  	多个索引优先级是如何匹配的？
主键（唯一索引）匹配
全值匹配（单值匹配）
最左前缀匹配
范围匹配
索引扫描
全表扫描

 一般性建议
Ø 对于单键索引，尽量选择过滤性更好的索引（例如：手机号，邮件，身份证）
Ø 在选择组合索引的时候，过滤性最好的字段在索引字段顺序中，位置越靠前越好。
Ø 选择组合索引时，尽量包含where中更多字段的索引
Ø 组合索引出现范围查询时，尽量把这个字段放在索引次序的最后面
Ø 尽量避免造成索引失效的情况
041  	使用Order By时能否通过索引排序？没有过滤条件不走索引
042  		通过索引排序内部流程是什么？select name,id  from user where  name like ‘%明’ order by name；
select name,id，age  from user where  name like ‘%明’
关键配置：

sort_buffer可供排序的内存缓冲区大小
max_length_for_sort_data 单行所有字段总和限制，超过这个大小启动双路排序


通过索引检过滤筛选条件索到需要排序的字段+其他字段（如果是符合索引）
判断索引内容是否覆盖select的字段
如果覆盖索引，select的字段和排序都在索引上，那么在内存中进行排序，排序后输出结果
如果索引没有覆盖查询字段，接下来计算select的字段是否超过max_length_for_sort_data限制，如果超过，启动双路排序，否则使用单路



043  		什么是双路排序和单路排序单路排序：一次取出所有字段进行排序，内存不够用的时候会使用磁盘
双路排序：取出排序字段进行排序，排序完成后再次回表查询所需要的其他字段
如果不在索引列上，filesort有两种算法： mysql就要启动双路排序和单路排序
 双路排序（慢）
Select id,age,name from stu order by name;
Ø MySQL 4.1之前是使用双路排序，字面意思就是两次扫描磁盘，最终得到数据， 读取行指针和order by列，对他们进行排序，然后扫描已经排序好的列表，按照列表中的值重新从列表中读取对应的数据输出
Ø 从磁盘取排序字段，在buffer进行排序，再从磁盘取其他字段。
Ø 取一批数据，要对磁盘进行两次扫描，众所周知，I\O是很耗时的，所以在mysql4.1之后，出现了第二种改进的算法，就是单路排序。
单路排序（快）
从磁盘读取查询需要的所有列，按照order by列在buffer对它们进行排序，然后扫描排序后的列表进行输出， 它的效率更快一些，避免了第二次读取数据。并且把随机IO变成了顺序IO，但是它会使用更多的空间， 因为它把每一行都保存在内存中了。
结论及引申出的问题
 但是用单路有问题
在sort_buffer中，单路比多路要多占用很多空间，因为单路是把所有字段都取出, 所以有可能取出的数据的总大小超出了sort_buffer的容量，导致每次只能取sort_buffer容量大小的数据，进行排序（创建tmp文件，多路合并），排完再取sort_buffer容量大小，再排……从而多次I&#x2F;O。
单路本来想省一次I&#x2F;O操作，反而导致了大量的I&#x2F;O操作，反而得不偿失。
优化策略
Ø 增大sort_buffer_size参数的设置
Ø 增大max_length_for_sort_data参数的设置
Ø 减少select 后面的查询的字段。 禁止使用select * 
提高Order By的速度 
\1. Order by时select * 是一个大忌。只Query需要的字段， 这点非常重要。在这里的影响是：
l 当Query的字段大小总和小于max_length_for_sort_data 而且排序字段不是 TEXT|BLOB 类型时，会用改进后的算法——单路排序， 否则用老算法——多路排序。
l 两种算法的数据都有可能超出sort_buffer的容量，超出之后，会创建tmp文件进行合并排序，导致多次I&#x2F;O，但是用单路排序算法的风险会更大一些，所以要提高sort_buffer_size。 
\2. 尝试提高 sort_buffer_size
l 不管用哪种算法，提高这个参数都会提高效率，当然，要根据系统的能力去提高，因为这个参数是针对每个进程（connection）的 1M-8M之间调整。 MySQL5.7和8.0，InnoDB存储引擎默认值是1048576字节，1MB。
SHOW VARIABLES LIKE ‘%sort_buffer_size%’;
​                               
\3. 尝试提高 max_length_for_sort_data
l 提高这个参数， 会增加用改进算法的概率。
SHOW VARIABLES LIKE ‘%max_length_for_sort_data%’; 
#5.7默认1024字节
#8.0默认4096字节
l 但是如果设的太高，数据总容量超出sort_buffer_size的概率就增大，明显症状是高的磁盘I&#x2F;O活动和低的处理器使用率。如果需要返回的列的总长度大于max_length_for_sort_data，使用双路算法，否则使用单路算法。1024-8192字节之间调整
044  		group by 分组和order by在索引使用上有什么区别？group by 使用索引的原则几乎跟order by一致 ，唯一区别：

group by 先排序再分组，遵照索引建的最佳左前缀法则
group by没有过滤条件，也可以用上索引。Order By 必须有过滤条件才能使用上索引。

045  	如果表中有字段为null，又被经常查询该不该给这个字段创建索引？应该创建索引，使用的时候尽量使用is null判断。

IS NOT NULL 失效 和 IS NULL

EXPLAIN SELECT * FROM emp WHERE emp.name IS NULL;EXPLAIN SELECT * FROM emp WHERE emp.name IS NOT NULL; --索引失效

**注意：**当数据库中的数据的索引列的NULL值达到比较高的比例的时候，即使在IS NOT NULL 的情况下 MySQL的查询优化器会选择使用索引，此时type的值是range（范围查询）
-- 将 id&gt;20000 的数据的 name 值改为 NULLUPDATE emp SET `name` = NULL WHERE `id` &gt; 20000;-- 执行查询分析，可以发现 IS NOT NULL 使用了索引-- 具体多少条记录的值为NULL可以使索引在IS NOT NULL的情况下生效，由查询优化器的算法决定EXPLAIN SELECT * FROM emp WHERE emp.name IS NOT NULL



046  		有字段为null索引是否会失效？不一定会失效，每一条sql具体有没有使用索引 可以通过trace追踪一下
最好还是给上默认值
数字类型的给0，字符串给个空串“”，
参考上一题
二 MySQL 内部技术架构
047 Mysql内部支持缓存查询吗？当MySQL接收到客户端的查询SQL之后，仅仅只需要对其进行相应的权限验证之后，就会通过Query Cache来查找结果，甚至都不需要经过Optimizer模块进行执行计划的分析优化，更不需要发生任何存储引擎的交互
mysql5.7支持内部缓存，8.0之后就废弃掉了
048 	mysql8为何废弃掉查询缓存？缓存的意义在于快速查询提升系统性能，可以灵活控制缓存的一致性
mysql缓存的限制

mysql基本没有手段灵活的管理缓存失效和生效，尤其对于频繁更新的表
SQL必须完全一致才会导致cache命中
为了节省内存空间，太大的result set不会被cache (&lt; query_cache_limit)；
MySQL缓存在分库分表环境下是不起作用的；
执行SQL里有触发器,自定义函数时，MySQL缓存也是不起作用的；
在表的结构或数据发生改变时，基于该表相关cache立即全部失效。

049 	替代方案是什么？应用层组织缓存，最简单的是使用redis，ehcached等
050 Mysql内部有哪些核心模块组成，作用是什么？
Connectors（客户端）
MySQL服务器之外的客户端程序，与具体的语言相关，例如Java中的JDBC，图形用户界面SQLyog等。本质上都是在TCP连接上通过MySQL协议和MySQL服务器进行通信。
MySQL Server（服务器）
第1层：连接层

系统（客户端）访问 MySQL 服务器前，做的第一件事就是建立 TCP 连接。
经过三次握手建立连接成功后， MySQL 服务器对 TCP 传输过来的账号密码做身份认证、权限获取。
用户名或密码不对，会收到一个Access denied for user错误，客户端程序结束执行
用户名密码认证通过，会从权限表查出账号拥有的权限与连接关联，之后的权限判断逻辑，都将依赖于此时读到的权限


TCP 连接收到请求后，必须要分配给一个线程专门与这个客户端的交互。所以还会有个线程池，去走后面的流程。每一个连接从线程池中获取线程，省去了创建和销毁线程的开销。

第2层：服务层
Management Serveices &amp; Utilities： 系统管理和控制工具
SQL Interface：SQL接口：

接收用户的SQL命令，并且返回用户需要查询的结果。比如SELECT … FROM就是调用SQL Interface 
MySQL支持DML（数据操作语言）、DDL（数据定义语言）、存储过程、视图、触发器、自定义函数等多种SQL语言接口

Parser：解析器：

在SQL命令传递到解析器的时候会被解析器验证和解析。解析器中SQL 语句进行语法分析、语法解析，并为其创建语法树。


语法分析
语法分析主要是把输入转化成若干个tokens，包含key和非key。
在分析之后，会得到4个Token，其中有2个key，它们分别是SELECT、FROM。



key
非key
key
非key



SELECT
age
FROM
user



典型的解析树如下：


Optimizer：查询优化器：

SQL语句在语法解析后、查询前会使用查询优化器对查询进行优化，确定SQL语句的执行路径，生成一个执行计划。

Caches &amp; Buffers： 查询缓存组件：

MySQL内部维持着一些Cache和Buffer，比如Query Cache用来缓存一条SELECT语句的执行结果，如果能够在其中找到对应的查询结果，那么就不必再进行查询解析、查询优化和执行的整个过程了，直接将结果反馈给客户端。
这个缓存机制是由一系列小缓存组成的。比如表缓存，记录缓存，key缓存，权限缓存等 。
这个查询缓存可以在不同客户端之间共享 。

第3层：引擎层
插件式存储引擎层（ Storage Engines），负责MySQL中数据的存储和提取，对物理服务器级别维护的底层数据执行操作，服务器通过API与存储引擎进行通信。不同的存储引擎具有的功能不同，管理的表有不同的存储结构，采用的存取算法也不同，这样我们可以根据自己的实际需要进行选取。例如MyISAM引擎和InnoDB引擎。
存储层
所有的数据、数据库、表的定义、表的每一行的内容、索引，都是存在文件系统 上，以文件的方式存在，并完成与存储引擎的交互。
051 一条sql发送给mysql后，内部是如何执行的？（说一下 MySQL 执行一条查询语句的内部执行过程？）1.5、查询流程说明

首先，MySQL客户端通过协议与MySQL服务器建连接，通过SQL接口发送SQL语句，先检查查询缓存，如果命中，直接返回结果，否则进行语句解析。也就是说，在解析查询之前，服务器会先访问查询缓存，如果某个查询结果已经位于缓存中，服务器就不会再对查询进行解析、优化、以及执行。它仅仅将缓存中的结果返回给用户即可，这将大大提高系统的性能。
接下来，MySQL解析器通过关键字将SQL语句进行解析，并生成一棵对应的解析树，解析器使用MySQL语法规则验证和解析SQL语句。例如，它将验证是否使用了错误的关键字，或者使用关键字的顺序是否正确，引号能否前后匹配等；预处理器则根据MySQL规则进一步检查解析树是否合法，例如，这里将检查数据表和数据列是否存在，还会解析名字和别名，看是否有歧义等。然后预处理器会进行查询重写，生成一棵新解析树。
接下来，查询优化器将解析树转化成执行计划。MySQL优化程序会对我们的语句做一些优化，如子查询转换为连接、表达式简化等等。优化的结果就是生成一个执行计划，这个执行计划表明了应该使用哪些索引执行查询，以及表之间的连接顺序是啥样，等等。我们可以使用EXPLAIN语句来查看某个语句的执行计划。
最后，进入执行器阶段。完成查询优化后，查询执行引擎会按照生成的执行计划调用存储引擎提供的接口执行SQL查询并将结果返回给客户端。在MySQL8以下的版本，如果设置了查询缓存，这时会将查询结果进行缓存，再返回给客户端。




052 MySQL 提示“不存在此列”是执行到哪个节点报出的？是在Parser：解析器 分析sql语法的时候检查的列。
053 如果一张表创建了多个索引，在哪个阶段或模块进行的索引选择？在优化器阶段Optimizer：查询优化器：
054 MySQL 支持哪些存储引擎？默认使用哪个？查看MySQL提供什么存储引擎
SHOW ENGINES;

下面的结果表示MySQL中默认使用的存储引擎是InnoDB，支持事务，行锁，外键，支持分布式事务(XA)，支持保存点(回滚)

也可以通过以下语句查看默认的存储引擎：
SHOW VARIABLES LIKE &#x27;%default_storage_engine%&#x27;;


055 Mysql8.0自带哪些存储引擎？分别是做什么的？1. InnoDB存储引擎

InnoDB是MySQL的默认事务型引擎，它被设计用来处理大量的短期(short-lived)事务。可以确保事务的完整提交(Commit)和回滚(Rollback)。

除非有非常特别的原因需要使用其他的存储引擎，否则应该优先考虑InnoDB引擎。

数据文件结构：

表名.frm 存储表结构（MySQL8.0时，合并在表名.ibd中）

表名.ibd 存储数据和索引



InnoDB不仅缓存索引还要缓存真实数据， 对内存要求较 高 ，而且内存大小对性能有决定性的影响。


2. MyISAM存储引擎

MyISAM提供了大量的特性，包括全文索引、压缩、空间函数(GIS)等，但MyISAM不支持事务和行级锁，有一个毫无疑问的缺陷就是崩溃后无法安全恢复。

优势是访问的 速度快 ，对事务完整性没有要求或者以SELECT、INSERT为主的应用。

数据文件结构：

表名.frm 存储表结构

表名.MYD 存储数据

表名.MYI 存储索引



MyISAM只缓存索引，不缓存真实数据。


3. Archive引擎

Archive档案存储引擎只支持INSERT和SELECT操作。
Archive表适合日志和数据采集（档案）类应用。
根据英文的测试结论来看，Archive表比MyISAM表要小大约75%，比支持事务处理的InnoDB表小大约83%。

4. Blackhole引擎

Blackhole引擎没有实现任何存储机制，它会丢弃所有插入的数据，不做任何保存。
但服务器会记录Blackhole表的日志，所以可以用于复制数据到备库，或者简单地记录到日志。但这种应用方式会碰到很多问题，因此并不推荐。

5. CSV引擎 

CSV引擎可以将普通的CSV文件作为MySQL的表来处理，但不支持索引。
CSV引擎可以作为一种数据交换的机制，非常有用。
CSV存储的数据直接可以在操作系统里，用文本编辑器，或者excel读取。

6. Memory引擎

如果需要快速地访问数据，并且这些数据不会被修改，重启以后丢失也没有关系，那么使用Memory表是非常有用。
Memory表至少比MyISAM表要快一个数量级。

7. Federated引擎

Federated引擎是访问其他MySQL服务器的一个代理（跨库关联查询），尽管该引擎看起来提供了一种很好的跨服务器的灵活性，但也经常带来问题，因此默认是禁用的。

056 MySQL 存储引擎架构了解吗？https://dev.mysql.com/doc/refman/5.7/en/innodb-architecture.html
下面是官方的InnoDB引擎结构图，主要分为内存结构和磁盘结构两大部分。

内存区域
Buffer Pool:在InnoDB访问表记录和索引时会在Buffer Pool的页中缓存，以后使用可以减少磁盘IO操作，提升效率。主要用来缓存热的数据页和索引页。
Log Buffer：用来缓存redolog
Adaptive Hash Index：自适应哈希索引
Change Buffer:它是一种应用在非唯一普通索引页（non-unique secondary index page）不在缓冲池中，对页进行了写操作，并不会立刻将磁盘页加载到缓冲池，而仅仅记录缓冲变更（Buffer Changes），等未来数据被读取时，再将数据合并（Merge）恢复到缓冲池中的技术。写缓冲的目的是降低写操作的磁盘IO，提升数据库性能。
磁盘区域
磁盘中的结构分为两大类：表空间和重做日志。

表空间：分为系统表空间(MySQL 目录的 ibdata1 文件)，临时表空间，常规表空间，Undo 表空间以及 file-per-table 表空间(MySQL5.7默认打开file_per_table 配置）。系统表空间又包括了InnoDB数据字典，双写缓冲区(Doublewrite Buffer)，修改缓存(Change Buffer），Undo日志等。
Redo日志：存储的就是 Log Buffer 刷到磁盘的数据。

官方文档：
https://dev.mysql.com/doc/refman/8.0/en/innodb-storage-engine.html
057 	能否单独为一张表设置存储引擎？方法1：
设置默认存储引擎：
SET DEFAULT_STORAGE_ENGINE=MyISAM;



方法2：
或者修改 my.cnf 文件：vim &#x2F;etc&#x2F;my.cnf新增一行：default-storage-engine&#x3D;MyISAM重启MySQL：systemctl restart mysqld
方法3：
我们可以为 不同的表设置不同的存储引擎
CREATE TABLE 表名( 建表语句; ) ENGINE = 存储引擎名称;ALTER TABLE 表名 ENGINE = 存储引擎名称;





058 	阿里、京东等大厂都有自研的存储引擎，如何开发一套自己的？开发存储引擎并不难，难的是开发出来高效的有意义的存储引擎。
简单例子可以看一下官方源码中的示例，可以实现一个什么也没做的存储引擎。
有兴趣可以参考官方文档：https://dev.mysql.com/doc/dev/mysql-server/latest/
059 MyISAM 和 InnoDB 的区别是什么？外键 事务 锁



对比项
MyISAM
InnoDB



外键
不支持
支持


事务
不支持
支持


行表锁
表锁，即使操作一条记录也会锁住整个表，不适合高并发的操作
行锁，操作时只锁某一行，不对其它行有影响，适合高并发的操作


缓存
只缓存索引，不缓存真实数据
不仅缓存索引还要缓存真实数据，对内存要求较高，而且内存大小对性能有决定性的影响


关注点
并发查询，节省资源、消耗少、简单业务
并发写、事务、多表关系、更大资源


默认安装
Y
Y


默认使用
N
Y


自带系统表使用
Y
N


060 	具体说一下如何做技术选型除非几乎没有写操作全部都是高频的读操作可以选择MyISAM作为表的存储引擎，其他业务可以一律使用InnoDB。
三 mysql 事务061 什么是数据库事务？事务的特性是什么？事务：

是数据库操作的最小工作单元，是作为单个逻辑工作单元执行的一系列操作；

这些操作作为一个整体一起向系统提交，要么都执行、要么都不执行；

事务是一组不可再分割的操作集合（工作逻辑单元）


事务都有 ACID 特性
062 	什么是ACID？1 、原子性  atomicity
过程的保证
只做一个步骤
1 给钱
2 去买
3 交回来
事务是数据库的逻辑工作单位，事务中包含的各操作要么都做，要么都不做 
2 、一致性  consistency
结果的保证
保证要吃完 刚张嘴挂了，失去一致性
事 务执行的结果必须是使数据库从一个一致性状态变到另一个一致性状态。因此当数据库只包含成功事务提交的结果时，就说数据库处于一致性状态。如果数据库系统 运行中发生故障，有些事务尚未完成就被迫中断，这些未完成事务对数据库所做的修改有一部分已写入物理数据库，这时数据库就处于一种不正确的状态，或者说是 不一致的状态。3 、隔离性 isolation
并发事务互相干扰
不被干扰 刚张嘴别人塞了东西
一个事务的执行不能其它事务干扰。即一个事务内部的操作及使用的数据对其它并发事务是隔离的，并发执行的各个事务之间不能互相干扰。4 、持续性 永久性 durability
保存 吃到肚子里
也称永久性，指一个事务一旦提交，它对数据库中的数据的改变就应该是永久性的。接下来的其它操作或故障不应该对其执行结果有任何影响。 
063 并发事务会有哪些问题？多个事务并发执行一定会产生相互争夺资源的问题
064	什么是脏读    065	丢失修改    066	不可重复读 067	幻读脏读（Dirty read）
是一个事务在处理过程中读取了另外一个事务未提交的数据
当一个事务正在访问数据并且对其进行了修改，但是还没提交事务，这时另外一个事务也访问了这个数据，然后使用了这个数据，因为这个数据的修改还没提交到数据库，所以另外一个事务读取的数据就是“脏数据”，这种行为就是“脏读”，依据“脏数据”所做的操作可能是会出现问题的。
修改丢失（Lost of modify）
*是指一个事务读取一个数据时，另外一个数据也访问了该数据，那么在第一个事务修改了这个数据之后，第二个事务也修改了这个数据。这样第一个事务内的修改结果就被丢失，这种情况就被称为**修改丢失
不可重复读（Unrepeatableread）
*指在一个事务内多*次读取同一数据，在这个事务还没结束时，另外一个事务也访问了这个数据并对这个数据进行了修改，那么就可能造成第一个事务两次读取的数据不一致，这种情况就被称为**不可重复读。
幻读（Phantom read）
是指同一个事务内多次查询返回的结果集总数不一样（比如增加了或者减少了行记录）。
幻读与不可重复读类似，幻读是指一个事务读取了几行数据，这个事务还没结束，接着另外一个事务插入了一些数据，在随后的查询中，第一个事务读取到的数据就会比原本读取到的多，就好像发生了幻觉一样，所以称为 幻读。
068 不可重复读和幻读有什么区别？不可重复读 针对的是一份数据的修改
幻读 针对的是行数修改
069 Mysql是如何避免事务并发问题的？避免事务并发问题是需要付出性能代价的，此时和分布式系统设计一样（CAP定理及base理论），为了保证一致性就一定会牺牲性能，要做取舍
在mysql内部通过加锁的方式实现好了解决方案可供选择，就是配置事务隔离级别
070	什么是事务隔离级别？事务隔离级别                    脏读     不可重复读(被修改)    幻读（删减）读未提交（read-uncommitted）    是        是            是不可重复读（read-committed）    否        是            是可重复读（repeatable-read）     否        否            是串行化（serializable）          否        否            否





071	默认的级别是什么？MySQL InnoDB存储引擎默认的事务隔离级别是可重复读（REPEATABLE-READ）
MySQL 5.7 SELECT @@tx_isolation;MySQL 8.0 SELECT @@transaction_isolation;



072	如何选择事务隔离级别？隔离级别越低，事务请求的锁越少相应性能也就越高，如没有特殊要求或有错误发生，使用默认的隔离级别即可，如果系统中有高频读写并且对一致性要求高那么就需要比较高的事务隔离级别甚至串行化。
073 靠缓存可以提升高事务隔离级别的性能吗？提升事务级别的目的本质是提供更高的数据一致性，如果前置有缓存，那么缓存只能提供高效读并不能保证数据及时一致性，相反的我们还需要对缓存管理有额外的开销。
074 Mysql事务隔离是如何实现的？隔离的实现主要是读写锁和MVCC
075	什么是一致性非锁定读和锁定读？锁定读
使用到了读写锁
读写锁是最简单直接的的事务隔离实现方式

每次读操作需要获取一个共享(读)锁，每次写操作需要获取一个写锁。
共享锁之间不会产生互斥，共享锁和写锁之间、以及写锁与写锁之间会产生互斥。
当产生锁竞争时，需要等待其中一个操作释放锁后，另一个操作才能获取到锁。

锁机制，解决的就是多个事务同时更新数据，此时必须要有一个加锁的机制

行锁（记录锁）：解决的就是多个事务同时更新一行数据
间隙锁：解决的就是多个事务同时更新多行数据

下列操作属于锁定读
select ... lock in share modeselect ... for updateinsert、update、delete



非锁定读
v10 -&gt; age&#x3D;18 
v11 -&gt;age&#x3D;19
v12 -&gt;age&#x3D;15
使用mvcc 多版本控制实现
076	说一下MVCC内部细节https://dev.mysql.com/doc/refman/5.7/en/innodb-multi-versioning.html
Multi-Version Concurrency Control 多版本并发控制，MVCC 是一种并发控制的方法，一般在数据库管理系统中，实现对数据库的并发访问
InnoDB是一个多版本的存储引擎。它保存有关已更改行的旧版本的信息，以支持并发和回滚等事务特性。这些信息存储在一个称为回滚段的数据结构中的系统表空间或undo表空间中。参见第14.6.3.4节“撤消表空间”。InnoDB使用回滚段中的信息来执行事务回滚所需的撤消操作。它还使用这些信息构建行的早期版本，以实现一致的读取
MVCC 的实现依赖于：隐藏字段、Read View、undo log
隐藏字段

A 6-byte DB_TRX_ID 用来标识最近一次对本行记录做修改 (insert 、update) 的事务的标识符 ，即最后一次修改本行记录的事务 id。 如果是 delete 操作， 在 InnoDB 存储引擎内部也属于一次 update 操作，即更新行中的一个特殊位 ，将行标识为己删除，并非真正删除。
A 7-byte DB_ROLL_PTR 回滚指针，指向该行的 undo log 。如果该行未被更新，则为空.
A 6-byte DB_ROW_ID 如果没有设置主键且该表没有唯一非空索引时，InnoDB 会使用该 id 来生成聚簇索引.

Read View
不同的事务隔离级别中，当有事物在执行过程中修改了数据（更新版本号），在并发事务时需要判断一下版本链中的哪个版本是当前事务可见的。为此InnoDB有了ReadView的概念，使用ReadView来记录和隔离不同事务并发时此记录的哪些版本是对当前访问事物可见的。
undo log
除了用来回滚数据，还可以读取可见版本的数据。以此实现非锁定读
077Mysql事务一致性，原子性是如何实现的？首先是通过锁和mvcc实现了执行过程中的一致性和原子性
其次是在灾备方面通过Redo log实现，Redo log会把事务在执行过程中对数据库所做的所有修改都记录下来，在之后系统崩溃重启后可以把事务所做的任何修改都恢复出来。
078Mysql事务的持久性是如何实现的？使用Redo log保证了事务的持久性。当事务提交时，必须先将事务的所有日志写入日志文件进行持久化，就是我们常说的WAL(write ahead log)机制，如果出现断电重启便可以从redolog中恢复，如果redolog写入失败那么也就意味着修改失败整个事务也就直接回滚了。
079  表级锁和行级锁有什么区别？ 表级锁：串行化（serializable）时，整表加锁，事务访问表数据时需要申请锁，虽然可分为读锁和写锁，但毕竟是锁住整张表，会导致并发能力下降，一般是做ddl处理时使用
行级锁：除了串行化（serializable）时 InnoDB使用的都是行级锁，只锁一行数据，其他行数据不影响，并发能力强。
080	什么是行级锁？Mysql如何完成的？行级锁实现比较复杂不是单纯锁住一行数据，是由mvcc完成的。
081	什么是共享锁（读锁）？共享锁或S锁，其它事务可以继续加共享锁，但不能加排它锁
082	什么是排它锁（写锁&#x2F;独占锁）？排它锁或X锁，在进行写操作之前要申请并获得，其它事务不能再获得任何锁。
083	什么是意向锁？它分为意向共享锁（IS）和意向排他锁（IX）
一个事务对一张表的某行添加共享锁前，必须获得对该表一个IS锁或者优先级更高的锁。一个事务对一张表的某行添加排他锁之前，它必须对该表获取一个IX锁。
意向锁属于表锁，它不与innodb中的行锁冲突，任意两个意向锁之间也不会产生冲突，但是会与表锁（S锁和X锁）产生冲突
084	InnoDB支持哪几种锁？表锁，行锁，间隙锁，Next-Key锁等
在Serializable中读加共享锁，写加排他锁，读写互斥
两段锁协议，将事务分成两个阶段，加锁阶段和解锁阶段（所以叫两段锁）
085	当前读和快照读分别是什么？当前读 ：在锁定读（使用锁隔离事物）的时候读到的是最新版本的数据
快照读：可重复读（repeatable-read）下 mvcc生效读取的是数据的快照，并不是最新版本的数据（未提交事物的数据）
086 什么是XA协议？https://dev.mysql.com/doc/refman/8.0/en/xa.html


AP（Application Program）：应用程序，定义事务边界（定义事务开始和结束）并访问事务边界内的资源。

RM（Resource Manger）资源管理器: 管理共享资源并提供外部访问接口。供外部程序来访问数据库等共享资源。此外，RM还具有事务的回滚能力。

TM（Transaction Manager）事务管理器：TM是分布式事务的协调者，TM与每个RM进行通信，负责管理全局事务，分配事务唯一标识，监控事务的执行进度，并负责事务的提交、回滚、失败恢复等。

应用程序AP向事务管理器TM发起事务请求

TM调用xa_open()建立同资源管理器的会话

TM调用xa_start()标记一个事务分支的开头

AP访问资源管理器RM并定义操作，比如插入记录操作

TM调用xa_end()标记事务分支的结束

TM调用xa_prepare()通知RM做好事务分支的提交准备工作。其实就是二阶段提交的提交请求阶段。

TM调用xa_commit()通知RM提交事务分支，也就是二阶段提交的提交执行阶段。

TM调用xa_close管理与RM的会话。

这些接口一定要按顺序执行，比如xa_start接口一定要在xa_end之前。此外，这里千万要注意的是事务管理器只是标记事务分支并不执行事务，事务操作最终是由应用程序通知资源管理器完成的。另外，我们来总结下XA的接口


xa_start:负责开启或者恢复一个事务分支，并且管理XID到调用线程

xa_end:负责取消当前线程与事务分支的关系

xa_prepare:负责询问RM 是否准备好了提交事务分支 xa_commit:通知RM提交事务分支

xa_rollback:通知RM回滚事务分支


087 	什么是mysql xa事务？mysql的xa事务分为两部分：

InnoDB内部本地普通事务操作协调数据写入与log写入两阶段提交
外部分布式事务

5.7 SHOW VARIABLES LIKE &#x27;%innodb_support_xa%&#x27;;8.0 默认开启无法关闭



XA 事务语法示例如下：
XA START &#x27;自定义事务id&#x27;;SQL语句...XA END &#x27;自定义事务id&#x27;;XA PREPARE &#x27;自定义事务id&#x27;;XA COMMIT\ROLLBACK &#x27;自定义事务id&#x27;;

XA PREPARE 执行成功后，事务信息将被持久化。即使会话终止甚至应用服务宕机，只要我们将【自定义事务id】记录下来，后续仍然可以使用它对事务进行 rollback 或者 commit。
088 	xa事务与普通事务区别是什么？xa事务可以跨库或跨服务器，属于分布式事务，同时xa事务还支撑了InnoDB内部日志两阶段记录
普通事务只能在单库中执行
089 	什么是2pc 3pc？两阶段提交协议与3阶段提交协议，额外增加了参与的角色保证分布式事务完成更完善
090 是否使用过select for update？会产生哪些操作？查询库存 = 100  0 扣减库存  = -1 99记录日志 = log提交  commit



select本身是一个查询语句，查询语句是不会产生冲突的一种行为，一般情况下是没有锁的，用select for update 会让select语句产生一个排它锁(X), 这个锁和update的效果一样，会使两个事务无法同时更新一条记录。
https://dev.mysql.com/doc/refman/8.0/en/innodb-locks-set.html
https://dev.mysql.com/doc/refman/8.0/en/select.html

for update仅适用于InnoDB，且必须在事务块(BEGIN&#x2F;COMMIT)中才能生效。

在进行事务操作时，通过“for update”语句，MySQL会对查询结果集中每行数据都添加排他锁，其他线程对该记录的更新与删除操作都会阻塞。排他锁包含行锁、表锁。

InnoDB默认是行级别的锁，在筛选条件中当有明确指定主键或唯一索引列的时候，是行级锁。否则是表级别。


示例
SELECT … FOR UPDATE [OF column_list][WAIT n|NOWAIT][SKIP LOCKED];select * from t for update 会等待行锁释放之后，返回查询结果。select * from t for update nowait 不等待行锁释放，提示锁冲突，不返回结果select * from t for update wait 5 等待5秒，若行锁仍未释放，则提示锁冲突，不返回结果select * from t for update skip locked 查询返回查询结果，但忽略有行锁的记录



091说一下mysql死锁的原因和处理方法事务 a表 t  id=100 更新  加行锁表 t  id=200 更新  已加锁事务 b表 t  id=200 更新 加行锁表 t  id=100 更新 已加锁




死锁与锁等待是两个概念
如未开启事务，多个客户端执行的insert操作


当多个事务同时持有和请求同一资源上的锁而产生循环依赖的时候就产生了死锁。

排查：

正在运行的任务
show full processlist;  找到卡主的进程


解开死锁
UNLOCK TABLES ；


查看当前运行的事务
SELECT * FROM information_schema.INNODB_TRX;


当前出现的锁
SELECT * FROM information_schema.INNODB_LOCKS;


观察错误日志
查看InnoDB锁状态
show status like &quot;innodb_row_lock%&quot;;



lnnodb_row_lock_current_waits:当前正在等待锁定的数量;lnnodb_row_lock_time :从系统启动到现在锁定的总时间长度，单位ms;Innodb_row_lock_time_avg :每次等待所花平均时间;Innodb_row_lock_time_max:从系统启动到现在等待最长的一次所花的时间;lnnodb_row_lock_waits :从系统启动到现在总共等待的次数。

kill  id 杀死进程

解决：

死锁无法避免，上线前要进行严格的压力测试

快速失败

innodb_lock_wait_timeout 行锁超时时间


拆分sql，严禁大事务

充分利用索引，优化索引，尽量把有风险的事务sql使用上覆盖索，优化where条件前缀匹配，提升查询速度，引减少表锁

无法避免时：

操作多张表时，尽量以相同的顺序来访问避免形成等待环路
单张表时先排序再操作
使用排它锁 比如 for update



092 Mysql会产生几种日志？
错误日志（error log）

error log主要记录MySQL在启动、关闭或者运行过程中的错误信息，在MySQL的配置文件my.cnf中，可以通过log-error&#x3D;&#x2F;var&#x2F;log&#x2F;mysqld.log 执行mysql错误日志的位置。

慢查询日志（slow query log）

0.1秒
Ø MySQL的慢查询日志是MySQL提供的一种日志记录，它用来记录在MySQL中响应时间超过阀值的语句，具体指运行时间超过long_query_time值的SQL，则会被记录到慢查询日志中。 
Ø long_query_time的默认值为10，意思是运行10秒以上的语句。 
Ø 由他来查看哪些SQL超出了我们的最大忍耐时间值，比如一条sql执行超过5秒钟，我们就算慢SQL，希望能收集超过5秒的sql，结合之前explain进行全面分析。
Ø 默认情况下，MySQL数据库没有开启慢查询日志，需要我们手动来设置这个参数。
Ø 当然，如果不是调优需要的话，一般不建议启动该参数，因为开启慢查询日志会或多或少带来一定的性能影响。慢查询日志支持将日志记录写入文件。
 在生产环境中，如果要手工分析日志，查找、分析SQL，显然是个体力活，MySQL提供了日志分析工具mysqldumpslow。

一般查询日志（general log）

general log 记录了客户端连接信息以及执行的SQL语句信息，通过MySQL的命令

重写日志（redo log）

回滚日志（undo log）

二进制日志（bin log）


093 	bin log作用是什么？MySQL的bin log日志是用来记录MySQL中增删改时的记录日志。
当你的一条sql操作对数据库中的内容进行了更新，就会增加一条bin log日志。查询操作不会记录到bin log中。
bin log最大的用处就是进行主从复制，以及数据库的恢复。
094	redo log作用是什么？redo log是一种基于磁盘的数据结构，用来在MySQL宕机情况下将不完整的事务执行数据纠正，redo日志记录事务执行后的状态。
当事务开始后，redo log就开始产生，并且随着事务的执行不断写入redo log file中。redo log file中记录了xxx页做了xx修改的信息，我们都知道数据库的更新操作会在内存中先执行，最后刷入磁盘。
redo log就是为了恢复更新了内存但是由于宕机等原因没有刷入磁盘中的那部分数据。
095 	undo log作用是什么？undo log主要用来回滚到某一个版本，是一种逻辑日志。
undo log记录的是修改之前的数据，比如：当delete一条记录时，undolog中会记录一条对应的insert记录，从而保证能恢复到数据修改之前。在执行事务回滚的时候，就可以通过undo log中的记录内容并以此进行回滚。
undo log还可以提供多版本并发控制下的读取（MVCC）。
096 	Mysql日志是否实时写入磁盘？ 097 		bin log刷盘机制是如何实现的？098 		redo log刷盘机制是如何实现的？  099 		undo log刷盘机制是如何实现的？磁盘写入固然是比较慢的。
参数：sync_binlog
binlog 写入策略：
1、sync_binlog&#x3D;0 的时候，表示每次提交事务binlog不会马上写入到磁盘，而是先写到page cache,相对于磁盘写入来说写page cache要快得多,不过在Mysql 崩溃的时候会有丢失日志的风险。
2、sync_binlog&#x3D;1 的时候，表示每次提交事务都会执行 fsync 写入到磁盘 ；
3、sync_binlog的值大于1 的时候，表示每次提交事务都 先写到page cach，只有等到积累了N个事务之后才fsync 写入到磁盘，同样在此设置下Mysql 崩溃的时候会有丢失N个事务日志的风险。
很显然三种模式下，sync_binlog&#x3D;1 是强一致的选择，选择0或者N的情况下在极端情况下就会有丢失日志的风险，具体选择什么模式还是得看系统对于一致性的要求。

innodb_flush_log_at_trx_commit
取值0：每秒（一秒钟内提交的事务）写入磁盘  每秒触发一次缓存日志回写磁盘操作，并调用操作系统fsync刷新IO缓存。取值1：有事务提交就立即刷盘     每次提交事务都立即调用操作系统fsync刷新IO缓存。取值2：每次事务提交 都写给操作系统 由系统接管什么时候写入磁盘   每次都把redo log写到系统的page cache中，由系统接管什么时候写入磁盘



时机顺序： 

1 开启事务

2 查询数据库中需要更新的字段，加载到内存中 形成数据脏页

3 记录undo log到内存缓冲区（用于回滚和mvcc）并关联redo log   -&gt; 可刷盘

4 记录 redo log到内存缓冲区 （用于失败重放）准备提交事务           -&gt; 可刷盘

5 修改内存中的脏页数据

6 提交事务触发redolog刷盘

7 undo log 和脏页 刷盘

8 事务成功
redo log 与 binlog 的两阶段提交


redo log 的写入拆成了两个步骤：prepare 和 commit

prepare：redolog写入log buffer，并fsync持久化到磁盘，在redolog事务中记录2PC的XID，在redolog事务打上prepare标识

commit：binlog写入log buffer，并fsync持久化到磁盘，在binlog事务中记录2PC的XID，同时在redolog事务打上commit标识



100 MySQL的binlog有有几种录入格式？分别有什么区别？logbin格式：

binlog_format&#x3D;STATEMENT（默认）：数据操作的时间，同步时不一致 每一条会修改数据的sql语句会记录到binlog中。优点是并不需要记录每一 条sql语句和每一行的 数据变化，减少了binlog日志量，节约IO，提高性能。缺点是在某些情况下会导致 master-slave 中的数据不一致( 如sleep()函数， last_insert_id()，以及user-defined functions(udf)等会 出	现 问题)
binlog_format&#x3D;ROW：批量数据操作时，效率低   不记录每条sql语句的上下文信息，仅需记录哪条数据被修改了，修改成什么样 了。而且不会出 现某些特定情况下的存储过程、或function、或trigger的调用和触发无法被正确复制的 问题。缺 点是会产生大量的日志，尤其是alter table的时候会让日志暴涨。
binlog_format&#x3D;MIXED：是以上两种level的混合使用，有函数用ROW，没函数用STATEMENT，但是无法识别系统变量

101 Mysql集群同步时为什么使用binlog？优缺点是什么？
binlog是mysql提供的日志，所有存储引擎都可用。
支持增量同步
binlog还可以供其他中间件读取，比如同步到hdfs中
如果复制表数据：
不支持某个阶段回放
直接复制数据过程中一旦中断复制（比如断网），很难确定复制的offset



四 Mysql开发102 可以使用MySQL直接存储文件吗？可以使用 BLOB (binary large object)，用来存储二进制大对象的字段类型。
TinyBlob 255 值的长度加上用于记录长度的1个字节(8位)Blob 65K值的长度加上用于记录长度的2个字节(16位)MediumBlob 16M值的长度加上用于记录长度的3个字节(24位)LongBlob 4G 值的长度加上用于记录长度的4个字节(32位)。
103	什么时候存，什么时候不存？存：需要高效查询并且文件很小的时候
不存：文件比较大，数据量多或变更频繁的时候
104	存储的时候有遇到过什么问题吗？
上传数据过大sql执行失败 调整max_allowed_packet
主从同步数据时比较慢
应用线程阻塞
占用网络带宽
高频访问的图片无法使用浏览器缓存

105	Emoji乱码怎么办？使用utf8mb4
MySQL在5.5.3之后增加了这个utf8mb4的编码，mb4就是most bytes 4的意思，专门用来兼容四字节的unicode。好在utf8mb4是utf8的超集，除了将编码改为utf8mb4外不需要做其他转换。当然，一般情况下使用utf8也就够了。
106 如何存储ip地址？
使用字符串
使用无符号整型


4个字节即解决问题
可以支持范围查询
INET_ATON() 和 INET_NTOA() ipv6 使用 INET6_ATON() 和 INET6_NTOA()

107 长文本如何存储？可以使用Text存储
TINYTEXT(255长度)
TEXT(65535)
MEDIUMTEXT（int最大值16M）
LONGTEXT(long最大值4G)
108	大段文本如何设计表结构？
或将大段文本同时存储到搜索引擎
分表存储
分表后多段存储

109	大段文本查找时如何建立索引？
全文检索，模糊匹配最好存储到搜索引擎中
指定索引长度
分段存储后创建索引

110	有没有在开发中使用过TEXT,BLOB 数据类型 BLOB 之前做ERP的时候使用过，互联网项目一般不用BLOB 
TEXT  文献，文章，小说类，新闻，会议内容 等
111  日期，时间如何存取？
使用 TIMESTAMP，DATETIME
使用字符串

112	TIMESTAMP，DATETIME 的区别是什么？跨时区的业务使用 TIMESTAMP，TIMESTAMP会有时区转换
1、两者的存储方式不一样:对于TIMESTAMP，它把客户端插入的时间从当前时区转化为UTC（世界标准时间）进行存储。查询时，将其又转化为客户端当前时区进行返回。而对于DATETIME，不做任何改变，基本上是原样输入和输出。
2、存储字节大小不同



数据类型
MySQL 5.6.4之前需要存储
MySQL 5.6.4之后需要存储



DATETIME
8 bytes
5 bytes + 小数秒存储


TIMESTAMP
4 bytes
4 bytes + 小数秒存储





分秒数精度
存储字节大小



0
0 bytes


1,2
1 bytes


3,4
2 bytes


5,6
3 bytes


 3、两者所能存储的时间范围不一样:timestamp所能存储的时间范围为：’1970-01-01 00:00:01.000000’ 到 ‘2038-01-19 03:14:07.999999’。datetime所能存储的时间范围为：’1000-01-01 00:00:00.000000’ 到 ‘9999-12-31 23:59:59.999999’。
113	为什么不使用字符串存储日期？字符串无法完成数据库内部的范围筛选
在大数据量存储优化索引时，查询必须加上时间范围
114	如果需要使用时间戳 timestamp和int该如何选择？int 存储空间小，运算查询效率高，不受时区影响，精度低
timestamp 存储空间小，可以使用数据库内部时间函数比如更新，精度高，需要注意时区转换，timestamp更易读
一般选择timestamp，两者性能差异不明显，本质上存储都是使用的int
115	char与varchar的区别？如何选择？1.char的优点是存储空间固定（最大255），没有碎片，尤其更新比较频繁的时候，方便数据文件指针的操作，所以存储读取速度快。缺点是空间冗余，对于数据量大的表，非固定长度属性使用char字段，空间浪费。
2.varchar字段，存储的空间根据存储的内容变化，空间长度为L+size，存储内容长度加描述存储内容长度信息，优点就是空间节约，缺点就是读取和存储时候，需要读取信息计算下标，才能获取完整内容。
116	财务计算有没有出现过错乱？第一类：锁包括多线程，数据库，UI展示后超时提交等
第二类：应用与数据库浮点运算精度丢失

应用开发问题：多线程共享数据读写，
之前有过丢失精度的问题，使用decimal解决
使用乘法替换除法
使用事务保证acid特性
更新时使用悲观锁 SELECT … FOR UPDATE
数据只有标记删除
记录详细日志方便溯源

117		decimal与float,double的区别是什么？float：浮点型，4字节，32bit。
double：双精度实型，8字节，64位
decimal：数字型，128bit，不存在精度损失
对于声明语法DECIMAL(M,D)，自变量的值范围如下：

M是最大位数（精度），范围是1到65。可不指定，默认值是10。
D是小数点右边的位数（小数位）。范围是0到30，并且不能大于M，可不指定，默认值是0。

例如字段 salary DECIMAL(5,2)，能够存储具有五位数字和两位小数的任何值，因此可以存储在salary列中的值的范围是从-999.99到999.99。
118		浮点类型如何选型？为什么？
需要不丢失精度的计算使用DECIMAL

仅用于展示没有计算的小数存储可以使用字符串存储

低价值数据允许计算后丢失精度可以使用float double

整型记录不会出现小数的不要使用浮点类型


119   预编译sql是什么？完整解释：
https://dev.mysql.com/doc/refman/8.0/en/prepare.html
PreparedStatement
120		预编译sql有什么好处？
预编译sql会被mysql缓存下来
作用域是每个session，对其他session无效，重新连接也会失效
提高安全性防止sql注入
select * from user where id &#x3D;?
“1;delete from user where id &#x3D; 1”;


编译语句有可能被重复调用，也就是说sql相同参数不同在同一session中重复查询执行效率明显比较高
mysql 5,8 支持服务器端的预编译

121	子查询与join哪个效率高？子查询虽然很灵活，但是执行效率并不高。
122		为什么子查询效率低？在执行子查询的时候，MYSQL创建了临时表，查询完毕后再删除这些临时表
子查询的速度慢的原因是多了一个创建和销毁临时表的过程。而join 则不需要创建临时表 所以会比子查询快一点
123		join查询可以无限叠加吗？Mysql对join查询有什么限制吗？建议join不超过3张表关联，mysql对内存敏感，关联过多会占用更多内存空间，使性能下降
Too many tables; MySQL can only use 61 tables in a join；
系统限制最多关联61个表
124  join 查询算法了解吗？
Simple Nested-Loop Join：SNLJ，简单嵌套循环连接
Index Nested-Loop Join：INLJ，索引嵌套循环连接
Block Nested-Loop Join：BNLJ，缓存块嵌套循环连接

125		如何优化过多join查询关联？
适当使用冗余字段减少多表关联查询
驱动表和被驱动表（小表join大表）
业务允许的话 尽量使用inner join 让系统帮忙自动选择驱动表
关联字段一定创建索引
调整JOIN BUFFER大小

126	是否有过mysql调优经验？调优：

sql调优
表（结构）设计调优
索引调优
慢查询调优
操作系统调优
数据库参数调优

127		开发中使用过哪些调优工具？官方自带：

EXPLAIN 
mysqldumpslow
show profiles 时间
optimizer_trace

第三方：性能诊断工具，参数扫描提供建议，参数辅助优化
128		如何监控线上环境中执行比较慢的sql？ 129		如何分析一条慢sql？开启慢查询日志，收集sql
Ø 默认情况下，MySQL数据库没有开启慢查询日志，需要我们手动来设置这个参数。
Ø 当然，如果不是调优需要的话，一般不建议启动该参数，因为开启慢查询日志会或多或少带来一定的性能影响。慢查询日志支持将日志记录写入文件。
查看及开启

默认关闭

SHOW VARIABLES LIKE &#39;%slow_query_log%&#39;; 
默认情况下slow_query_log的值为OFF，表示慢查询日志是禁用的， 
​                               

开启：set global     slow_query_log=1; 只对窗口生效，重启服务失效

慢查询日志记录long_query_time时间


SHOW VARIABLES LIKE &#x27;%long_query_time%&#x27;;SHOW GLOBAL VARIABLES LIKE &#x27;long_query_time&#x27;;

l 全局变量设置，对所有客户端有效。但，必须是设置后进行登录的客户端。
SET GLOBAL long_query_time&#x3D;0.1;
l 对当前会话连接立即生效，对其他客户端无效。
SET SESSION long_query_time&#x3D;0.1; #session可省略
假如运行时间正好等于long_query_time的情况，并不会被记录下来。也就是说，
在mysql源码里是判断大于long_query_time，而非大于等于。

永久生效


修改配置文件my.cnf（其它系统变量也是如此） 
[mysqld]下增加或修改参数
slow_query_log 和slow_query_log_file后，然后重启MySQL服务器。也即将如下两行配置进my.cnf文件



slow_query_log &#x3D;1
slow_query_log_file&#x3D;&#x2F;var&#x2F;lib&#x2F;mysql&#x2F;localhost-slow.log
long_query_time&#x3D;3
log_output&#x3D;FILE


关于慢查询的参数slow_query_log_file，它指定慢查询日志文件的存放路径，如果不设置，系统默认文件：[host_name]-slow.log



case
Ø 记录慢SQL并后续分析
SELECT * FROM emp;
SELECT * FROM emp WHERE deptid &gt; 1;
Ø 查询当前系统中有多少条慢查询记录或者直接看慢查询日志
&#x2F;var&#x2F;lib&#x2F;mysql&#x2F;localhost-slow.log
SHOW GLOBAL STATUS LIKE ‘%Slow_queries%’; 
 日志分析工具mysqldumpslow

在生产环境中，如果要手工分析日志，查找、分析SQL，显然是个体力活，MySQL提供了日志分析工具mysqldumpslow。

查看mysqldumpslow的帮助信息


a)   mysqldumpslow --help·    -a: 将数字抽象成N，字符串抽象成S·    -s: 是表示按照何种方式排序； c: 访问次数 l: 锁定时间 r: 返回记录 **t:** **查询时间** al:平均锁定时间 ar:平均返回记录数 at:平均查询时间·    -t: 即为返回前面多少条的数据；·    -g: 后边搭配一个正则匹配模式，大小写不敏感的；  得到返回记录集最多的10个SQL  mysqldumpslow  -s r -t 10 /var/lib/mysql/localhost-slow.log  得到访问次数最多的10个SQL  mysqldumpslow  -s c -t 10 /var/lib/mysql/localhost-slow.log  得到按照时间排序的前10条里面含有左连接的查询语句  mysqldumpslow  -s t -t 10 -g  &quot;left join&quot;  /var/lib/mysql/localhost-slow.log  另外建议在使用这些命令时结合 | 和more 使用 ，否则有可能出现爆屏情况  mysqldumpslow  -s r -t 10 /var/lib/mysql/localhost-slow.log | more  







130		如何查看当前sql使用了哪个索引？可以使用EXPLAIN，选择索引过程可以使用 optimizer_trace
131	索引如何进行分析和调优？132	EXPLAIN关键字中的重要指标有哪些？EXPLAIN是什么使用EXPLAIN关键字可以模拟优化器执行SQL查询语句，从而知道MySQL是如何处理你的SQL语句的。分析你的查询语句或是表结构的性能瓶颈。
EXPLAIN的用法用法：
EXPLAIN + SQL语句



数据准备：
USE atguigudb; CREATE TABLE t1(id INT(10) AUTO_INCREMENT, content VARCHAR(100) NULL, PRIMARY KEY (id));CREATE TABLE t2(id INT(10) AUTO_INCREMENT, content VARCHAR(100) NULL, PRIMARY KEY (id));CREATE TABLE t3(id INT(10) AUTO_INCREMENT, content VARCHAR(100) NULL, PRIMARY KEY (id));CREATE TABLE t4(id INT(10) AUTO_INCREMENT, content1 VARCHAR(100) NULL, content2 VARCHAR(100) NULL, PRIMARY KEY (id));CREATE INDEX idx_content1 ON t4(content1);  -- 普通索引# 以下新增sql多执行几次，以便演示INSERT INTO t1(content) VALUES(CONCAT(&#x27;t1_&#x27;,FLOOR(1+RAND()*1000)));INSERT INTO t2(content) VALUES(CONCAT(&#x27;t2_&#x27;,FLOOR(1+RAND()*1000)));INSERT INTO t3(content) VALUES(CONCAT(&#x27;t3_&#x27;,FLOOR(1+RAND()*1000)));INSERT INTO t4(content1, content2) VALUES(CONCAT(&#x27;t4_&#x27;,FLOOR(1+RAND()*1000)), CONCAT(&#x27;t4_&#x27;,FLOOR(1+RAND()*1000)));



各字段解释table
**单表：**显示这一行的数据是关于哪张表的

EXPLAIN SELECT * FROM t1;



**多表关联：**t1为驱动表，t2为被驱动表。

注意：内连接时，MySQL性能优化器会自动判断哪个表是驱动表，哪个表示被驱动表，和书写的顺序无关
EXPLAIN SELECT * FROM t1 INNER JOIN t2;


id表示查询中执行select子句或操作表的顺序

**id相同：**执行顺序由上至下

EXPLAIN SELECT * FROM t1, t2, t3;



**id不同：**如果是子查询，id的序号会递增，id值越大优先级越高，越先被执行

EXPLAIN SELECT t1.id FROM t1 WHERE t1.id =(  SELECT t2.id FROM t2 WHERE t2.id =(    SELECT t3.id FROM t3 WHERE t3.content = &#x27;t3_434&#x27;  ));


注意：查询优化器可能对涉及子查询的语句进行优化，转为连接查询
EXPLAIN SELECT * FROM t1 WHERE content IN (SELECT content FROM t2 WHERE content = &#x27;a&#x27;);



**id为NULL：**最后执行

EXPLAIN SELECT * FROM t1 UNION SELECT * FROM t2;


小结：

id如果相同，可以认为是一组，从上往下顺序执行
在所有组中，id值越大，优先级越高，越先执行
关注点：id号每个号码，表示一趟独立的查询, 一个sql的查询趟数越少越好

select_type查询的类型，主要是用于区别普通查询、联合查询、子查询等的复杂查询。

**SIMPLE：**简单查询。查询中不包含子查询或者UNION。

EXPLAIN SELECT * FROM t1;



**PRIMARY：**主查询。查询中若包含子查询，则最外层查询被标记为PRIMARY。
**SUBQUERY：**子查询。在SELECT或WHERE列表中包含了子查询。

EXPLAIN SELECT * FROM t3 WHERE id = ( SELECT id FROM t2 WHERE content= &#x27;a&#x27;);



**DEPENDENT SUBQUREY：**如果包含了子查询，并且查询语句不能被优化器转换为连接查询，并且子查询是相关子查询（子查询基于外部数据列），则子查询就是DEPENDENT SUBQUREY。

EXPLAIN SELECT * FROM t3 WHERE id = ( SELECT id FROM t2 WHERE content = t3.content);



**UNCACHEABLE SUBQUREY：**表示这个subquery的查询要受到外部系统变量的影响

EXPLAIN SELECT * FROM t3 WHERE id = ( SELECT id FROM t2 WHERE content = @@character_set_server);



**UNION：**对于包含UNION或者UNION ALL的查询语句，除了最左边的查询是PRIMARY，其余的查询都是UNION。
**UNION RESULT：**UNION会对查询结果进行查询去重，MYSQL会使用临时表来完成UNION查询的去重工作，针对这个临时表的查询就是”UNION RESULT”。

EXPLAIN SELECT * FROM t3 WHERE id = 1 UNION  SELECT * FROM t2 WHERE id = 1;



**DEPENDENT UNION：**子查询中的UNION或者UNION ALL，除了最左边的查询是DEPENDENT SUBQUREY，其余的查询都是DEPENDENT UNION。

EXPLAIN SELECT * FROM t1 WHERE content IN(SELECT content FROM t2 UNION SELECT content FROM t3);



**DERIVED：**在包含派生表（子查询在from子句中）的查询中，MySQL会递归执行这些子查询，把结果放在临时表里。

EXPLAIN SELECT * FROM (   SELECT content, COUNT(*) AS c FROM t1 GROUP BY content) AS derived_t1 WHERE c &gt; 1;

这里的&lt;derived2&gt;就是在id为2的查询中产生的派生表。

**补充：**MySQL在处理带有派生表的语句时，优先尝试把派生表和外层查询进行合并，如果不行，再把派生表物化掉（执行子查询，并把结果放入临时表），然后执行查询。下面的例子就是就是将派生表和外层查询进行合并的例子：
EXPLAIN SELECT * FROM (SELECT * FROM t1 WHERE content = &#x27;t1_832&#x27;) AS derived_t1;



**MATERIALIZED：**优化器对于包含子查询的语句，如果选择将子查询物化后再与外层查询连接查询，该子查询的类型就是MATERIALIZED。如下的例子中，查询优化器先将子查询转换成物化表，然后将t1和物化表进行连接查询。

EXPLAIN SELECT * FROM t1 WHERE content IN (SELECT content FROM t2);


partitions代表分区表中的命中情况，非分区表，该项为NULL
type ☆
说明：
结果值从最好到最坏依次是： 
system &gt; const &gt; eq_ref &gt; ref &gt; fulltext &gt; ref_or_null &gt; index_merge &gt; unique_subquery &gt; index_subquery &gt; range &gt; index &gt; ALL 
比较重要的包含：system、const 、eq_ref 、ref、range &gt; index &gt; ALL
SQL 性能优化的目标：至少要达到 range 级别，要求是 ref 级别，最好是 consts级别。（阿里巴巴开发手册要求）


**ALL：**全表扫描。Full Table Scan，将遍历全表以找到匹配的行

EXPLAIN SELECT * FROM t1;



**index：**当使用覆盖索引，但需要扫描全部的索引记录时

覆盖索引：如果能通过读取索引就可以得到想要的数据，那就不需要读取用户记录，或者不用再做回表操作了。一个索引包含了满足查询结果的数据就叫做覆盖索引。 
-- 只需要读取聚簇索引部分的非叶子节点，就可以得到id的值，不需要查询叶子节点EXPLAIN SELECT id FROM t1;


-- 只需要读取二级索引，就可以在二级索引中获取到想要的数据，不需要再根据叶子节点中的id做回表操作EXPLAIN SELECT id, deptId FROM t_emp;



**range：**只检索给定范围的行，使用一个索引来选择行。key 列显示使用了哪个索引，一般就是在你的where语句中出现了between、&lt;、&gt;、in等的查询。这种范围扫描索引扫描比全表扫描要好，因为它只需要开始于索引的某一点，而结束于另一点，不用扫描全部索引。

EXPLAIN SELECT * FROM t1 WHERE id IN (1, 2, 3);



**ref：**通过普通二级索引列与常量进行等值匹配时

EXPLAIN SELECT * FROM t_emp WHERE deptId = 1;



**eq_ref：**连接查询时通过主键或不允许NULL值的唯一二级索引列进行等值匹配时

EXPLAIN SELECT * FROM t1, t2 WHERE t1.id = t2.id;



**const：**根据主键或者唯一二级索引列与常数进行匹配时

EXPLAIN SELECT * FROM t1 WHERE id = 1;



**system：**MyISAM引擎中，当表中只有一条记录时。（这是所有type的值中性能最高的场景）

CREATE TABLE t(i int) Engine=MyISAM;INSERT INTO t VALUES(1);EXPLAIN SELECT * FROM t;


其他不太常见的类型（了解）：

index_subquery：利用普通索引来关联子查询，针对包含有IN子查询的查询语句。content1是普通索引字段

EXPLAIN SELECT * FROM t1 WHERE content IN (SELECT content1 FROM t4 WHERE t1.content = t4.content2) OR content = &#x27;a&#x27;;



unique_subquery：类似于index_subquery，利用唯一索引来关联子查询。t2的id是主键，也可以理解为唯一的索引字段

EXPLAIN SELECT * FROM t1 WHERE id IN (SELECT id FROM t2 WHERE t1.content = t2.content) OR content = &#x27;a&#x27;;



index_merge：在查询过程中需要多个索引组合使用，通常出现在有 or 的关键字的sql中。

EXPLAIN SELECT * FROM t_emp WHERE deptId = 1 OR id = 1;



ref_or_null：当对普通二级索引进行等值匹配，且该索引列的值也可以是NULL值时。

EXPLAIN SELECT * FROM t_emp WHERE deptId = 1 OR deptId IS NULL;



**fulltext：**全文索引。一般通过搜索引擎实现，这里我们不展开。

possible_keys 和 keys ☆
possible_keys表示执行查询时可能用到的索引，一个或多个。 查询涉及到的字段上若存在索引，则该索引将被列出，但不一定被查询实际使用。

keys表示实际使用的索引。如果为NULL，则没有使用索引。


EXPLAIN SELECT id FROM t1 WHERE id = 1;


key_len ☆表示索引使用的字节数，根据这个值可以判断索引的使用情况，检查是否充分利用了索引，针对联合索引值越大越好。
如何计算：

先看索引上字段的类型+长度。比如：int&#x3D;4 ; varchar(20) &#x3D;20 ; char(20) &#x3D;20 
如果是varchar或者char这种字符串字段，视字符集要乘不同的值，比如utf8要乘 3，如果是utf8mb4要乘4，GBK要乘2
varchar这种动态字符串要加2个字节
允许为空的字段要加1个字节

-- 创建索引CREATE INDEX idx_age_name ON t_emp(age, `name`);-- 测试1EXPLAIN SELECT * FROM t_emp WHERE age = 30 AND `name` = &#x27;ab%&#x27;;-- 测试2EXPLAIN SELECT * FROM t_emp WHERE age = 30;


ref显示与key中的索引进行比较的列或常量。
-- ref=atguigudb.t1.id   关联查询时出现，t2表和t1表的哪一列进行关联EXPLAIN SELECT * FROM t1, t2 WHERE t1.id = t2.id;-- ref=const  与索引列进行等值比较的东西是啥，const表示一个常数EXPLAIN SELECT * FROM t_emp WHERE age = 30;


rows ☆MySQL认为它执行查询时必须检查的行数。值越小越好。
-- 如果是全表扫描，rows的值就是表中数据的估计行数EXPLAIN SELECT * FROM t_emp WHERE empno = &#x27;10001&#x27;;-- 如果是使用索引查询，rows的值就是预计扫描索引记录行数EXPLAIN SELECT * FROM t_emp WHERE deptId = 1;


filtered最后查询出来的数据占所有服务器端检查行数（rows）的百分比。值越大越好。
-- 先根据二级索引deptId找到数据的主键，有3条记录满足条件，-- 再根据主键进行回表，最终找到3条记录，有100%的记录满足条件EXPLAIN SELECT * FROM t_emp WHERE deptId = 1;-- 这个例子如果name列是索引列则 filtered = 100 否则filtered = 10(全表扫描)EXPLAIN SELECT * FROM t_emp WHERE `name` = &#x27;风清扬&#x27;;


Extra ☆包含不适合在其他列中显示但十分重要的额外信息。通过这些额外信息来理解MySQL到底将如何执行当前的查询语句。MySQL提供的额外信息有好几十个，这里只挑介绍比较重要的介绍。

Impossible WHERE：where子句的值总是false

EXPLAIN SELECT * FROM t_emp WHERE 1 != 1;



**Using where：**使用了where，但在where上有字段没有创建索引

EXPLAIN SELECT * FROM t_emp WHERE `name` = &#x27;风清扬&#x27;;



**Using temporary：**使了用临时表保存中间结果

EXPLAIN SELECT DISTINCT content FROM t1;



Using filesort：

在对查询结果中的记录进行排序时，是可以使用索引的，如下所示：
EXPLAIN SELECT * FROM t1 ORDER BY id;


如果排序操作无法使用到索引，只能在内存中（记录较少时）或者磁盘中（记录较多时）进行排序（filesort），如下所示：
EXPLAIN SELECT * FROM t1 ORDER BY content;



**Using index：**使用了覆盖索引，表示直接访问索引就足够获取到所需要的数据，不需要通过索引回表

EXPLAIN SELECT id, content1 FROM t4;


EXPLAIN SELECT id FROM t1;



**Using index condition：**叫作  Index Condition Pushdown Optimization （索引下推优化）
如果没有索引下推（ICP），那么MySQL在存储引擎层找到满足content1 &gt; &#39;z&#39;条件的第一条二级索引记录。主键值进行回表，返回完整的记录给server层，server层再判断其他的搜索条件是否成立。如果成立则保留该记录，否则跳过该记录，然后向存储引擎层要下一条记录。
如果使用了索引下推（ICP），那么MySQL在存储引擎层找到满足content1 &gt; &#39;z&#39;条件的第一条二级索引记录。不着急执行回表，而是在这条记录上先判断一下所有关于idx_content1索引中包含的条件是否成立，也就是content1 &gt; &#39;z&#39; AND content1 LIKE &#39;%a&#39;是否成立。如果这些条件不成立，则直接跳过该二级索引记录，去找下一条二级索引记录；如果这些条件成立，则执行回表操作，返回完整的记录给server层。



-- content1列上有索引idx_content1EXPLAIN SELECT * FROM t4 WHERE content1 &gt; &#x27;z&#x27; AND content1 LIKE &#x27;%a&#x27;;


**注意：**如果这里的查询条件只有content1 &gt; &#39;z&#39;，那么找到满足条件的索引后也会进行一次索引下推的操作，判断content1 &gt; ‘z’是否成立（这是源码中为了编程方便做的冗余判断）


**Using join buffer：**在连接查询时，当被驱动表不能有效的利用索引时，MySQL会为其分配一块名为连接缓冲区（join buffer）的内存来加快查询速度

EXPLAIN  SELECT * FROM t1, t2 WHERE t1.content = t2.content;


下面这个例子就是被驱动表使用了索引：
EXPLAIN SELECT * FROM t_emp, t_dept WHERE t_dept.id = t_emp.deptId;


133 MySQL数据库cpu飙升的话你会如何分析重点是定位问题。
先
1 使用top观察mysqld的cpu利用率

切换到常用的数据库

使用show full processlist;查看会话

观察是哪些sql消耗了资源，其中重点观察state指标

定位到具体sql


2 pidstat 

定位到线程
在PERFORMANCE_SCHEMA.THREADS中记录了thread_os_id 找到线程执行的sql
根据操作系统id可以到processlist表找到对应的会话
在会话中即可定位到问题sql

3 使用show profile观察sql各个阶段耗时
4 服务器上是否运行了其他程序
5 检查一下是否有慢查询
6 pref top
使用pref 工具分析哪些函数引发的cpu过高来追踪定位

134 有没有进行过分库分表？135 	什么是分库分表？垂直分库
一个数据库由很多表的构成，每个表对应着不同的业务，垂直切分是指按照业务将表进行分类，分布到不同 的数据库上面，这样也就将数据或者说压力分担到不同的库上面，如下图：       
​                   
系统被切分成了，用户，订单交易，支付几个模块。
水平分表
把一张表里的内容按照不同的规则 写到不同的库里
相对于垂直拆分，水平拆分不是将表做分类，而是按照某个字段的某种规则来分散到多个库之中，每个表中包含一部分数据。简单来说，我们可以将数据的水平切分理解为是按照数据行的切分，就是将表中的某些行切分 到一个数据库，而另外的某些行又切分到其他的数据库中，如图： 

136 	什么时候进行分库分表？有没有配合es使用经验？
能不分就不分
单机性能下降明显的时候
增加缓存（通常查询量比较大），细分业务
首先尝试主被集群，读写分离
尝试分库
尝试分表 -&gt; 冷热数据分离

大数据量下可以配合es完成高效查询
137 	说一下实现分库分表工具的实现思路
伪装成mysql服务器，代理用户请求转发到真实服务器
基于本地aop实现，拦截sql，改写，路由和结果归集处理。

138 	用过哪些分库分表工具？
139 	分库分表后可能会有哪些问题？经典的问题

执行效率明显降低
表结构很难再次调整
引发分布式id问题
产生跨库join
代理类中间件网络io成为瓶颈

140 	说一下读写分离常见方案？

141 为什么要使用视图？ 什么是视图？视图定义：1、视图是一个虚表，是从一个或几个基本表（或视图）导出的表。2、只存放视图的定义，不存放视图对应的数据。3、基表中的数据发生变化，从视图中查询出的数据也随之改变。视图的作用：1、视图能够简化用户的操作2、视图使用户能以多种角度看待同一数据3、视图对重构数据库提供了一定程度的逻辑独立性4、视图能够对机密数据提供安全保护5、适当的利用视图可以更清晰的表达查询
142 什么是存储过程？有没有使用过？项目中禁止使用存储过程，存储过程难以调试和扩展，更没有移植性
143 有没有使用过外键？有什么需要注意的地方？不得使用外键与级联，一切外键概念必须在应用层解决。 
说明：以学生和成绩的关系为例，学生表中的 student_id是主键，那么成绩表中的 student_id 则为外键。如果更新学生表中的 student_id，同时触发成绩表中的 student_id 更新，即为 级联更新。外键与级联更新适用于单机低并发，不适合分布式、高并发集群；级联更新是强阻 塞，存在数据库更新风暴的风险；外键影响数据库的插入速度。
144 用过processlist吗？关键的就是state列，mysql列出的状态主要有以下几种：

Checking table正在检查数据表（这是自动的）。
Closing tables正在将表中修改的数据刷新到磁盘中，同时正在关闭已经用完的表。这是一个很快的操作，如果不是这样的话，就应该确认磁盘空间是否已经满了或者磁盘是否正处于重负中。
Connect Out复制从服务器正在连接主服务器。
Copying to tmp table on disk由于临时结果集大于tmp_table_size，正在将临时表从内存存储转为磁盘存储以此节省内存。
Creating tmp table正在创建临时表以存放部分查询结果。
deleting from main table服务器正在执行多表删除中的第一部分，刚删除第一个表。
deleting from reference tables服务器正在执行多表删除中的第二部分，正在删除其他表的记录。
Flushing tables正在执行FLUSH TABLES，等待其他线程关闭数据表。
Killed发送了一个kill请求给某线程，那么这个线程将会检查kill标志位，同时会放弃下一个kill请求。MySQL会在每次的主循环中检查kill标志位，不过有些情况下该线程可能会过一小段才能死掉。如果该线程程被其他线程锁住了，那么kill请求会在锁释放时马上生效。
Locked被其他查询锁住了。
Sending data正在处理Select查询的记录，同时正在把结果发送给客户端。Sending data”状态的含义，原来这个状态的名称很具有误导性，所谓的“Sending data”并不是单纯的发送数据，而是包括“收集 + 发送 数据”。
Sorting for group正在为GROUP BY做排序。
Sorting for order正在为ORDER BY做排序。
Opening tables这个过程应该会很快，除非受到其他因素的干扰。例如，在执Alter TABLE或LOCK TABLE语句行完以前，数据表无法被其他线程打开。正尝试打开一个表。
Removing duplicates正在执行一个Select DISTINCT方式的查询，但是MySQL无法在前一个阶段优化掉那些重复的记录。因此，MySQL需要再次去掉重复的记录，然后再把结果发送给客户端。
Reopen table获得了对一个表的锁，但是必须在表结构修改之后才能获得这个锁。已经释放锁，关闭数据表，正尝试重新打开数据表。
Repair by sorting修复指令正在排序以创建索引。
Repair with keycache修复指令正在利用索引缓存一个一个地创建新索引。它会比Repair by sorting慢些。
Searching rows for update正在讲符合条件的记录找出来以备更新。它必须在Update要修改相关的记录之前就完成了。
Sleeping正在等待客户端发送新请求.
System lock正在等待取得一个外部的系统锁。如果当前没有运行多个mysqld服务器同时请求同一个表，那么可以通过增加–skip-external-locking参数来禁止外部系统锁。
Upgrading lockInsert DELAYED正在尝试取得一个锁表以插入新记录。&#x3D;
Updating正在搜索匹配的记录，并且修改它们。
User Lock正在等待GET_LOCK()。
Waiting for tables该线程得到通知，数据表结构已经被修改了，需要重新打开数据表以取得新的结构。然后，为了能的重新打开数据表，必须等到所有其他线程关闭这个表。以下几种情况下会产生这个通知：FLUSH TABLES tbl_name, Alter TABLE, RENAME TABLE, REPAIR TABLE, ANALYZE TABLE,或OPTIMIZE TABLE。
waiting for handler insertInsert DELAYED已经处理完了所有待处理的插入操作，正在等待新的请求。

145 某个表有数千万数据，查询比较慢，如何优化？说一下思路
前端优化 减少查询
合并请求:多个请求需要的数据尽量一条sql拿出来
会话保存：和用户会话相关的数据尽量一次取出重复使用
避免无效刷新


多级缓存 不要触及到数据库
应用层热点数据高速查询缓存（低一致性缓存）
高频查询大数据量镜像缓存（双写高一致性缓存）
入口层缓存（几乎不变的系统常量）


使用合适的字段类型，比如varchar换成char
一定要高效使用索引。
使用explain 深入观察索引使用情况
检查select 字段最好满足索引覆盖
复合索引注意观察key_len索引使用情况
有分组，排序，注意file sort，合理配置相应的buffer大小


检查查询是否可以分段查询，避免一次拿出过多无效数据
多表关联查询是否可以设置冗余字段，是否可以简化多表查询或分批查询
分而治之：把服务拆分成更小力度的微服务
冷热数据分库存储
读写分离，主被集群 然后再考虑分库分表
等

146 count(列名)和 count(*)有什么区别？count()是 SQL92 定义的标准统计行数的语法，跟数据库无关，跟 NULL 和非 NULL 无关。说明：count()会统计值为 NULL 的行，而 count(列名)不会统计此列为 NULL 值的行。
147 如果有超大分页改怎么处理？
select name from user limit 10000,10;在 使用的时候并不是跳过 offset 行，而是取 offset+N 行，然后返回放弃前 offset 行，返回 N 行

通过索引优化的方案：

如果主键自增可以 select name from user where id &gt; 10000 limit 10;
延迟关联
需要order by时
一定注意增加筛选条件，避免全表排序
where  -》 order by -》 limit


减少select字段
优化相关参数避免filesort




一般大分页情况比较少（很少有人跳转到几百万页去查看数据），实际互联网业务中多数还是按顺序翻页，可以使用缓存提升前几页的查询效率，实际上大多数知名互联网项目也都是这么做的


在阿里巴巴《Java开发手册》中的建议：
【推荐】利用延迟关联或者子查询优化超多分页场景。 说明：MySQL 并不是跳过 offset 行，而是取 offset+N 行，然后返回放弃前 offset 行，返回 N 行，那当 offset 特别大的时候，效率就非常的低下，要么控制返回的总页数，要么对超过 特定阈值的页数进行 SQL 改写。 正例：先快速定位需要获取的 id 段，然后再关联： SELECT a.* FROM 表 1 a, (select id from 表 1 where 条件 LIMIT 100000,20 ) b where a.id&#x3D;b.id
148 mysql服务器毫无规律的异常重启如何排查问题？首先是查看mysql和系统日志来定位错误
最常见的是关闭swap分区后OOM问题：
mysql 分为应用进程和守护进程
当应用进程内存占用过高的时候操作系统可能会kill掉进程，此时守护进程又帮我们重启了应用进程，运行一段时间后又出现OOM如此反复
可以排查以下几个关键点

运行时内存占用率
mysql buffer相关参数
mysql 网络连接相关参数

异常关机或kill -9 mysql 后导致表文件损坏

直接使用备份
配置 innodb_force_recovery 跳过启动恢复过程

149 mysql 线上修改表结构有哪些风险?针对ddl命令，有以下几种方式

copy table  锁原表，创建临时表并拷贝数据

inplace 针对索引修改删除的优化，不需要拷贝所有数据

Online DDL 细分DDL命令来决定是否锁表

可能会锁表，导致无法读写

ORM中的映射失效

索引失效


建议：建个新表，导入数据后重命名
150 什么是mysql多实例部署？指的是在一台主机上部署多个实例
主要目的是压榨服务器性能
缺点是互相影响
]]></content>
      <categories>
        <category>database</category>
      </categories>
      <tags>
        <tag>MySQL数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>javaScript</title>
    <url>/2026/06/07/web/Java%20Script/</url>
    <content><![CDATA[
 

]]></content>
      <categories>
        <category>web</category>
      </categories>
      <tags>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>tools</title>
    <url>/website/tools/index.html</url>
    <content><![CDATA[
 

]]></content>
  </entry>
  <entry>
    <title>星标索引</title>
    <url>/tags/index.html</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>link</title>
    <url>/website/link/index.html</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>关于我的故事</title>
    <url>/about/index.html</url>
    <content><![CDATA[编程在路上
网瘾少年
  


打工牛马

  


编程求学
  


码农人生
  


辗转深圳
  



魔都之旅

  


生活并不会那么一帆风顺，过去不值得留恋，未来需要更加努力!
  


学而不思则亡，思而不学则怠，学习永无止境！
  


  


未来还在继续，下一站等你
  


只要出发就能到达，梦想从来不曾遥远…….
  
]]></content>
  </entry>
</search>
